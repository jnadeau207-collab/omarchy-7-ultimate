# Writers and the root executor — 2026-09-01

SHA `6e01615d` on `work` (session that landed the root executor and write-plane expansion), rebased onto `upstream/quattro` at `b71dcad9`. Honesty addendum 2026-09-02 vs tip `c192afea`: `entry.trash` write-plane reachability after PR #8 v1 directory family widen is KEEP. The helper-only / schema-blocker paragraphs below are rewritten to residual. Honesty addendum 2026-09-02 vs tip `c1490f82`: Files Trash controls stay gated `trashAuthorized=false` for the shell principal (same invent class as LIVE End Task). Write plane remains reachable at `consequential`; SHELL still refused at `grant.shell-consequential`. Honesty addendum 2026-09-02: `files.trash.restore` write plane is reachable the same way. Real preflight reads `.trashinfo` to derive the restore destination. Risk stays `consequential`. SHELL still refused at `grant.shell-consequential`. Catalog claim=partial; humanRoute planned empty. Files does not offer Restore LIVE. Do not invent Restore LIVE, `files.trash.manage`, Empty Recycle, Recycle Bin product-complete, or LIVE Trash under SHELL. Product REJECTED.

Honesty addendum 2026-09-02: `files.entry.open` launch plane is reachable (`files-entry-open` helper). Real preflight binds entry identity and location scope. Risk is `low`. SHELL grant is allowed. Catalog claim=partial; humanRoute visible `Files > Open`. Agent stays unavailable. Files Open / double-click is LIVE for regular files. File contents are never read. Do not invent Open With / MIME association UI, Restore LIVE, Empty Bin, or Explorer product-complete. Recycle and MIME residuals stay OPEN. Product REJECTED.

Honesty addendum 2026-09-03: `files.entry.rename` write plane is reachable (`files-entry-rename` helper). Real preflight binds entry identity inside the scoped directory listing and keeps the rename in the same directory. Collision, drift, traversal, symlink, and Trash refuse. Risk is `low` (same class as `directory.create`: a reversible same-directory name change, not a Trash relocation). SHELL grant is allowed. Catalog claim=partial; humanRoute visible `Files > Rename`. Agent stays unavailable. Files Rename is LIVE (`renameAuthorized=true`). Do not invent cut/move, Open With / MIME UI, Restore LIVE, Empty Bin, or Explorer product-complete. Recycle and MIME residuals stay OPEN. Product REJECTED.

Honesty addendum 2026-09-03: `files.entry.copy` write plane is reachable (`files-entry-copy` helper). Real preflight binds source identity inside the destination directory listing. Copy writes a scoped regular-file or directory replica into a Files location. Collision, drift, traversal, symlink loops, nest-inside-source, and Trash refuse. Copy maps `EXDEV` errno from `mkdir`/`open` only; overlay or bind-mount children may copy. Risk is `low` (additive replica, same class as `directory.create`, not a cut/move). SHELL grant is allowed. Catalog claim=partial; humanRoute visible `Files > Copy and Paste`. Agent stays unavailable. Files Copy and Paste are LIVE (`copyAuthorized=true`) with in-app staging only. The OS clipboard, thumbnails, Open With / MIME UI, Restore LIVE, Empty Bin, and Explorer product-complete stay leftovers. Recycle and MIME residuals stay OPEN. Product REJECTED.

Honesty addendum 2026-09-03: `files.entry.move` write plane is reachable (`files-entry-move` helper). Real preflight binds source identity inside the destination directory listing. Move relocates a regular file with `os.rename`. Collision, drift, traversal, symlink, directory, Trash, same-directory rename, and cross-device `EXDEV` refuse. Risk is `consequential` (source name leaves its original listing, same class as `entry.trash`, not an additive replica). SHELL grant is refused at `grant.shell-consequential`. Catalog claim=partial. Dated leftover Fabric under SHELL: humanRoute was planned empty and Fabric Cut was unavailable. Tip-true after #77: session Cut is visible `Files > Cut`. Files pins `cutAuthorized=false` as leftover Fabric SHELL refuse only; session Cut does not consult that pin. Folder copy CLOSED via `files.entry.copy` directories; move still refuses directories. Do not invent LIVE Cut under SHELL, an OS clipboard, Restore LIVE, Empty Bin, or Explorer product-complete. Recycle and MIME residuals stay OPEN. Product REJECTED.

Honesty addendum 2026-09-03: `files.entry.delete` write plane is reachable (`files-entry-delete` helper). Real preflight binds entry identity inside the scoped directory listing. Delete unlinks a regular file or rmdirs an empty directory. Drift, traversal, symlink, non-empty directory, and Trash refuse. No recursive `rm -rf`. Risk is `consequential` (same class as `entry.trash` / `entry.move`). SHELL grant is refused at `grant.shell-consequential`. Catalog claim=partial; dated leftover humanRoute planned empty is Fabric under SHELL. Agent stays unavailable. Files pins `deleteAuthorized=false` as leftover Fabric SHELL refuse and does not offer Fabric LIVE Delete. Dated leftover: session Permanent Delete after 2026-09-06 is CLOSED leftover; this paragraph's does not offer LIVE Delete / humanRoute planned empty is Fabric under SHELL. Session Permanent Delete does not consult that pin. Do not invent LIVE Delete under SHELL, Empty Bin, `files.trash.manage`, or Recycle product-complete. Recycle and MIME residuals stay OPEN. Product REJECTED.

Honesty addendum 2026-09-03 vs tip after PR #60 (`c1886b423c11`): Files residuals still named after #60 stay OPEN. Copy/Paste stay in-app only; OS clipboard residual OPEN after PR #60. Recycle Bin / `files.trash.manage` residual OPEN (`availability.claim=missing`). Files pins `deleteAuthorized=false`, `cutAuthorized=false`, `trashAuthorized=false`. Dated leftover: session Cut/Delete after 2026-09-06 is CLOSED leftover; these cut/delete pins are leftover Fabric SHELL refuse and session Cut/Delete do not consult them. Folder copy CLOSED via `files.entry.copy` directories. The permanent delete write plane exists but is not shell-authorizable. Do not invent an OS clipboard, a LIVE Empty Bin, Restore LIVE under SHELL, or Recycle product-complete. Product REJECTED.

Honesty addendum 2026-09-03 vs tip after PR #62 (`ad4a68e1b225`): MIME / Default Programs association UI residual OPEN after PR #62. `defaults.mime.set` write plane is reachable (`defaults-mime-set` helper; preflight OK; risk low). `association.inspect` is published so durable apply can re-read scoped association state. Settings Apps reads MIME inventory via `defaults.inspect`. Settings does not offer MIME LIVE CONTROL. Catalog `defaults.mime.set` keeps claim=partial and humanRoute planned empty. `files.associations.set` stays missing/planned MIME. `parity.file-associations` stays missing as product. Browser writer stays `defaults.protocol.set` (browser LIVE only). Do not invent a present MIME association UI, a LIVE association manager, or claim=present Default Programs product. Product REJECTED.

Honesty addendum 2026-09-03 vs tip after PR #63 (`73ec97959174`): `files.trash.manage` write plane is reachable (`files-trash-manage` helper). Real preflight binds the Trash location listing and empties regular files and empty directories plus their `.trashinfo`. Drift, traversal, symlink, non-empty trash trees, and any location other than Trash refuse. No recursive `rm -rf`. Risk is `consequential` (same class as `entry.trash` / `entry.delete`). SHELL grant is refused at `grant.shell-consequential`. Catalog claim=partial; humanRoute planned empty. Agent stays unavailable. Files pins `emptyBinAuthorized=false` and does not offer Empty Bin LIVE under SHELL. Recycle Bin / Empty Bin LIVE residual OPEN after PR #63. Desktop Recycle icon, Recycle Properties, Restore UI, and Recycle product-complete stay leftovers. Dated leftover: session Restore/Empty after #76 is CLOSED leftover; this paragraph's Empty Bin LIVE not offered / Restore UI leftover is Fabric under SHELL. Do not invent Empty Bin LIVE, Restore LIVE under SHELL, `files.trash.manage` claim=present, or Recycle product-complete. Product REJECTED.

Honesty addendum 2026-09-03 vs tip after PR #64 (`40212d204a1c`): OS clipboard residual OPEN after PR #64. After #64 files.trash.manage plane, the leftover named after #60 stays OPEN. Files Copy and Paste stay in-app staging (`files.entry.copy`; claim=partial; risk low; SHELL-grantable; visible `Files > Copy and Paste`; `copyAuthorized=true`; regular files and directories; folder copy CLOSED). Files does not offer an OS clipboard. There is no Files `wl-copy` / `wl-paste` / `Qt.application.clipboard` bridge. Copy/Paste stay in-app only. Do not invent an OS clipboard LIVE, Explorer claim=present, LIVE Cut under SHELL, a LIVE Empty Bin, a LIVE Restore, or a product-complete Recycle Bin. Product REJECTED.

Honesty addendum 2026-09-03: MIME defaults apply through defaults.provider mime.set. `defaults.mime.set` write plane was already reachable (`defaults-mime-set` helper; preflight OK; risk low); Settings > Default Programs now offers MIME LIVE CONTROL for writable associations with more than one installed candidate (`mimeRows`, `applyMimeDefault`; same preflight/approve/start plane as the browser writer). Refusals stay: already-default and non-candidate selections never reach preflight. Catalog `defaults.mime.set` keeps claim=partial; humanRoute visible `Settings > Default Programs`. `files.associations.set` stays missing/planned MIME. `parity.file-associations` stays missing as product. Do not invent a present Default Programs product, protocol defaults beyond the browser, or claim=present file associations. Product REJECTED.

Honesty addendum 2026-09-05: Settings Apps browser, mailto, and MIME apply were dead until this PR. Provider preflight emits `proposedState.{resourceId,revision,value}` and `normalizedArguments`; the daemon payload builders read `proposedState.defaultAppId` and died with `operation.intent-build-failed`. MIME idempotency keys carrying `/` or `+` (`text/plain`, `image/svg+xml`) died earlier at `operation.idempotency-invalid`. After this PR the builders read `normalizedArguments` and Settings sanitizes MIME keys to the coordinator charset `[A-Za-z0-9._:-]`. Current tree: partial MIME rows LIVE on Settings > Default Programs; Default Programs applet still missing. Catalog `defaults.mime.set` keeps claim=partial and humanRoute visible `Settings > Default Programs`. Dated leftovers after PR #49 and PR #62 stay tip-true. Do not invent Default Programs present, claim the writers were apply-LIVE at those tips, or invent Fabric agent window mutation. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Recycle session plane: Files can move a selected item to Trash, restore a selected Trash item, and empty regular files plus empty directories plus `.trashinfo` through this session's `omarchy-fabric-session-apply` `files-entry-trash` / `files-trash-restore` / `files-trash-manage` path. Elevated auth never enters Fabric durable evidence. Paths stay on session stdin JSON only. Fabric `files.provider` trash/restore/manage stays consequential / SHELL-refused / not LIVE. Files pins `trashAuthorized=false` and `emptyBinAuthorized=false` for Fabric OperationDialog paths. Catalog `files.entry.trash` / `files.trash.restore` / `files.trash.manage` stay leftover claim=partial with visible `Files > Delete`, `Files > Restore`, and `Files > Empty Recycle Bin`. Recycle Bin is not product-complete. Recycle Properties and a full desktop context menu stay leftovers. CLOSED leftover: session Trash/Restore/Empty UI. OPEN leftovers: Fabric LIVE under SHELL, Recycle Bin is not product-complete, Files LIVE metal, Win7 visual, Fabric SHELL Cut (session Cut CLOSED by #77), Settings Power LIVE. Do not invent claim=present. Do not invent Fabric SHELL LIVE Trash/Restore/Empty. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Cut/Move session plane: Files can cut a selected regular file and paste-after-cut as a move, and permanently delete a selected regular file or empty directory after confirm, through this session's `omarchy-fabric-session-apply` `files-entry-move` / `files-entry-delete` path. Elevated auth never enters Fabric durable evidence. Paths stay on session stdin JSON only. Paste-after-cut composes with the existing OS clipboard / in-app copy path and does not invent Explorer claim=present. Fabric `files.provider` move/delete stays consequential / SHELL-refused / not LIVE. Files pins `cutAuthorized=false` and `deleteAuthorized=false` as leftover Fabric SHELL refuse; session Cut/Delete do not consult these pins; there is no Fabric OperationDialog Cut/Delete path. Catalog `files.entry.move` / `files.entry.delete` stay leftover claim=partial with visible `Files > Cut` and `Files > Permanently delete`. Permanent Delete refuses Trash location misuse; Empty Bin stays on session trash-manage. CLOSED leftover: session Cut/Move + permanent Delete UI. OPEN leftovers: Fabric LIVE under SHELL, Explorer present, Files LIVE metal, Win7 visual, Settings Power LIVE, End Task LIVE, Recycle Bin is not product-complete (Properties and a full desktop context menu stay leftovers), Software Center present, Update present. Do not invent claim=present. Do not invent Fabric SHELL LIVE Cut/Delete. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Compress session plane: Files can zip selected regular file(s) and/or folder(s) through this session's `omarchy-fabric-session-apply` `files-archive-create` path. Paths stay on session stdin JSON only. The helper writes a non-colliding `.zip` beside the selection and refuses traversal, symlinks, and Trash. Catalog `files.archive.create` stays leftover `legacy-direct` with claim=partial and visible `Files > Compress`. `windows-native.13` stays prototype/pending. Files does not invent a Fabric SHELL LIVE archive writer or Explorer claim=present. CLOSED leftover: session Compress UI. OPEN leftovers: Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present. Do not invent claim=present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Extract session plane: Files can extract a selected `.zip` into a non-colliding folder named after the archive, created beside the zip, through this session's `omarchy-fabric-session-apply` `files-archive-extract` path. Paths stay on session stdin JSON only. The helper opens with O_NOFOLLOW and refuses zip-slip, symlink writes, traversal, and Trash. Catalog `files.archive.extract` stays leftover `legacy-direct` with claim=partial and visible `Files > Extract`. No windows-native Extract row exists; do not invent one as present. `windows-native.13` stays Compress-only prototype/pending. Files does not invent a Fabric SHELL LIVE archive writer or Explorer claim=present. CLOSED leftover: session Extract UI. OPEN leftovers: Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present. Do not invent claim=present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Properties session plane: Files can show an honest read-only Properties sheet for a selected file or folder through this session's `omarchy-fabric-session-apply` `files-entry-properties` path. Paths stay on session stdin JSON only. The helper opens with O_NOFOLLOW and refuses symlink follow, traversal, and Trash. Catalog `files.properties.read` stays leftover `legacy-direct` with claim=partial and visible `Files > right-click > Properties`. `parity.properties` stays missing as product. Files does not invent a Fabric SHELL LIVE Properties writer or Explorer claim=present. CLOSED leftover: session Properties sheet UI. OPEN leftovers: full Properties product, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present. Do not invent claim=present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Devices Eject leftover plane (windows-native.15): session leftover recorded: Files can eject a selected removable USB volume or optical drive from Files Devices with the mouse through this session's tip-true `FilesSessionEject.qml` `ejectDevice` plane (`omarchy-fabric-session-apply` `storage-removable-eject` → absolute udisksctl/gio). Identities stay on session stdin JSON only (`mountId`). The helper re-inventories `/proc/self/mountinfo` with the same Files mount digest, unmounts USB volumes under `/run/media/$USER/` or `/media/$USER/`, then ejects optical (`udisksctl eject`) or powers off the USB parent disk (`udisksctl power-off`) when udisks supports it. Honest busy or in-use refusals use `device.busy` and do not pretend eject succeeded. recoveryExpectation tip-true report `device.busy` via the same ejectDevice plane (compensating-without-fp; remount via tip-true `FilesSessionMount.mountVolume` → `storage-removable-mount` when still attached; no Fabric durable undo fingerprint invent; no timed auto-rollback). Elevated auth never enters Fabric durable evidence. Fabric `storage.provider` stays inspect/plan-only and never executes storage mutations. Fabric `files.provider` `mount.disconnect` stays consequential / SHELL-refused / not LIVE. Files pins `ejectAuthorized=false` as leftover Fabric SHELL refuse; session Eject does not consult that pin. Catalog `storage.removable.eject` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Files > Devices > Eject` (never claim=present; source tip-aligns to `FilesSessionEject.ejectDevice`). Soft leftover-attach ACC `windows-native.15` to that tip-true Files > Devices > Eject plane. Files does not invent a Fabric SHELL LIVE eject writer, Explorer claim=present, Devices and Printers product, or mount LIVE. `windows-native.15` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Eject it leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.15. OPEN leftovers: metal proof, mount if unfinished, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, claim=present, Settings Power LIVE, End Task LIVE, Software Center present, Update present. Do not invent claim=present. Do not invent Explorer present. Do not invent Fabric LIVE under SHELL. Do not invent Settings Power LIVE. Do not invent End Task LIVE. Do not invent Software Center present. Do not invent Update present. Do not invent a Fabric SHELL LIVE eject writer. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `storage.removable.eject` or windows-native.15 to present. Do not walk windows-native.15 to present. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Devices Mount session plane: The session Mount plane landed. Files can mount a selected unmounted removable volume from Files Devices with the mouse through this session's `omarchy-fabric-session-apply` `storage-removable-mount` path. Identities stay on session stdin JSON only (`volumeId`). The helper inventories removable volumes through absolute `/usr/bin/lsblk` and mounts USB or optical volumes with absolute `/usr/bin/udisksctl` (`gio` fallback if udisksctl is unavailable). Honest busy refusals use `device.busy` and do not pretend mount succeeded. Structured auth refusals use `mount.auth-denied` and do not echo secrets. Elevated auth never enters Fabric durable evidence. Fabric `storage.provider` stays inspect/plan-only and never executes storage mutations. Fabric `files.provider` `mount.connect` stays consequential / SHELL-refused / not LIVE. Files pins `mountAuthorized=false` as leftover Fabric SHELL refuse; session Mount does not consult that pin. Catalog `storage.removable.mount` stays leftover `legacy-direct` with claim=partial and visible `Files > Devices > Mount`. `windows-native.14` stays prototype/pending. Files does not invent a Fabric SHELL LIVE mount writer, Explorer claim=present, Devices and Printers product, or claim=present. session leftover recorded: Files Devices Mount session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.14. OPEN leftovers: metal proof, eject already session, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, claim=present. Do not invent claim=present. Do not walk `storage.removable.mount` or windows-native.14 to present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Connect to Server session plane: The session Connect plane landed. Files can connect a guest or public SMB share from Files > Connect to Server through this session's `omarchy-fabric-session-apply` `sharing-smb-connect` path. Identities stay on session stdin JSON only (`host` and `share`). The helper mounts with absolute `/usr/bin/gio mount --anonymous` and a validated `smb://host/share` URI. Password shares are an honest structured refuse (`share.auth-required`) and do not invent password vault or keyring UI. Extra credential keys are `payload.invalid` and are never echoed. Elevated auth never enters Fabric durable evidence. Fabric does not grow a sharing.provider writer. Fabric `files.provider` `mount.connect` stays consequential / SHELL-refused / not LIVE. Files pins `smbAuthorized=false` as leftover Fabric SHELL refuse; session Connect does not consult that pin. Catalog `sharing.smb.connect` stays leftover `legacy-direct` with claim=partial and visible `Files > Connect to Server`. `windows-native.16` stays missing/pending. Files does not invent a Fabric SHELL LIVE SMB writer, Explorer claim=present, Network Places product, or claim=present. session leftover recorded: Files Connect to Server session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.16. OPEN leftovers: metal proof, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present, claim=present, password-share / keyring UI. Do not invent claim=present. Do not invent Explorer present. Do not invent Network Places product. Do not invent Fabric LIVE under SHELL. Do not walk `sharing.smb.connect` or windows-native.16 to present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Open a PDF leftover plane: session leftover recorded: Files can open a selected PDF with the default/associated graphical viewer from Files with the mouse (Open / double-click) through the tip-true `files.provider` `entry.open` launch plane (`files-entry-open` helper → `/usr/bin/xdg-open`). Shipped association `application/pdf` → Evince stays tip-true in `default/applications/mimeapps.list`; Defaults MIME catalog already lists `application/pdf`. File contents are never read. Catalog `files.document.open` stays leftover `legacy-direct` with claim=partial and visible `Files > PDF`, soft leftover-attached to `FilesApplication.qml` `openEntry` (no Nautilus invent). Catalog tip-align 2026-09-06 MUST_FIX: kind=`writer` effects=`launch` (not reader invent); consent=`implicit`; idempotency=`non-idempotent`; cancellation=`before-apply`; recovery tip-true openEntry → files-entry-open → /usr/bin/xdg-open launch (no Files inventory undo / no read-only invent); source.symbol=`openEntry`. `files.entry.open` stays claim=partial / visible `Files > Open`. `windows-native.17` stays prototype/pending. Files does not invent Open With / MIME association UI product-complete, Explorer claim=present, or claim=present. session leftover recorded: Files Open a PDF leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.17. OPEN leftovers: metal proof, Open With / MIME association UI, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present, claim=present. Do not invent claim=present. Do not invent Open With / MIME association UI product-complete. Do not invent Explorer present. Do not invent Settings Power LIVE. Do not walk `files.document.open` or windows-native.17 to present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Edit a text file leftover plane: session leftover recorded: Files can open/edit a selected `.txt` with the default/associated graphical editor from Files with the mouse (Open / double-click) through the tip-true `files.provider` `entry.open` launch plane (`files-entry-open` helper → `/usr/bin/xdg-open`). Shipped association `text/plain` → org.gnome.TextEditor stays tip-true in `default/applications/mimeapps.list` (not Neovim-as-default); Defaults MIME catalog already lists `text/plain`. File contents are never read by Files. Catalog `files.text.edit` stays leftover `legacy-direct` with claim=partial and visible `Files > Text`, soft leftover-attached to `FilesApplication.qml` `openEntry` (no Nautilus invent). Catalog tip-align 2026-09-06 MUST_FIX: kind=`writer` effects=`launch` (not mutating invent); consent=`implicit`; idempotency=`non-idempotent`; cancellation=`before-apply`; recovery tip-true openEntry → files-entry-open → /usr/bin/xdg-open launch (no Files inventory undo / no fingerprint invent); source.symbol=`openEntry`. `files.entry.open` stays claim=partial / visible `Files > Open`. `windows-native.18` stays prototype/pending. Files does not invent Open With / MIME association UI product-complete, Explorer claim=present, or claim=present. session leftover recorded: Files Edit a text file leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.18. OPEN leftovers: metal proof, Open With / MIME association UI, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present, claim=present. Do not invent claim=present. Do not invent Open With / MIME association UI product-complete. Do not invent Neovim-as-default for Files `.txt`. Do not invent Explorer present. Do not invent Settings Power LIVE. Do not walk `files.text.edit` or windows-native.18 to present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Files Open Downloads leftover plane (windows-native.9): session leftover recorded: Start / Superbar Files can open the Downloads place with the mouse through tip-true `FilesModel.js` `files.downloads` → `files.location.downloads` browse (Start Downloads desktop action / AppSearch; Superbar Files > Downloads nav). File contents are never read. recoveryExpectation tip-true read-only navigation via the same Files Downloads place (mode=none; no durable mutation; no Fabric fingerprint invent). Catalog `files.downloads.open` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Start > Downloads; Superbar > Files > Downloads` (never claim=present; source tip-aligns to `FilesModel.js` `files.downloads`, not `bin/omarchy-launch-files` invent). Soft leftover-attach ACC `windows-native.9` to that tip-true Files Downloads place. Files does not invent a Fabric SHELL LIVE Downloads writer, Explorer claim=present, Nautilus invent, or claim=present. `windows-native.9` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Open Downloads leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.9. OPEN leftovers: metal proof, Files LIVE metal, Win7 visual, Explorer present, Fabric LIVE under SHELL, Settings Power LIVE, End Task LIVE, Software Center present, Update present, claim=present. Do not invent claim=present. Do not invent Explorer present. Do not invent Fabric LIVE under SHELL. Do not invent Settings Power LIVE. Do not invent End Task LIVE. Do not invent Software Center present. Do not invent Update present. Do not invent launch-files as the Downloads place. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `files.downloads.open` or windows-native.9 to present. Do not walk windows-native.9 to present. Product REJECTED.

Honesty addendum 2026-09-06 vs session elevating helpers: session apply pins `omarchy-pkg-add` / `omarchy-pkg-drop` / `omarchy-update` / `omarchy-update-available` / `omarchy-update-requires-free-space` / `omarchy-version-channel` to `/usr/bin/...` (Security MUST_FIX closed). Do not walk any claim to present. Product REJECTED.

Honesty addendum 2026-09-06 vs Settings Display night-light leftover plane (windows-native.34): session leftover recorded: Settings can enable or disable night light from Settings > Display through the tip-true `SettingsSessionNightlight.qml` `setEnabled` plane (`omarchy-shell nightlight` → `NightlightService`, the same heritage plane as Superbar > Quick Settings). Structured success/failure. Settings and QS share that session/heritage principal. recoveryExpectation tip-true re-toggle via the same setEnabled plane (omarchy-shell nightlight IPC; no Fabric durable undo fingerprint invent). Fabric `display.inspect` stays separate. Settings does not invent a `display.provider` night-light durable writer. Settings does not invent Fabric durable night-light LIVE / claim=present. Catalog `display.night-light.set` stays leftover `legacy-direct` with claim=partial, human=partial, and visible dual routes `Settings > Display; Superbar > Quick Settings > Night light` (Settings is the primary path; the QS leftover tile remains; never claim=present; source stays `NightlightService`). Soft leftover-attach ACC `windows-native.34` to that tip-true Settings > Display night-light plane. `parity.modern-display-scaling-hdr-night-light` stays prototype. `windows-native.34` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Enable night light leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.34. OPEN leftovers: metal proof, QS leftover tile residual, scaling/HDR, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Files LIVE metal, Win7 visual, Update present, Software Center present, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent End Task LIVE. Do not invent Win7 visual closed. Do not invent a Fabric night-light durable writer. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `parity.modern-display-scaling-hdr-night-light` to present. Do not walk `windows-native.34` to present. Do not walk windows-native.34 to present. Product REJECTED.

Honesty addendum 2026-09-06 vs Settings System information leftover plane (windows-native.38): session leftover recorded: Settings can read OS, product, hardware, and root storage from Settings > System information through the tip-true `SettingsSessionSystemInformation.qml` `readInformation` plane (`omarchy-fabric-session-apply` `system-information-inspect` → `apply_system_information_inspect`). Extra credential keys are `payload.invalid` and are never echoed. recoveryExpectation tip-true read-only inspect via the same readInformation plane (no durable mutation; no Fabric fingerprint invent). Settings does not invent a `system-information.provider` durable writer. Settings does not invent Fabric durable `system.info.read` LIVE / claim=present. Agent Center bubblewrap `system.info.read` stays separate. Catalog `system.info.read` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > System information` (never claim=present; source stays `SettingsSessionSystemInformation.readInformation`). Soft leftover-attach ACC `windows-native.38` to that tip-true Settings > System information plane. `windows-native.38` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Find system/storage information leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.38. Encryption, firmware flash, Device Manager, and storage mutation remain unavailable. OPEN leftovers: metal proof, encryption UI, firmware flash, Device Manager, storage mutation, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Files LIVE metal, Win7 visual, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent End Task LIVE. Do not invent Win7 visual closed. Do not invent a Fabric system-information durable writer. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `system.info.read` or windows-native.38 to present. Product REJECTED.

Honesty addendum 2026-09-06 vs Settings Input keyboard layout leftover plane (windows-native.33): session leftover recorded: Settings can change the keyboard layout from Settings > Input through the tip-true `SettingsSessionKeyboardLayout.qml` `setLayout` plane (`omarchy-fabric-session-apply` `input-keyboard-layout` / `input-keyboard-layout-status`). That verb only shells absolute `/usr/bin/hyprctl switchxkblayout` with configured layout identities. Arbitrary strings and extra credential keys are `payload.invalid` and are never echoed. recoveryExpectation tip-true re-select via the same setLayout plane (absolute hyprctl; no Fabric durable undo fingerprint invent). Settings does not invent an `input.provider` keyboard-layout durable writer. Settings does not invent Fabric durable keyboard-layout LIVE / claim=present. Catalog `input.keyboard-layout.set` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Input` (never claim=present; source stays `apply_input_keyboard_layout_session`). Soft leftover-attach ACC `windows-native.33` to that tip-true Settings > Input plane. `parity.language-locale` stays prototype. `windows-native.33` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Change keyboard layout leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.33. OPEN leftovers: metal proof, locale/region, IME, pointer/repeat, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Files LIVE metal, Win7 visual, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent End Task LIVE. Do not invent Win7 visual closed. Do not invent a Fabric keyboard-layout durable writer. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `parity.language-locale` to present. Do not walk `windows-native.33` to present. Do not walk windows-native.33 to present. Product REJECTED.

Honesty addendum 2026-09-06 vs Settings Display scaling leftover plane (windows-native.3): session leftover recorded: Settings can set focused Hyprland monitor scale from Settings > Display through the tip-true `SettingsSessionScaling.qml` `setScale` plane (`omarchy-fabric-session-apply` `display-monitor-scale` / `display-monitor-scale-status` → absolute `omarchy-hyprland-monitor-scaling` whitelist `1` / `1.25` / `1.6` / `2` / `3` / `4`). Arbitrary floats and strings are a fixed `payload.invalid` refuse and are never echoed. recoveryExpectation tip-true re-select via the same setScale plane (display-monitor-scale / omarchy-hyprland-monitor-scaling; no Fabric durable undo fingerprint invent; no timed auto-rollback). Fabric `display.inspect` stays separate. Settings does not invent a `display.provider` scale durable writer. Settings does not invent Fabric durable scale LIVE / claim=present. Catalog `display.scale.set` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Display` (never claim=present; source tip-aligns to `SettingsSessionScaling.setScale`). Soft leftover-attach ACC `windows-native.3` to that tip-true Settings > Display scale plane. `parity.modern-display-scaling-hdr-night-light` stays prototype. `windows-native.3` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Set display scaling leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.3. HDR, arrangement, and multi-monitor policy remain unavailable. OPEN leftovers: metal proof, HDR, arrangement, multi-monitor policy, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Files LIVE metal, Win7 visual, Update present, Software Center present, claim=present, modern-display present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent End Task LIVE. Do not invent Win7 visual closed. Do not invent a Fabric scale durable writer. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `parity.modern-display-scaling-hdr-night-light` to present. Do not walk `windows-native.3` to present. Do not walk windows-native.3 to present. Product REJECTED.

Honesty addendum 2026-09-05 vs Recycle desktop place: Desktop Recycle icon is the shipped `Recycle Bin.desktop` shortcut. Double-click runs `omarchy-launch-files --source desktop files.trash` through the same uwsm argv path as Computer. Files pins `emptyBinAuthorized=false` and does not offer Empty Bin LIVE under SHELL. Restore UI stays unavailable under SHELL. Recycle Properties and a full desktop context menu stay leftovers. Recycle Bin / Empty Bin LIVE residual OPEN after PR #63. Recycle Bin is not product-complete. Dated leftover: session Restore/Empty after #76 is CLOSED leftover; this paragraph's Restore UI unavailable / Empty Bin LIVE not offered is Fabric under SHELL. Do not invent Empty Bin LIVE, Restore LIVE under SHELL, `files.trash.manage` claim=present, or Recycle product-complete. Product REJECTED.

Honesty addendum 2026-09-05 vs Files LIVE metal leftover: Metal leftover on tip `e9dda27c70b8fc61fdb7b3f739c3774ca48c9809` HDMI-A-1 1920×1080@60 Samsung. Files LIVE Open/Rename/Copy grim leftover OPEN. Cloud EXIT 0 is not metal leftover. Hyprland 0.56 `hl.dsp.window.move` parks Cursor/Settings onto desktop 2 (`omarchy-shell window moveToDesktop` returns `changed:true` without moving). After park, Files is the only mapped client on workspace 1 and is visible on HDMI-A-1 grim `fail-hdmi.png` sha256 `9394a019ee4d89954be0eccde682460c7f59f33202c411f5d77dd5a2d1719948`. Compositor focus stays on Cursor (workspace 2). `hl.dsp.focus` (workspace, class, address), `hl.dsp.window.bring_to_top`, omarchy-shell window activate, and AbsPointer click on the Files surface all return ok and do not change `activewindow` off Cursor. Files address bar reached `Documents > omarchy-files-live-e9dda27c70b8`. Listing stayed empty (0 items) with `Session has expired` and a network-policy `Enter password` overlay. Open/Rename/Copy verbs unrun. Files pins `cutAuthorized=false`, `emptyBinAuthorized=false`, `trashAuthorized=false`. Do not invent Open/Rename/Copy grim leftover as closed, Cut/Trash/Empty Bin LIVE, or Win7 visual parity from this grim. Product REJECTED.

Honesty addendum 2026-09-05 vs Settings Bluetooth pair: Settings Bluetooth pairs and connects through this session's BlueZ adapter (`SettingsBluetoothPair.qml` / `pairOrConnect`; catalog `bluetooth.audio.pair` humanRoute visible `Settings > Bluetooth`). Pairing secrets never enter Fabric; `bluetooth.provider` `audio.pair` stays a read-only LeafProvider with no session helper. Radio/rfkill stays the heritage Quick Settings path. Claim stays partial; leftover `legacy-direct`. `windows-native.5` stays pending. This metal has no `/sys/class/bluetooth`, `bluetooth.service` inactive, and no lsusb Bluetooth device. Metal grim `settings-bluetooth.png` sha256 `aca4efb74bdedfdbf86e9135e545a9b3d364cf0d9e2d7256fc90a6b04e723eeb` shows Settings > Bluetooth **NO ADAPTER** (`No Bluetooth adapter is present. Pairing stays unavailable until this computer has a BlueZ adapter.`). Live pair cannot be grim-proven here; that is an honest metal FAIL, not a closed leftover. Do not invent Settings Power LIVE, Empty Bin LIVE, Cut LIVE, Restore LIVE under SHELL, Files LIVE metal CLOSED, or Win7 visual leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-05 vs Files OS clipboard: OS clipboard residual CLOSED for Files copy-out/paste-in. Copy offers the selected regular file or directory on this session's clipboard (`files-clipboard-copy` / `wl-copy --type text/uri-list`) so other apps can paste. Paste reads `text/uri-list` (and gnome-copied-files) and writes a replica through the existing copy safety (Trash, symlink, traversal, and out-of-location refuse). Paths stay on the session helper; they are not Fabric durable arguments. `files.entry.copy` remains the in-app replica writer. Cut stays `cutAuthorized=false` as leftover Fabric SHELL refuse; session Cut does not consult that pin. Empty Bin / Restore LIVE under SHELL stay forbidden. Files LIVE metal leftover and Win7 visual leftover stay OPEN. Dated OS clipboard leftover after PR #60 / PR #64 stay tip-true. Product REJECTED.

Honesty addendum 2026-09-05 vs Settings Wi-Fi join: Settings Network joins open or password-protected networks through this session's NetworkManager access (`SettingsWifiJoin.qml` / `joinNetwork`; catalog `network.wifi.connect` humanRoute visible `Settings > Network`). The passphrase never enters Fabric; `network.provider` still has no `wifi.connect` writer because credentials cannot enter durable evidence. Radio stays the typed Fabric writer (`wifi.set-enabled` / `network.manage`). Provider state for connect stays leftover `legacy-direct`. Claim stays partial. `windows-native.2` and `parity.network` stay prototype/pending. Enterprise, VPN, and per-connection DNS stay unavailable. This metal has `WIFI-HW: missing` (ethernet only), so live join cannot be grim-proven here; that is an honest metal FAIL, not a closed leftover. Do not invent Settings Power LIVE, Empty Bin LIVE, Cut LIVE, Restore LIVE under SHELL, Files LIVE metal CLOSED, or Win7 visual leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-05 vs Default Programs page: Settings ships `settings.apps.default-programs` as Default Programs page partial LIVE; Win7 applet parity still open. Browser (http+https) and mailto apply through `defaults.protocol.set`; writable multi-candidate MIME rows apply through `defaults.mime.set` with coordinator-safe idempotency keys. Catalog humanRoute visible `Settings > Default Programs`. Claim stays partial/prototype. AutoPlay, Set Program Access and Computer Defaults, and `files.associations.set` stay unavailable. Do not invent Default Programs present, files.associations.set product-complete, Empty Bin LIVE, Cut LIVE, Restore LIVE under SHELL, Files LIVE metal CLOSED, or Win7 visual leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Settings Default Programs browser leftover plane (windows-native.19): session leftover recorded: Settings can change the default browser (http+https scheme-by-scheme) from Settings > Default Programs through the tip-true `defaults.provider` `protocol.set` durable plane (`SettingsApplication.qml` `applyDefaultBrowser`; catalog `defaults.protocol.set`). Catalog `defaults.protocol.set` stays claim=partial / human=partial with visible `Settings > Default Programs` (never claim=present). Soft leftover-attach ACC `windows-native.19` to that tip-true Default Programs browser plane. `windows-native.19` stays prototype/pending. `parity.default-programs` stays prototype. Default Programs page partial LIVE; Win7 applet parity still open. AutoPlay, Set Program Access and Computer Defaults, and `files.associations.set` stay unavailable. MIME / Default Programs association UI residual OPEN after PR #62. Settings does not invent MIME association UI product-complete, AutoPlay, SPAD, `files.associations.set` product-complete, or claim=present. session leftover recorded: Change the default browser leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.19. Cloud EXIT 0 is not metal leftover CLOSED. OPEN leftovers: metal proof, AutoPlay, SPAD, Win7 Default Programs applet parity, MIME association UI residual, `files.associations.set`, Settings Power LIVE, Empty Bin LIVE, Files LIVE metal, Win7 visual, claim=present. Do not invent claim=present. Do not invent MIME association UI / AutoPlay / SPAD / `files.associations.set` product-complete. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Win7 visual closed. Do not walk `defaults.protocol.set` or windows-native.19 to present. Product REJECTED.

Honesty addendum 2026-09-06 vs Settings Update history leftover plane (windows-native.29): session leftover recorded: Settings > Update can read this session's Omarchy update transcript (`/tmp/omarchy-update.log`) through `omarchy-fabric-session-apply` `system-update-history` (`SettingsSessionUpdate.qml` `readHistory`; SettingsUpdateHistory chrome). Empty state is honest when no transcript exists. Structured failures reuse in-tree analyze-logs patterns (initramfs) plus transcript failure markers. Elevated auth never enters Fabric durable evidence. Fabric update.inspect / system.update stay inspect-only / not LIVE apply. Catalog `update.history.read` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Update` (never claim=present). Soft leftover-attach ACC `windows-native.29` to that tip-true Settings > Update history plane. `parity.update` stays prototype. windows-native.28 stays missing/pending. `windows-native.29` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. Soft leftover-attach: Settings > Update history exists (tip-visible); not product CLOSED / not metal CLOSED / not claim=present. session leftover recorded: Inspect update history leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.29. OPEN leftovers: metal proof, Update present, windows-native.28 metal, Fabric LIVE, Restart/reboot writers, Files LIVE metal, Win7 visual, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Software Center present, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Fabric LIVE. Do not invent Restart/reboot writers. Do not invent Win7 visual closed. Do not walk `update.history.read` or windows-native.29 to present. Product REJECTED.


Honesty addendum 2026-09-06 vs Settings Personalization wallpaper leftover plane (windows-native.4): session leftover recorded: Settings can change the wallpaper from Settings > Personalization through the tip-true `omarchy.image-picker` / `SettingsPersonalizationHost` wallpaper plane (`ImagePicker.qml` `applyEmbedded` → `omarchy-theme-bg-set`). recoveryExpectation tip-true set-only applyEmbedded → omarchy-theme-bg-set (mode=none; no prior-wallpaper undo / no compensating fingerprint invent). Catalog `desktop.wallpaper.set` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Personalization` (never claim=present). Soft leftover-attach ACC `windows-native.4` to that tip-true Settings > Personalization wallpaper plane. `parity.personalization` stays prototype. Typed personalization.provider writers for density, cursor, motion, and a full theme service remain unavailable. `windows-native.4` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Change the wallpaper leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.4. OPEN leftovers: metal proof, density/cursor/motion/full theme service, Win7 Personalization bottom-row product, Files LIVE metal, Win7 visual, Settings Power LIVE, Empty Bin LIVE, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Win7 visual closed. Do not invent wallpaper product-complete or Win7 visual closed from hex-grep. Do not walk `desktop.wallpaper.set` or windows-native.4 to present. Product REJECTED.


Honesty addendum 2026-09-06 vs Start Shut down leftover plane (windows-native.40): session leftover recorded: Start can shut down from Start > Shut down through the tip-true `shell/plugins/ultimate-start/Start.qml` `shutdown` plane (`Util.execDetached("omarchy-system-shutdown")` and the power flyout Shut down row → `omarchy-system-shutdown` → systemd-run user timer → `systemctl poweroff`). recoveryExpectation tip-true irreversible poweroff with no undo / no compensating fingerprint invent (visible Shut down choice is the consent gesture). Catalog `power.shutdown` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Start > Shut down` (never claim=present; source stays `shutdown`). Catalog tip-align 2026-09-06 MUST_FIX: effects=`mutating`+`irreversible` (not logout invent); cancellation=`uncancellable` (no fake before-apply cancel window vs irreversible poweroff recovery); source.symbol=`shutdown` (real `function shutdown()` in Start.qml). Soft leftover-attach ACC `windows-native.40` to that tip-true Start > Shut down plane. `parity.start` stays prototype. `windows-native.40` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Shut down leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.40. OPEN leftovers: metal proof, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Files LIVE metal, Win7 visual, Restart/reboot writers as product-complete, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent End Task LIVE. Do not invent Win7 visual closed. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `power.shutdown` or windows-native.40 to present. Product REJECTED.



Honesty addendum 2026-09-06 vs Settings Apps startup disable leftover plane (windows-native.27): session leftover recorded: Settings can list and enable or disable XDG autostart entries from Settings > Apps through the tip-true `SettingsSessionStartup.qml` `setEnabled` plane (`omarchy-fabric-session-apply` `apps-startup-list` / `apps-startup-set`). User entries write `~/.config/autostart`. System entries use a user override (`Hidden=true` / `Hidden=false`). recoveryExpectation tip-true re-toggle via the same setEnabled plane (writes ~/.config/autostart; no Fabric durable undo fingerprint invent). Elevated auth never enters Fabric durable evidence. Fabric `defaults.inspect` stays readable. Settings does not invent a Fabric `apps.startup.disable` durable writer. Settings does not invent Task Manager present. Hyprland session hooks stay outside this list. Catalog `apps.startup.disable` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Apps` (never claim=present; source stays `SettingsSessionStartup.setEnabled`). Soft leftover-attach ACC `windows-native.27` to that tip-true Settings > Apps startup plane. `parity.task-manager` stays missing as product. `windows-native.27` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. End Task stays unauthorized (`terminationAuthorized=false`). session leftover recorded: Disable a startup application leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.27. OPEN leftovers: metal proof, Task Manager present, End Task LIVE, Settings Power LIVE, Empty Bin LIVE, Files LIVE metal, Win7 visual, Software Center present, Update present, claim=present. Do not invent claim=present. Do not invent Task Manager present. Do not invent End Task LIVE. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Win7 visual closed. Do not invent Fabric apps.startup.disable durable writer. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `apps.startup.disable` or windows-native.27 to present. Product REJECTED.

Honesty addendum 2026-09-05 vs Win7 visual leftover: Metal leftover on tip `0c54390554ece5c0f70c5c5509890b48355133e7` HDMI-A-1 1920×1080@60 Samsung grim `hdmi.png` sha256 `6d0ebf9b971c5a93e1037e624f2467ff482a5889ea1a4bc2af334aca7b6a7f26`. Win7 visual leftover OPEN. hex-grep is not pixel proof (`ultimate-explorer-chrome-test.sh` is a SOURCE LOCK). Caption centering OPEN: hyprbars `bar_text_align=center` still paints centred `Files`; empty `windowTitle` fell back to `quickshell` and is not a fix. Aero glass alpha OPEN: caption mean RGB (77,80,101) vs measured `#4580c4` at 0.6; token `aeroAlpha=0.42`. Superbar stack/underline OPEN: workspace indices 1–5 and a `chromeGlow` running underline in `TaskButton.qml`; not stacked glass tiles. Badge painting OPEN: TaskButton has no `badgeCount` (source lock); Notification Center still paints a count Badge (grim tray `1D`). Selection wash OPEN: source tokens `#ffffff`/`#e6ecf5`/`#aaddfa`; grim is not a closed pixel proof. Files LIVE metal leftover stays OPEN. `emptyBinAuthorized=false`. Do not invent Files LIVE metal CLOSED, Empty Bin LIVE, or Win7 visual parity from hex-grep. Product REJECTED.

Honesty addendum 2026-09-02 vs tip after PR #31 (`62372edf9e62`): Settings Power LIVE stays refused. Heritage Superbar / Quick Settings Power remains the catalog leftover (`power.profile.set` / `legacy-direct` / Process/`omarchy-powerprofiles-set`). Distinct from fabric `app.slice`: shell launch is Hyprland `exec_cmd`, compositor documented in `session.slice`. Not a `session-N.scope` leftover. That leftover was unverified on metal after PR #31. Metal FAIL on tip `20484de6` (Not authorized / session-5103; !batteryPresent; amd_pstate EINVAL). QS Power METAL_HEAD OPEN / KEEP OPEN. Settings Power LIVE refused. Do not claim the heritage QS Power panel works on this metal. Do not honesty-gate the heritage panel; do not invent Settings LIVE or a broad polkit grant. Product REJECTED.

Honesty addendum 2026-09-02 vs tip after PR #29 (`aee1d78a8cf1`): opting power into `session_operable=True` without a session-scope or polkit authorization for `org.freedesktop.UPower.PowerProfiles.switch-profile` was a LIVE invent (Settings Power buttons that cannot succeed from `app.slice`). KEEP: `session_operable=False`; Power not in `LIVE_WRITER_ROUTES`; `profileMutationAuthorized=false`; CHANGES UNAVAILABLE honesty names polkit/session. Write plane kept. Operator owns polkit/session-scope. Product REJECTED. METAL_HEAD OPEN.

## Verified on metal

Everything below runs green on the Arch box.

| Suite | Result |
|-------|--------|
| `test/all` (264 files) | **exit 0**, 4369 assertions, zero failures |
| every suite under `test/fabric` | **OK** |

The baseline before this work was 13 of 247 shell files failing and four failures in the administration fabric suite. All of them are closed. Nine shell files and one fabric suite were fixed here; none of the failures were caused by this work, and each one is described in its own commit.

Two of those were real defects rather than stale tests:

- Forty-one `Text` elements relied on `Text.AutoText`, so anything rendering data from outside the shell could have had markup interpreted. They now declare `Text.PlainText`.
- My own comment sweep collapsed a blank line inside a heredoc, silently editing a tmux config fixture that a test compares byte for byte. Auditing every heredoc in that commit found seven files touched; six were embedded code where it changes nothing, one was data. `AGENTS.md` now forbids a sweep from changing a byte inside a heredoc.

A third is a contract nuance worth keeping: a scoped leaf provider reports a projected resource from preflight, and that projection is lossy, so provider-side `apply`, `validate` and `rollback` cannot round-trip it. The coordinator and the code-owned executor own mutation for those operations. The administration lifecycle tests had been driving that unsupported path against `process` since End Task shipped.

Local, `origin`, and metal hold the same commit and the same tree.
## The rebase

`work` was replayed with `git rebase --onto`, not merged, so `upstream/quattro` is a true ancestor. 367 commits replayed, one dropped as empty because upstream independently reworked the same theme-guard test and its version supersedes ours. Recovery ref `pre-rebase-20260901` holds the old tip.

`work` now reads 487 ahead / 367 behind `origin/work`. Publishing it needs a force-push, which the plan forbids. That call is still open.

`main` carries 218 Ultimate commits and sits 120 behind upstream, against a plan that defines it as clean upstream tracking. Pre-existing, not caused by the rebase, and restoring it is another history rewrite.

## What landed

**The root executor exists.** `SystemCommandExecutor` was wired into the daemon and invoked `/usr/libexec/omarchy-fabric-system-executor`, which was not in the repository. It is now, for `packages.install` and `packages.remove`, with a Polkit policy. Polkit binds one action id to one program path, so each verb has its own program under `/usr/libexec/omarchy-fabric/` and its own admin rule; the dispatcher maps a verb to a literal path through a fixed `case` and execs `pkexec`, so request data never reaches the argv. The root half re-validates the typed request from scratch and resolves package ids through the signed catalog.

**The write plane went from four actions to nine.** Honesty vs tip `c192afea`: `directory.create` stays `low` and SHELL-grantable. `entry.trash` is write-plane reachable at `consequential` (not helper-only); SHELL approve fails `grant.shell-consequential`. `files.trash.restore` is write-plane reachable at `consequential` after real `.trashinfo` preflight; SHELL approve fails `grant.shell-consequential`. Catalog claim=partial; humanRoute planned empty. Fabric Restore UI stays unavailable. Session Restore after #76 is CLOSED leftover.

| Action | Surface | State |
|--------|---------|-------|
| `audio-output-volume-set` | Settings › Sound | pre-existing |
| `audio-output-status` / `audio-output-mute-set` / `audio-output-default-set` | Settings › Sound | session leftover recorded: Settings Sound mute + default sink via absolute `/usr/bin/pactl` (`audio.output.manage`, leftover `legacy-direct`; claim=partial; humanRoute visible `Settings > Sound; Superbar > Quick Settings > Sound`; tip-true `audio.sink.*` identity; no Fabric audio.provider mute/default durable writer; session-UI leftover only; not product CLOSED; not metal CLOSED; not claim=present; `windows-native.6` stays prototype/pending; Cloud mocks do not close windows-native.6) |
| `audio-troubleshoot-restart` | Settings › Sound | session leftover recorded: Settings Sound audio restart via absolute tip-true `omarchy-restart-audio` FixedArgv (`troubleshooting.audio.run`, leftover `legacy-direct`; claim=partial; humanRoute visible `Settings > Sound`; no Fabric troubleshooting.provider / audio.provider troubleshoot durable writer; port changes and full troubleshoot wizard remain OPEN leftover; session-UI leftover only; not product CLOSED; not metal CLOSED; not claim=present; `windows-native.39` stays prototype/pending; Cloud mocks do not close windows-native.39; does not close `windows-native.6`) |
| `process-terminate` | Administration › End task | pre-existing |
| `power-profile-set` | Settings › Power | surfaced here |
| `files-directory-create` | Files › New folder | LIVE (`directory.create`, risk `low`; SHELL grant OK) |
| `display-brightness-set` | Settings › Display | new here |
| `input-keyboard-layout-set` | Settings › Input | Fabric daemon leftover; Settings no longer mints this durable writer |
| `input-keyboard-layout` | Settings › Input | session leftover recorded: Settings Input keyboard layout via tip-true `SettingsSessionKeyboardLayout.setLayout` → `input-keyboard-layout` (`input.keyboard-layout.set`, leftover `legacy-direct`; claim=partial; humanRoute visible `Settings > Input`; whitelist refuse; no Fabric input.provider keyboard-layout durable writer; soft leftover-attach ACC `windows-native.33` prototype/pending; never claim=present; Cloud mocks do not close windows-native.33; session-UI leftover-attach only; not product CLOSED; not metal CLOSED; not claim=present; locale complete / Settings Power LIVE / Empty Bin LIVE / End Task LIVE refused) |
| `system-information-inspect` | Settings › System information | session leftover recorded: soft leftover-attach ACC `windows-native.38` via tip-true `SettingsSessionSystemInformation.readInformation` → `apply_system_information_inspect` (READ-ONLY; `system.info.read` leftover `legacy-direct`; claim=partial / human=partial; humanRoute visible `Settings > System information`; no Fabric system-information.provider durable writer; leftover-attach only; not product CLOSED; not metal CLOSED; not claim=present; Soft Ship park before suite EXIT 0 as metal; `windows-native.38` stays prototype/pending; Cloud mocks do not close windows-native.38) |
| `network-wifi-enabled-set` | Settings › Network | new here |
| `files-entry-trash` | Files › Trash | write plane reachable (`entry.trash`, risk `consequential`; SHELL refused at `grant.shell-consequential`; Files Trash controls gated `trashAuthorized=false`, not LIVE) |
| `files-trash-restore` | none | write plane reachable (`files.trash.restore`, risk `consequential`; SHELL refused at `grant.shell-consequential`; catalog claim=partial; humanRoute planned empty; no Restore UI; Recycle residual stays OPEN) |
| `files.downloads` / `files.location.downloads` | Start › Downloads / Superbar › Files › Downloads | session leftover recorded: soft leftover-attach ACC `windows-native.9` via tip-true `FilesModel.js` `files.downloads` → `files.location.downloads` browse (`files.downloads.open`, leftover `legacy-direct`; claim=partial; humanRoute visible `Start > Downloads; Superbar > Files > Downloads`; source tip-aligns off launch-files invent; READ-ONLY; no Fabric fingerprint invent; not product CLOSED / not metal CLOSED / not claim=present; `windows-native.9` stays prototype/pending; Cloud mocks do not close windows-native.9) |
| `files-entry-open` | Files › Open / Files › PDF / Files › Text | launch plane reachable (`files.entry.open`, risk `low`; SHELL grant OK; catalog claim=partial; humanRoute visible; Files Open LIVE for regular files including PDF via default/associated viewer and `.txt` via tip-true graphical TextEditor not Neovim-as-default; `files.document.open` leftover-direct soft leftover-attached claim=partial visible `Files > PDF`; `windows-native.17` stays prototype/pending; Cloud mocks do not close windows-native.17; `files.text.edit` leftover-direct soft leftover-attached claim=partial visible `Files > Text`; `windows-native.18` stays prototype/pending; Cloud mocks do not close windows-native.18; no Open With / MIME UI) |
| `files-entry-rename` | Files › Rename | write plane reachable (`files.entry.rename`, risk `low`; SHELL grant OK; catalog claim=partial; humanRoute visible; Files Rename LIVE for same-directory names; collision refuse; no cut/move) |
| `files-entry-copy` | Files › Copy and Paste | write plane reachable (`files.entry.copy`, risk `low`; SHELL grant OK; catalog claim=partial; humanRoute visible; Files Copy/Paste LIVE for regular files and directories; session clipboard copy-out/paste-in CLOSED; collision refuse; OS clipboard residual CLOSED for Files copy-out/paste-in) |
| `files-entry-move` | Files › Cut | write plane reachable (`files.entry.move`, risk `consequential`; SHELL refused at `grant.shell-consequential`; catalog claim=partial; dated leftover humanRoute planned empty is Fabric under SHELL; current humanRoute visible `Files > Cut`; Files `cutAuthorized=false` leftover Fabric SHELL refuse; session Cut/Paste-after-cut; session Cut does not consult that pin; no Fabric LIVE Cut; folder copy CLOSED via `files.entry.copy`) |
| `files-entry-delete` | Files › Permanently delete | write plane reachable (`files.entry.delete`, risk `consequential`; SHELL refused at `grant.shell-consequential`; catalog claim=partial; humanRoute visible `Files > Permanently delete`; Files `deleteAuthorized=false`; session permanent Delete after confirm; no Fabric LIVE Delete) |
| `files-archive-create` | Files › Compress | session Compress writes a zip beside the selection (`files.archive.create`, leftover `legacy-direct`; claim=partial; humanRoute visible `Files > Compress`; no Fabric SHELL LIVE archive writer; `windows-native.13` stays prototype/pending) |
| `files-archive-extract` | Files › Extract | session Extract writes a non-colliding folder named after the archive beside the zip (`files.archive.extract`, leftover `legacy-direct`; claim=partial; humanRoute visible `Files > Extract`; no Fabric SHELL LIVE archive writer; no windows-native Extract row; `windows-native.13` stays Compress-only prototype/pending) |
| `files-entry-properties` | Files › right-click › Properties | session Properties reads name, type, size for regular files, modified time, and location path (`files.properties.read`, leftover `legacy-direct`; claim=partial; humanRoute visible `Files > right-click > Properties`; READ-ONLY; no Fabric SHELL LIVE Properties writer; `parity.properties` stays missing as product) |
| `storage-removable-eject` | Files › Devices › Eject | session leftover recorded: soft leftover-attach ACC `windows-native.15` via tip-true `FilesSessionEject.ejectDevice` → `storage-removable-eject` (`storage.removable.eject`, leftover `legacy-direct`; claim=partial; humanRoute visible `Files > Devices > Eject`; `device.busy` refuse; no Fabric SHELL LIVE eject writer; not product CLOSED / not metal CLOSED / not claim=present; `windows-native.15` stays prototype/pending; Cloud mocks do not close windows-native.15) |
| `storage-removable-mount` | Files › Devices › Mount | session leftover recorded: session Mount mounts a selected unmounted removable USB volume or optical drive (`storage.removable.mount`, leftover `legacy-direct`; claim=partial; humanRoute visible `Files > Devices > Mount`; `device.busy` refuse; no Fabric SHELL LIVE mount writer; session-UI leftover only; not product CLOSED; `windows-native.14` stays prototype/pending; Cloud mocks do not close windows-native.14) |
| `sharing-smb-connect` | Files › Connect to Server | session leftover recorded: session Connect mounts a guest or public SMB share (`sharing.smb.connect`, leftover `legacy-direct`; claim=partial; humanRoute visible `Files > Connect to Server`; `share.auth-required` refuse; no Fabric SHELL LIVE SMB writer; session-UI leftover only; not product CLOSED; `windows-native.16` stays missing/pending; Cloud mocks do not close windows-native.16) |
| `display-monitor-scale` | Settings › Display | session leftover recorded: soft leftover-attach ACC `windows-native.3` via tip-true `SettingsSessionScaling.setScale` → `display-monitor-scale` / `omarchy-hyprland-monitor-scaling` (`display.scale.set`, leftover `legacy-direct`; claim=partial; humanRoute visible `Settings > Display`; whitelist refuse; no Fabric display.provider scale durable writer; not product CLOSED / not metal CLOSED / not claim=present; `windows-native.3` stays prototype/pending; Cloud mocks do not close windows-native.3) |
| `files-trash-manage` | none | write plane reachable (`files.trash.manage`, risk `consequential`; SHELL refused at `grant.shell-consequential`; catalog claim=partial; humanRoute planned empty; Files `emptyBinAuthorized=false`; no Empty Bin LIVE; Recycle Bin / Empty Bin LIVE residual OPEN after PR #63) |
| `defaults-mime-set` | Settings › Default Programs | write plane reachable (`defaults.mime.set`, risk `low`; catalog claim=partial; humanRoute visible `Settings > Default Programs`; Settings Default Programs MIME LIVE CONTROL for writable associations with more than one installed candidate; already-default and non-candidate refuse before preflight; do not invent MIME association UI / AutoPlay / SPAD / `files.associations.set` product-complete) |
| `apps-startup-list` / `apps-startup-set` | Settings › Apps | session leftover recorded: Settings Apps startup enable/disable via tip-true `SettingsSessionStartup.setEnabled` → `apps-startup-set` (`apps.startup.disable`, leftover `legacy-direct`; claim=partial; humanRoute visible `Settings > Apps`; no Fabric apps.startup.disable durable writer; soft leftover-attach ACC `windows-native.27` prototype/pending; never claim=present; Cloud mocks do not close windows-native.27; session-UI leftover-attach only; not product CLOSED; not metal CLOSED; not claim=present; Task Manager present / End Task LIVE refused) |
| `defaults-protocol-set` | Settings › Default Programs | write plane reachable (`defaults.protocol.set`, risk `low`; catalog claim=partial; humanRoute visible `Settings > Default Programs`; Settings Default Programs browser LIVE CONTROL for http+https via `applyDefaultBrowser`; soft leftover-attach ACC `windows-native.19` prototype/pending; never claim=present; Cloud mocks do not close windows-native.19; AutoPlay/SPAD/`files.associations.set` stay unavailable) |

Each device-scoped writer resolves its opaque resource id by recomputing the provider's own digest over the live device list, so a payload can never name a monitor, keyboard or sink directly.

**`session_apply.py` moved out of `operations/`.** `fabric-operation-coordinator-test.sh` refuses any subprocess or privilege escape under `operations/`, and the helper had been sitting there calling `subprocess.run` four times since the write plane shipped. It now lives in `omarchy_fabric.helpers`.

## Three defects this surfaced

1. **`require_resource_id` is audio-specific.** It demands an `audio.sink.` prefix and a 64-hex digest. The display, input and network branches all called it, so each would have refused its own resource id at runtime. The QML and model tests could not see this because they never exercised the helper. Any new branch needs its own prefix check.

2. **A missing command escaped as `FileNotFoundError`.** `main()` caught `ApplyError` and `TimeoutExpired` but not `OSError`, so a machine without `pactl` would have crashed the helper instead of refusing. Predates this work.

3. **The power provider was never opted into session-operable mutation.** It had an intent, an operation definition and a helper branch, and `ReadOnlyProbeBackend` defaulted `session_operable` to `False`, so the Settings control would have been a dead button. Check this first for any new domain.

## Files writers vs tip (not helper-only)

`files-entry-trash` and `files-trash-restore` remain implemented and proven against a real temporary tree, including both security guards failing the test when removed. The schema-family blocker is closed. PR #8 widened `files-directory-{preflight,result,state}-v1` in place: `action` is an enum of `directory.create` and `entry.trash` (and still lists `trash.restore`) rather than a parallel Trash family. v0 workspace schemas were not edited. Shared family is not shared risk.

**LIVE**

- `directory.create` — Files › New folder. Risk `low`. SHELL may hold a standing grant.
- `entry.rename` — Files › Rename. Risk `low`. SHELL may hold a standing grant. Same-directory only. Collision refuses. The scoped listing carries the selected entry identity so a names-only TOCTOU cannot hide inode drift.
- `entry.copy` — Files › Copy and Paste. Risk `low`. SHELL may hold a standing grant. Regular files and directories. Destination is a writable Files location. Collision, symlink loops, and nest-inside-source refuse. Copy maps `EXDEV` errno from `mkdir`/`open` only; overlay or bind-mount children may copy. Staging is in-app only. The scoped destination listing carries the source entry identity so a names-only TOCTOU cannot hide inode drift.

**Write plane reachable, not LIVE under SHELL**

- `entry.move` — Files cut/move through `files.provider`. Write plane reachable. Scope is the destination directory plus source identity, reusing `files.directory.<digest>`. Risk `consequential`. SHELL cannot hold a standing grant (`grant.shell-consequential`). Fabric Cut stays unavailable under SHELL. Files pins `cutAuthorized=false` as leftover Fabric SHELL refuse. Session Cut/Paste-after-cut is LIVE via `omarchy-fabric-session-apply` and does not consult that pin. Do not invent Fabric LIVE Cut under SHELL or an OS clipboard.
- `entry.trash` — Files › Trash through `files.provider`. Write plane reachable. Scope is the entry's parent directory, reusing `files.directory.<digest>`. Risk `consequential`. SHELL cannot hold a standing grant (`grant.shell-consequential`). Same grant rule as End Task. Files Trash controls stay hidden while `trashAuthorized=false` so the shell principal cannot start a doomed preflight. Do not invent a TASK workaround or LIVE Trash under SHELL.

**Unavailable (do not invent)**

- Fabric Restore LIVE under SHELL. The `files.trash.restore` write plane is reachable. Real preflight reads `.trashinfo` and scopes the original destination directory. Risk `consequential`. SHELL cannot hold a standing grant. Catalog claim=partial; humanRoute planned empty. Session Restore after #76 is CLOSED leftover. Files does not offer a Fabric Restore control.
- Fabric Empty Bin LIVE under SHELL. The `files.trash.manage` write plane is reachable. Real preflight binds the Trash location listing. Empties regular files and empty directories plus their `.trashinfo`. Non-empty trash trees refuse. Risk `consequential`. SHELL cannot hold a standing grant. Catalog claim=partial; humanRoute planned empty. Files pins `emptyBinAuthorized=false` and does not offer Empty Bin LIVE under SHELL. Session Empty after #76 is CLOSED leftover.
- Fabric Permanent delete LIVE under SHELL (write plane reachable; `deleteAuthorized=false` leftover Fabric SHELL refuse; no SHELL LIVE Delete). Session Permanent Delete after 2026-09-06 is CLOSED leftover.
- Recycle Bin as a product-complete place

The Files banner matches: New folder runs through `files.provider`; Copy and Paste run through `entry.copy` and also place or read files on this session's clipboard; the cut/move write plane exists but is not shell-authorizable under Fabric; session Cut is CLOSED leftover; Trash write plane exists but is not shell-authorizable (CHANGES UNAVAILABLE); Restore write plane exists but is not shell-authorizable; the permanent delete write plane exists but is not shell-authorizable under Fabric; session Permanent Delete is CLOSED leftover; the empty Recycle Bin write plane exists but is not shell-authorizable; Fabric Restore UI and Empty Bin LIVE remain unavailable under SHELL; Recycle Bin is not product-complete.

The scope function and payload deriver shipped with the v1 widen (not left reverted). `_entry_trash_scope` resolves the entry, derives its parent, and proposes the listing minus that name. The payload deriver reads scoped `currentState.value` and refuses any plan that does not remove exactly one name.

## Why entry.rename is scoped with identity in the listing

A names-only directory listing is the wrong scope for rename. The listing revision tracks names. Renaming changes a name, so the effect is visible, but an identity drift that leaves the names in place does not move the revision. `test_compare_and_swap_contains_concurrent_execution_and_toctou_drift` requires `files.state-stale` when the target entry's `identity` changes between preflight and execute.

The scoped rename plane keeps the v1 directory family and puts `selectedEntry.{entryId,identity}` on the listing document. Resource id is `files.directory.<digest(location,parent,entryId)>` so the session reader can rehydrate that identity from live inventory. Adversarial tests stay workspace-shaped where they already inspect the whole workspace. Lifecycle rename asserts the scoped listing.

Create and trash stay names-only. Their observable effect is a name appearing or disappearing, and the helper still inode-checks. Restore write plane is reachable from `.trashinfo` at real preflight; Restore UI stays honest-unavailable. Do not invent Restore LIVE.
## What still blocks Software Center

Not the executor. The shipped catalog carries `assurance: contract-seed`, and `PackageProvider` deliberately refuses live mutation for an unattested seed. That gate is correct and was left closed. Opening it is a release-attestation decision.

`RELEASE_PLAN_ONLY_DETAIL` in both the packages and compatibility providers still says live mutation "waits for the privileged system executor". For packages that sentence is now stale, but the accurate replacement depends on whether the provider's `_require_live_execution` sits on the operation path at all, which needs a running daemon to determine. Left alone rather than guessed at.

## Remaining leaf work

`bluetooth` is the last leaf domain without a writer. Its operation is `audio.pair`, a multi-step interactive flow rather than a single verb, so it does not follow the five-step recipe.

`defaults` is a `StateDomainProvider` like `files` and needs the same scoped-resource treatment.

## Debt paid

`input.keyboard-layout.set` stays leftover `legacy-direct` after the Settings Input session plane. Catalog claim stays partial. Catalog recovery tip-aligns to re-select via tip-true `SettingsSessionKeyboardLayout.setLayout` → `input-keyboard-layout` (stateFingerprintRequired=false; no Fabric durable undo fingerprint invent). Soft leftover-attach ACC `windows-native.33` to that tip-true Settings > Input plane. The Superbar widget still assembles `hyprctl switchxkblayout` as a shell string and cycles from the bar; Settings applies a configured identity through `omarchy-fabric-session-apply` `input-keyboard-layout` instead of minting Fabric durable LIVE.

Neither the `parity.language-locale` nor the `windows-native.33` row moves off `prototype`. Switching between configured layouts is not the whole job, and forty-task row 33 stays unproven until it runs on metal. Cloud mocks do not close windows-native.33.

`display.scale.set` stays leftover `legacy-direct` after the Settings Display / tip-true `SettingsSessionScaling.setScale` → `display-monitor-scale` / `omarchy-hyprland-monitor-scaling` plane. Catalog claim stays partial. Catalog source tip-aligns to `SettingsSessionScaling.setScale`. Catalog recovery tip-aligns to re-select via that tip-true plane (stateFingerprintRequired=false; no Fabric durable undo fingerprint invent; no timed auto-rollback). Soft leftover-attach ACC `windows-native.3` to that tip-true Settings > Display scale plane. Settings does not invent a `display.provider` scale durable writer.

Neither the `parity.modern-display-scaling-hdr-night-light` nor the `windows-native.3` row moves off `prototype`. Scaling alone is not modern display complete, and forty-task row 3 stays unproven until it runs on metal. Cloud mocks do not close windows-native.3.

`files.downloads.open` stays leftover `legacy-direct` after the Files Downloads / tip-true `FilesModel.js` `files.downloads` → `files.location.downloads` browse plane. Catalog claim stays partial. Catalog source tip-aligns to `FilesModel.js` `files.downloads` (not `bin/omarchy-launch-files` invent). Catalog recovery tip-aligns to read-only navigation via that tip-true place (mode=none; stateFingerprintRequired=false; no Fabric fingerprint invent). Soft leftover-attach ACC `windows-native.9` to that tip-true Files Downloads place. Files does not invent a Fabric SHELL LIVE Downloads writer.

Neither the Explorer present row nor the `windows-native.9` row moves off `prototype`. Open Downloads alone is not Explorer complete, and forty-task row 9 stays unproven until it runs on metal. Cloud mocks do not close windows-native.9.

`storage.removable.eject` stays leftover `legacy-direct` after the Files Devices / tip-true `FilesSessionEject.ejectDevice` → `storage-removable-eject` plane. Catalog claim stays partial. Catalog source tip-aligns to `FilesSessionEject.ejectDevice`. Catalog recovery tip-aligns to report `device.busy` honestly via that tip-true plane (mode=compensating; stateFingerprintRequired=false; remount via tip-true `FilesSessionMount.mountVolume` → `storage-removable-mount` when still attached; no Fabric durable undo fingerprint invent; no timed auto-rollback). Soft leftover-attach ACC `windows-native.15` to that tip-true Files > Devices > Eject plane. Files does not invent a Fabric SHELL LIVE eject writer.

Neither the Explorer present row nor the `windows-native.15` row moves off `prototype`. Eject alone is not Explorer complete, and forty-task row 15 stays unproven until it runs on metal. Cloud mocks do not close windows-native.15.

`display.night-light.set` stays leftover `legacy-direct` after the Settings Display / NightlightService plane. Catalog claim stays partial. Catalog recovery tip-aligns to re-toggle via tip-true `SettingsSessionNightlight.setEnabled` → `NightlightService` (stateFingerprintRequired=false; no Fabric durable undo fingerprint invent). Soft leftover-attach ACC `windows-native.34` to that tip-true Settings > Display night-light plane. Settings and QS share that session/heritage principal; Settings does not invent a `display.provider` night-light durable writer.

Neither the `parity.modern-display-scaling-hdr-night-light` nor the `windows-native.34` row moves off `prototype`. Night light alone is not modern display complete, and forty-task row 34 stays unproven until it runs on metal. Cloud mocks do not close windows-native.34.

`system.info.read` stays leftover `legacy-direct` after the Settings System information / tip-true `SettingsSessionSystemInformation.readInformation` → `apply_system_information_inspect` plane. Catalog claim stays partial. Catalog recovery tip-aligns to read-only inspect via that tip-true plane (mode=none; stateFingerprintRequired=false; no Fabric fingerprint invent). Soft leftover-attach ACC `windows-native.38` to that tip-true Settings > System information plane. Settings does not invent a `system-information.provider` durable writer; Agent Center bubblewrap `system.info.read` stays separate.

Neither the Disk Management product row nor the `windows-native.38` row moves off missing/prototype. Find system/storage information stays unproven until it runs on metal. Cloud mocks do not close windows-native.38.

Honesty addendum 2026-09-06 vs Settings Printers session plane: The session Printers plane landed. Settings can inventory printers and set default (plus pause/resume/test-page) from Settings > Printers with the mouse through this session's `omarchy-fabric-session-apply` `printer-status` / `printer-default-set` / `printer-pause` / `printer-resume` / `printer-test-page` path. Identities stay on session stdin JSON only (`resourceId` tip-true `printer.*`). Absolute FixedArgv helpers are `/usr/bin/lpstat`, `/usr/bin/lpoptions`, `/usr/sbin/cupsdisable`, `/usr/sbin/cupsenable`, and `/usr/bin/lp` with `/usr/share/cups/data/testprint`. Credentialed printer URIs are refused. Fabric `printer.provider` stays inspect / plan-only `queue.plan` and never executes CUPS mutation. Settings does not invent a `printers.provider` durable writer or Fabric SHELL LIVE queue mutation. Catalog `printers.manage` stays leftover `legacy-direct` with claim=partial / human=partial and visible `Settings > Printers`. `windows-native.32` stays prototype/pending with tip-true recoveryExpectation on the session inventory/set-default/pause/resume/test-page plane (no Add-setup cancel/remove invent). Network Add / driver wizard remains OPEN leftover and is not product-complete. Administration > Printers and scanners remains the bounded `printer.inspect` host. session leftover recorded: Settings Printers inventory and queue session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.32. OPEN leftovers: metal proof, Network Add / driver wizard, Devices and Printers product, Win7 visual, Settings Power LIVE, End Task LIVE, Software Center present, Update present, claim=present. Do not invent claim=present. Do not invent Add product-complete / Win7 printer wizard present. Do not invent Fabric SHELL LIVE. Do not walk `printers.manage` or windows-native.32 to present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

Honesty addendum 2026-09-06 vs Administration process CPU leftover plane (windows-native.26): The tip-true Fabric `process.provider` already inventories processes with CPU share through FixedArgv `/usr/bin/ps` (`-ww -eo pid=,uid=,pcpu=,pmem=,rss=,comm=,cgroup=`) plus kernel boot ID and double-read `/proc/<pid>/stat` / `/proc/<pid>/status` identity bind. Administration > Processes is the visible host: record cards surface `cpuPercent`, sort highest-CPU first, and keep End Task unauthorized (`terminationAuthorized=false`). Catalog `process.inspect` soft leftover-attaches to claim=partial / human=partial with visible `Administration > Processes` (never claim=present). `windows-native.26` stays prototype/pending. `process.termination.plan` / End Task remains OPEN leftover (claim=missing; consent high-risk / consequential; no Fabric SHELL LIVE kill invent; do not invent terminationAuthorized=true). `parity.task-manager` stays missing as product. session leftover recorded: Administration Processes CPU inspect session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.26. OPEN leftovers: metal proof, Task Manager product, End Task LIVE, Superbar Task Manager, Settings Power LIVE, Empty Bin LIVE, Win7 visual, Software Center present, Update present, claim=present. Do not invent claim=present. Do not invent Task Manager present. Do not invent End Task LIVE. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Win7 visual closed. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.
