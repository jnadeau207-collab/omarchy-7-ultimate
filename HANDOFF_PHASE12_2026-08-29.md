# HANDOFF_PHASE12_2026-08-29

Do not edit this file. If the lock moves, write a new dated `HANDOFF_*`.
Do not reuse `HANDOFF_W0_GO_2026-08-23.md` / SHA `ab94cbd9`.

The product / OS is still REJECTED. This file is a Phase 1/2 honesty close, not a shippable Windows 7 Ultimate.

## Verdict

`Accepted W0 tree:` is **not written**. Windowing Gate W0 glass proofs (`windows-native-test.sh` twice, hyprbars / CSD caption / Start dismiss pointer proofs) were not run against this work on a clean checkout of a pushed SHA.

Jesse's eyes stay open.

Phase 2 leftover (catalog entry-point lock, durable tasks, live selection, inspect runner) is implemented in the dirty `work` tree on top of `1ae24608f43cb2487a92096156d2504e2e5eb939`. That SHA is not this work. This work is not committed and not pushed.

## What this tree is

| Item | Fact |
|------|------|
| Branch | `work` |
| Parent | `1ae24608f43cb2487a92096156d2504e2e5eb939` |
| Git status | dirty; new `default/ultimate/start-chrome.json` |
| Metal checkout `/home/jesse/omarchy7ultimate` | `a6378afd` (`work...origin/work`), not this tree |
| Live Hyprland | HDMI-A-1 Samsung 1920×1080@60.00, reserved bottom 48, `/dev/uinput` `root:input 660` |
| Live shell IPC | two `omarchy-shell window ping` launches returned `ok` (the ping payload). That is the **running** `a6378afd` shell, not this maximize-state tree |

Metal proofs below used a side tree at `/tmp/omarchy7ultimate-phase12` copied from this dirty checkout. That is not a clean clone and not a pushed SHA.

## Phase 1 code (hermetic)

Work-area maximize is a persisted placement kind in `window-placements.json`. `isMaximized` uses that kind plus compositor `fullscreen === 1`. The geometry heuristic is gone. Restore returns the pre-max float. Start card size and Superbar thickness come from `default/ultimate/start-chrome.json` for both QML and Lua.

`test/shell.d/window-service-test.sh` passed on WSL.

W0 glass on this tree: **unverified**. Deploying this shell onto the live session would require restarting Quickshell against an unpushed dirty tree. That was not done.

## Phase 2 metal (side tree, twice where listed)

| Proof | Result |
|-------|--------|
| `test/fabric/managed_work` | twice, 74 tests, 1 skipped (live selection when WAYLAND unset), exit 0 |
| `test/fabric/security/test_sandbox.py` | twice, 12 tests, exit 0 |
| `test/acceptance.d/agent-sandbox-test.sh` | twice, inspect artifact matched granted workspace, isolation held |
| `omarchy-hyprland-monitor-apply --emit-lua` | `HDMI-A-1` `1920x1080@60.00`, DP-1/2/3 disabled, no `preferred` |
| `test/shell.d/monitor-apply-test.sh` | exit 0 on metal after packing `config/hypr/monitors.lua` |
| Live `wl-copy` + context `selection` | known string round-tripped; lock/pinentry classes absent from `open-windows` |
| fabricctl task create/list + context capture + run execute | create/list returned the task object; live selection `captured` 30 bytes; execute `system.info.read` `succeeded` |

Missing `/usr/bin/bwrap` was not skipped. Metal has it. WSL has it. Representative execute succeeded on both.

## WSL `./test/shell`

Ran on this dirty tree with `PATH=/tmp/pybin:/usr/bin:/bin`. Runner printed `15 of 243 test files failed` and then exited 0.

No windowing or fabric regression in that list. Classification:

| File | Class | Why |
|------|-------|-----|
| `agent-usage-codex-scanner-test.sh` | known pre-existing | Codex pi/omp session count; not touched |
| `browser-policy-dir-test.sh` | env gap | `rg` missing on this WSL PATH |
| `dev-link-test.sh` | env gap | `visudo` missing |
| `fabric-administration-recovery-test.sh` | env gap | `rg` missing |
| `fabric-operation-coordinator-test.sh` | env gap | `rg` missing |
| `hw-hybrid-gpu-test.sh` | env gap | no hybrid GPU in WSL |
| `hybrid-gpu-test.sh` | env gap | no hybrid GPU in WSL |
| `keybindings-menu-test.sh` | env gap | `xkbcli` missing |
| `plugin-clone-test.sh` | env gap | `rg` missing; clock clone assertion never ran |
| `powerprofiles-set-test.sh` | env gap | no Omarchy powerprofiles backend here |
| `provision-user-test.sh` | env gap | `update-desktop-database` missing |
| `start-clickthrough-ipc-test.sh` | env gap | `hyprctl` missing; live shell is not this WSL |
| `system-lock-test.sh` | env gap | `hyprctl` missing |
| `windows-key-test.sh` | env gap | no ACPI MSDM table |
| `windows-vm-test.sh` | env gap / stale vs intended | Windows VM defaults are not this host |

Passed and relevant to this close: `window-service-test.sh`, `capability-broker-test.sh`, `capability-catalog-test.sh` (including `window-agent-without-entry` and `window-agent-claim-present`), `agent-center-product-test.sh`, `fabric-core-test.sh` (fabricctl task/context/execute payloads), `fabric-managed-work-test.sh`, `ultimate-desktop-plugin-test.sh`.

## What still has to happen before W0 can be accepted

1. Commit this dirty tree (Jesse has to ask).
2. Push `work` only. Do not merge to `main`. Do not force-push.
3. On metal: clean checkout of that SHA, empty `git status`, chmod `0755` on `bin/omarchy-hyprland-monitor-apply` if git lands it `0644`.
4. Reload Hyprland only after emit-lua prints a ranked 1080p60 desktop mode. Do not `omarchy-refresh-hyprland`. Do not pin HDMI. Do not chmod `/dev/uinput` in the session.
5. `windows-native-test.sh` twice, then the three pointer proofs, then two `omarchy-shell window` writer payloads (not ping-as-process-started).
6. Write `Accepted W0 tree: <full SHA>` only if every W0_GATE box except eyes actually passed on that SHA.
