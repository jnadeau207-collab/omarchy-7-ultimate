#!/bin/bash

set -euo pipefail

ROOT=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../../.." && pwd)

PYTHONPATH="$ROOT/default/fabric" python -m unittest discover -s "$ROOT/test/fabric/administration" -p 'test_*.py' -v
