# HANDOFF_PHASE12_W0_CLOSE_2026-08-29

Do not edit this file. If the lock moves, write a new dated `HANDOFF_*`.
Do not edit `HANDOFF_W0_2026-08-29.md`. That file still records the windowing battery on `a7eb6dd1` and still says eyes were open. This file supersedes the open-eyes line: Jesse waived W0 item 8 for this close. `HANDOFF_PHASE12_2026-08-29.md` is the earlier honesty stop on a dirty tree. Leave it.

The product / OS is still REJECTED. W0 is a windowing architecture gate. Phase 2 leftover is catalog, durable tasks, context, and sandboxed execute — not Agent Center UI (Phase 4) and not Display/Audio/Network services (Phase 5).

Accepted W0 tree: a7eb6dd1bc789756bc8de3589bde36c2c3409bcd
Phase 2 leftover tree: d290d619805cd64e11caa01d9220aab11c6cc4cf

## Goal

Close founding Phase 1 (Windowing Gate W0) and Phase 2 leftover Agent Fabric so either `phase 1 and 2 including W0 are truly and properly closed` or `this is a fake service that does not deliver usable code` is true. Jesse waived human eyes for this close. If a behavior cannot be guaranteed in code, it cannot ship.

## State of the world

| Item | Fact |
|------|------|
| Branch | `work` |
| Windowing SHA | `a7eb6dd1bc789756bc8de3589bde36c2c3409bcd` |
| Phase 2 SHA | `d290d619805cd64e11caa01d9220aab11c6cc4cf` |
| Diff `a7eb6dd1..d290d619` | W0 handoff plus managed-work automation admit, schema 5, tests. No windowing files. |
| Metal `/home/jesse/omarchy7ultimate` | `d290d619`, porcelain empty during both fabric passes |
| Authority | `PRODUCT_DOCTRINE.md`, `plans/project-ultimate.md` Phases 1–2, `W0_GATE.md` |
| Not authority | Luna 7–16 / 2026-08-26 overlay; `AGENT_NATIVE_ACCEPTANCE.md` historical table |

## Done & verified

### W0 machine boxes on `a7eb6dd1`

Recorded in `HANDOFF_W0_2026-08-29.md`. Windowing files are unchanged after that SHA.

| Gate item | Result |
|-----------|--------|
| 1. Pushed SHA | `a7eb6dd1` on `origin/work`; still an ancestor of `d290d619` |
| 2. Clean checkout | metal at that SHA, porcelain empty, for the windowing battery |
| 3. `./test/shell` | 2 of 243 failed without `HYPRLAND_INSTANCE_SIGNATURE`; both classified env gap; both passed with the live socket |
| 4. `monitor-apply-test.sh` | exit 0 |
| 5. `windows-native-test.sh` | twice, both `NATIVE_EXIT=0` |
| 6. Pointer proofs | hyprbars, CSD caption, Start dismiss `ok: true`; `/dev/uinput` group-writable via shipped udev |
| 7. Auto ranking | `--emit-lua` `HDMI-A-1` `1920x1080@60.00`, DP-* disabled, no `preferred` |
| 8. Human eyes | waived by Jesse for this close |
| 9. Adversarial review | done on the windowing files of that close; not deferred |

### Phase 2 leftover on `d290d619`

Local WSL:

- `test/fabric/managed_work`: 75 tests, 1 skipped (live clipboard), exit 0
- `test/fabric/security`: 39 tests, exit 0, including 12 sandbox tests
- `test/shell.d/fabric-managed-work-test.sh`: exit 0
- `test/shell.d/fabric-core-test.sh`: exit 0, including `omarchy-fabricctl` task / context / execute
- `test/shell.d/capability-catalog-test.sh`: exit 0, including `window-agent-without-entry` and `window-agent-claim-present`
- `test/shell.d/agent-center-product-test.sh`: exit 0

Metal, twice, clean checkout of `d290d619`:

| Proof | Pass 1 | Pass 2 |
|-------|--------|--------|
| managed-work 75 | `MANAGED_EXIT:0` | `MANAGED_EXIT:0` |
| sandbox 12 | `SANDBOX_EXIT:0` | `SANDBOX_EXIT:0` |
| `agent-sandbox-test.sh` | `AGENT_SANDBOX_EXIT:0` | `AGENT_SANDBOX_EXIT:0` |
| `fabric-core-test.sh` | `FABRIC_CORE_EXIT:0` | `FABRIC_CORE_EXIT:0` |

`managed-execution.not-integrated` is gone from the tree. Due automations now write `execution_status()["code"]`. When that code is `managed-execution.bubblewrap`, the firing state is `accepted` and `create_task` admits a draft task with `detail.taskId`. When execution is unavailable, the firing stays `pending-unavailable` with `sandbox.unavailable`. Templates without a sandbox allowlist capability fail `automation.capability`. Schema 5 adds `accepted` to the firing CHECK.

## Done but UNVERIFIED

Nothing required for this close remains unverified except behaviors that are out of scope: Agent Center UI (Phase 4), Display/Audio/Network as services (Phase 5), and Jesse looking at the glass (waived).

## In flight

None. Do not reopen W0 windowing. Do not merge `work` into `main`. Do not force-push. Do not pin HDMI. Do not write `~/.config/omarchy/shell.json`. Do not register `OperationCoordinator`. Do not widen `CapabilityBroker.permit("agent")`.

## Open defects / known traps

- Lua `load_start_chrome()` still parses four integer keys with `gmatch`. The current file is only those integers. A later string field would be ignored unless a required key is missing.
- `_desiredRect` drops if the compositor box drifts more than 16px from the last applied box.
- SSH often lacks `HYPRLAND_INSTANCE_SIGNATURE`. Find the live `/run/user/1000/hypr/*/.socket.sock`.
- `wl-copy` from SSH blocks if you `subprocess.run` it.
- Quattro rebase recovery refs (`4a4cd72d` / `7b3fd00d` / `08c5ae1d`) stay until Jesse asks.
- Windows `ssh` has the metal key. WSL `ssh`/`rsync` got `Permission denied (publickey)`.
- After Windows-authored checkout, `chmod 0755` `bin/omarchy-hyprland-monitor-apply` if git lands `0644`.

## Exact commands

```powershell
ssh -o UserKnownHostsFile=C:\dev\omarchy-vm\omarchy_known_hosts jesse@192.168.1.171 "cd /home/jesse/omarchy7ultimate && git rev-parse HEAD && git status --porcelain"
```

```bash
PYTHONPATH=default/fabric python3 -m unittest discover -s test/fabric/managed_work -p 'test_*.py'
PYTHONPATH=default/fabric python3 -m unittest discover -s test/fabric/security -p 'test_sandbox.py'
test/acceptance.d/agent-sandbox-test.sh
test/shell.d/fabric-core-test.sh
test/shell.d/capability-catalog-test.sh
```

Resume: this close is finished. Next product work is Phase 3 (design system) or Phase 4 (desktop shell / Agent Center UI), not another W0 or Phase 2 leftover pass.
