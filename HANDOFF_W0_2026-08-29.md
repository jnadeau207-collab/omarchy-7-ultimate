# HANDOFF_W0_2026-08-29

Do not edit this file. If the lock moves, write a new dated `HANDOFF_*`.
Do not reuse `HANDOFF_W0_GO_2026-08-23.md` / SHA `ab94cbd9`.
`HANDOFF_PHASE12_2026-08-29.md` is the earlier honesty stop on a dirty unpushed tree. This file supersedes it.

The product / OS is still REJECTED. W0 is a windowing architecture gate, not a shippable Windows 7 Ultimate.

Accepted W0 tree: a7eb6dd1bc789756bc8de3589bde36c2c3409bcd

Jesse's eyes stay open.

## Verdict

Machine boxes on `W0_GATE.md` except eyes passed on that SHA. Adversarial review of this windowing close was done this session (item 9). It is not deferred.

Phase 2 leftover (catalog entry-point lock, durable tasks, live selection, inspect runner) is on the same SHA and was proved by catalog mutations plus metal fabric batteries, not by retconning the old AGENT_NATIVE matrix.

## What this tree is

| Item | Fact |
|------|------|
| Branch | `work` |
| SHA | `a7eb6dd1bc789756bc8de3589bde36c2c3409bcd` |
| Parent of the close | `1ae24608f43cb2487a92096156d2504e2e5eb939` plus `f8a8a67a` then this compose fix |
| Metal checkout `/home/jesse/omarchy7ultimate` | that SHA, empty `git status` |
| Live Hyprland | HDMI-A-1 1920×1080@60.00, reserved bottom 48, `/dev/uinput` `root:input 660` via shipped udev |
| Jesse in `input` | yes |

## Gate checklist

| Gate item | Result |
|-----------|--------|
| 1. Pushed SHA | `a7eb6dd1bc789756bc8de3589bde36c2c3409bcd` on `origin/work` |
| 2. Clean checkout, empty status | metal `/home/jesse/omarchy7ultimate` at that SHA, porcelain empty |
| 3. `./test/shell` | 2 of 243 files failed in the first SSH run; both classified below; neither is a windowing or fabric regression |
| 4. `monitor-apply-test.sh` | exit 0 |
| 5. `windows-native-test.sh` | twice, both `NATIVE_EXIT=0`, including rows 20–25 and unnumbered proofs |
| 6. Three pointer proofs | both native passes: hyprbars, CSD caption, Start dismiss all `ok: true`. `/dev/uinput` was group-writable; no session chmod |
| 7. Auto ranking | `--emit-lua` `HDMI-A-1` `1920x1080@60.00`, DP-1/2/3 disabled, no `preferred` |
| 8. Human eyes | **open — Jesse** |
| 9. Adversarial review | done this session on the windowing files this close added or edited |

Live writer payloads on this SHA: `omarchy-shell window maximize` and `unmaximize` each returned `{"changed":true,"error":null}`.

Hyprland was reloaded only after emit-lua printed the ranked 1080p60 line. Quickshell was restarted onto this SHA. `omarchy-refresh-hyprland` was not run. HDMI was not pinned.

## `./test/shell` classification

First aggregate run from SSH without `HYPRLAND_INSTANCE_SIGNATURE` failed two files. Both were re-run with the live Hyprland socket and passed.

| File | Class | Why |
|------|-------|-----|
| `screenshot-sanity-test.sh` | env gap | first run: fixture Quickshell plus `hyprctl` with no instance signature; `jq` got a non-JSON grim/hyprctl blob. Re-run with `HYPRLAND_INSTANCE_SIGNATURE` set: exit 0 |
| `start-clickthrough-ipc-test.sh` | env gap | first run: `hyprctl -j layers` empty, JSON decode failed. Re-run with the live socket: exit 0, Start card matches `start-chrome.json` |

No windowing or fabric regression in that list.

## Phase 1 on this SHA

Work-area maximize is a persisted placement kind. `isMaximized` uses that kind plus compositor `fullscreen === 1`. Sequential `moveTo` then `resizeTo` compose from the requested box (`_desiredRect`) so clamp cannot keep a remembered 1200-wide foot at x=720 after a 640 resize. That was the second-pass Start click-through miss on `f8a8a67a`; `a7eb6dd1` fixed it. Both native passes placed the click-through foot at `[900, 80]` `[640, 400]`.

Start card size and Superbar thickness come from `default/ultimate/start-chrome.json` for QML and Lua.

SSD maximize in the hyprbars proof stayed `fullscreen: 0` at `1920×1000` (work area minus hyprbars). Chrome CSD caption maximize still reports compositor `fullscreen: 1` at `1920×1032`; that is the CSD client, not exclusive Hyprland fullscreen as the maximize verb.

## Phase 2 leftover on this SHA

| Proof | Result |
|-------|--------|
| `test/fabric/managed_work` | twice, 74 tests, exit 0 |
| `test/fabric/security/test_sandbox.py` | twice, 12 tests, exit 0 |
| `test/acceptance.d/agent-sandbox-test.sh` | twice, inspect path, missing bwrap is a failure |
| `test/shell.d/fabric-core-test.sh` | twice, fabricctl task/context/execute returned the RPC result object |
| Catalog lock | `window-agent-without-entry` and `window-agent-claim-present` mutations in `capability-catalog-test.sh` |

## Adversarial notes on this windowing close

- `_desiredRect` is dropped if the compositor box drifts more than 16px from the last applied box. That is so a user drag wins over a stale request. A compositor that is merely slow to apply can drop the desired box; the Start proof now resizes before it moves and waits for the 640×400 size.
- Lua `load_start_chrome` parses numbers with a `gmatch` on the JSON text. The file is four integer fields. A later string field would be ignored, not rejected, unless the four required keys are missing.
- PID-sibling placement skip from `1ae24608` is unchanged. This close does not reopen that rule.
- Chrome CSD `fullscreen: 1` after a caption maximize is compositor truth for that client. Do not teach the harness that SSD work-area max must also be `fullscreen: 1`.

## Slice branches

These were not created this session. They are Quattro-rebase recovery refs.

| Ref | Tip | What it is |
|-----|-----|------------|
| `codex/work-pre-quattro-rebase-2026-08-27-1654` and local `work-pre-rebase-4a4cd72d` | `4a4cd72d` | Exact pre-rebase `work` tip. `HANDOFF_QUATTRO_REBASE_2026-08-27.md` says do not delete until Jesse asks |
| `codex/work-pre-quattro-rebase-2026-08-29-7b3fd00d` | `7b3fd00d` | Rebased stack plus rebase notes |
| `codex/origin-work-merge-08c5ae1d` | `08c5ae1d` | Merge of quattro into that rebased tip |

Current `work` is ahead of all three. The only path on those tips missing from current `work` is `plans/ultimate-product-completion-program-2026-08-26.md`. They were not deleted.
