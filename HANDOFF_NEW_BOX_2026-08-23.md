# HANDOFF_NEW_BOX_2026-08-23

Immutable resume for the next model and the new box. Do not edit this file. Write a new dated `HANDOFF_*` if the lock moves.

The product is **REJECTED**. Desktop shell acceptance is **FAIL** (click-through and mutual exclusion still open; ordinary outside-click and Start-orb re-toggle are already fixed). Nothing earns GO from an agent writeup. GO requires an inspectable commit SHA, adversarial source review, clean-checkout tests, and physical interaction proof.

> Windows 7 Ultimate's complete, obvious, mouse-native desktop model rebuilt for 2026, with an agent-native operating fabric underneath every system capability.

Not: Windows-like Omarchy with AI tools.

## Goal

Jesse is picking this up from a new box. W0 product-windowing, Phase 2 window fabric, and Start outside-click / orb re-toggle are **already done**. This file is the lock for everything after that.

Next work is not more windowing prose and not Agent Center UI. Next work is the two P0 security holes, then Start as a real transient (click-through, not another swallow), then screensaver / Ultimate branding / hi-fi Superbar icons.

## State of the world

**Repo:** `https://github.com/jnadeau207-collab/omarchy-7-ultimate.git`

**Branches (hard rule, two heads only):**

```text
upstream/quattro  →  main   (clean upstream tracking; do not push; do not merge work into it as the OS)
work              →  all Ultimate product work
```

Do not create `cursor/*` or other slice branches. Do not force-push. Do not amend locked SHAs `4ea1dcf3`–`c1ae994f`. Do not restore `--disable-features=WaylandWindowDecorations`. Do not put `hyprland-plugin-hyprbars` in `install/omarchy-other.packages`. Overlay rule: Desktop Mode must not rewrite `~/.config/omarchy/shell.json`.

**Done product commits (do not redo):**

| SHA | What |
|-----|------|
| `687d022bdcb08ed73bda029b896fb4494b7138b6` | W0 product-windowing + Phase 2 WindowService fabric minimum. Agent-reported metal. |
| `e3bf9385b4407023aa093cb42f15afe004979ec3` | Start outside-click and Start-orb re-toggle. Super already toggled both ways. |

This file's commit is the status record only. `git log -1 --format=%H -- HANDOFF_NEW_BOX_2026-08-23.md` is the exact SHA after clone.

**`main`:** `2c247e390e357ae0fee3f8565b0c816adb705e6a` == `basecamp/omarchy` `quattro` at ledger time (includes `#7786` About-logo animation fix).

**Metal:** SSH `jesse@192.168.1.171`, known_hosts `C:\dev\omarchy-vm\omarchy_known_hosts`, checkout `/home/jesse/omarchy7ultimate`. HDMI **must** stay `1920x1080@60`. Phantom DP-1 disabled. Jesse develops on Windows; Git bash + LF; PowerShell eats `$(…)`, `sed`, `awk`, heredocs.

**Authoritative docs:** this file (current lock), `PRODUCT_DOCTRINE.md`, `plans/project-ultimate.md`, `plans/desktop-mode-handoff.md` (historical W0 evidence), `WINDOWS_NATIVE_ACCEPTANCE.md`, `WINDOWS_7_ULTIMATE_PARITY.md`, `AGENT_NATIVE_ACCEPTANCE.md`. Reconciliation ledger **outside git:** `C:\dev\omarchy-vm\branch-reconciliation-ledger-2026-08-22.md`.

## Review signal (keep; do not relitigate Start-unfixed)

An independent review accepted an agent handoff as verified evidence, then retracted. At that moment GitHub `work` was still `2e8b559f` (ledger-only), so it had not seen `687d022b` / `e3bf9385`. That was a real process failure. It is not a reason to re-implement Start dismiss.

Signal that still applies **after** Start dismiss:

1. **A tree that is not on the connected remote is not reviewed.** Future GO claims need a GitHub SHA, not a chat.
2. **Windows click-outside is click-through.** `e3bf9385` closes Start by swallowing the click on a full-screen `MouseArea`. That is the Omarchy clipboard pattern, not Windows. The same click must still hit the window or Superbar item underneath.
3. **Do not solve transients with one more random `MouseArea`.** Shared `TransientSurfaceCoordinator` + `HyprlandFocusGrab` (Start + taskbar + child popups). One active flyout.
4. **Desktop shell acceptance stays FAIL** until the click-through / exclusion / multi-monitor matrix is green. Ordinary “click empty desktop closes” and “click Start again closes” are already proven on metal.
5. **W0 core windowing is a separate claim from Start.** Minimize, addressed close, CSD, snap, Aero drag, placement, compositor title bars are not disproven by Start. Plan “product-windowing GO” is **agent-reported**, not independently reproduced from a clean checkout of `687d022b`.
6. **Download Video command execution is in our fork. P0.** Not zero-click, not auto-root. Hostile title → extra `OMARCHY_FILE` → `mpv` option → `Util.execDetached`. Writeup: https://mehmetince.net/omarchy/ — upstream https://github.com/basecamp/omarchy/pull/7847 (`49e870c9`, open vs `quattro`).
7. **FIDO2 user-owned PAM mapping is in our fork. P0**, worse on affected machines (sudo/polkit). `pamu2fcfg >/tmp/fido2` + `sudo mv`. Upstream https://github.com/basecamp/omarchy/pull/7703 (`e83a3944`, open vs `quattro`).
8. **Persisted notification strings as shell commands are the durable smell.** Typed `CapabilityBroker` requests must replace user-data-derived `omarchy-exec`.
9. **No more feature sprawl ahead of the P0s.** Then Start click-through. Then screensaver/branding.

## Done and verified

### Tree

- Locked W0 SHAs `4ea1dcf3`–`c1ae994f`. Overlay chrome gone. `WaylandWindowDecorations` on. No AUR hyprbars pin.
- `687d022b`: three-button hyprbars (min/max/close, maximize hover → snap chooser), `default/ultimate/csd-clients.json` including Nautilus, WindowService reopen memory, `omarchy-update` rebuilds plugins after system pkgs, Superbar “Close group” / “Close window”, `CapabilityBroker` + ledger, window IPC `{ changed, error }`, agents cluster without rewriting `shell.json`.
- `e3bf9385`: `Start.qml` `Loader` + full-screen transparent overlay, card swallows, `close()` also `shell.hide`. No scrim. `test/acceptance.d/start-dismiss-proof.py` + greps in `ultimate-desktop-plugin-test.sh`.
- Download Video still vulnerable in `bin/omarchy-chromium-ytdlp-host`.
- FIDO2 still vulnerable in `bin/omarchy-setup-security-fido2`.

### Metal (agent-run, HDMI-A-1 1920×1080) — not independently reproduced

- `windows-native-test.sh` exit 0 (20–25 + unnumbered).
- `hyprbars-pointer-proof.py` exit 0.
- Live grim: Chromium one CSD row; Files no hyprbars row; three caption buttons; agents empty-state.
- Foot reopen: 1000×620 at `[120,80]`.
- Disk `shell.json`: `bar.id` unset, `position: top`.
- Start dismiss proof `ok: true`: outside closed, orb closed, card chrome stayed open, IPC toggle both ways. Grims `/tmp/start-open.png` and the three closed/still-open shots.

Start proof did **not** cover click-through, Escape, Super via uinput, Superbar-app activation while open, other flyouts, or another monitor.

## Done but UNVERIFIED

- Independent source review of `687d022b` / `e3bf9385` (reviewer never had those SHAs).
- Clean-checkout re-run of windows-native + pointer + Start dismiss on those SHAs.
- Super key via compositor bind (proof used `omarchy-shell shell toggle`).
- Click-through (current overlay swallows; known remaining Start debt).
- Screensaver (Jesse-observed; see below).
- Washed-out Superbar / Chromium-blue icons (Jesse-observed).
- Metal `/etc/fido2/fido2` ownership (`stat` not run this turn).

## Open defects after Start dismiss

### Start click-through (remaining shell debt, not “Start cannot close”)

`e3bf9385` is done. Remaining: Windows click-outside must close **and** deliver the click. Shared coordinator, not a third overlay. `HyprlandFocusGrab` whitelist: Start surface + Superbar `PanelWindow` + Start children. `onCleared: close`.

Still required before desktop-shell GO:

| Interaction | Required |
|---|---|
| Click desktop | Start closes **and** the click reaches the desktop |
| Click an application window | Start closes **and** that window gets the click |
| Click a Superbar app | Start closes **and** the app activates |
| Escape | closes |
| Super open/close via the bind | already expected to work; prove with uinput/QMP, not only IPC |
| Open Quick Settings / Notification Center | Start closes first (those surfaces do not exist yet; flags false) |
| Click on another monitor | the correct Start instance closes |

Already proven: click Start while closed; click Start again; click inside chrome stays open; click a Start item closes (launch path); IPC toggle.

Trace if click-through fails: `StartButton.onClicked` → `isPluginOpen` before toggle → toggle result → `open`/`close` → `opened` after.

### Screensaver is a windowed Omarchy logo

Jesse: *“foot just opened itself and the omarchy logo was dancing around — I think the screen saver is broken and is now a smaller windowed shell, and that shell does not have our new windows chrome it has the omarchy slop bar.”*

Tree:

- `omarchy-launch-screensaver` spawns the default terminal as `org.omarchy.screensaver` running `ttfx` on `~/.config/omarchy/branding/screensaver.txt`.
- Reset copies `$OMARCHY_PATH/logo.txt` — stock **Omarchy** ASCII, not Ultimate.
- `system.lua` asks fullscreen. Desktop Mode then `o.window(".*", { float = true, size = { 880, 560 } })` and WindowService `_placeNewClients` cascade/remembers every mapped client. Screensaver is not excluded. If the compositor reports class `foot`, W0 foot memory (`1000×620` at `[120,80]`) would make it literally look like foot opened.
- hyprbars is on for non-CSD clients. A screensaver must have **no** caption bar.

Fix: exclude `org.omarchy.screensaver` (and lock surfaces) from 880×560, hyprbars, and placement; fullscreen per output. Live moving logo can stay as a layer-shell / borderless surface with Ultimate art.

### Branding is still Omarchy / Tokyo Night

| Surface | Where |
|---|---|
| ASCII wordmark | `logo.txt` → screensaver/about branding files |
| Screensaver / About | `bin/omarchy-screensaver`, `omarchy-launch-about`, `omarchy-branding-*` |
| SDDM | `default/sddm/omarchy/Main.qml` Tokyo Night `#1a1b26`, `logo.png` (png not in the tracked sddm dir listing) |
| Plymouth | `default/plymouth/omarchy.script` `Image("logo.png")` |
| Icon font | `default/fonts/omarchy/omarchy.ttf` `U+E900` (`agents/skills/icon-font.md`) |
| Pixmap | `/usr/share/pixmaps/omarchy.png` |

**Generate (do not steal Microsoft art):** SVG + PNG wordmark “OMARCHY 7 ULTIMATE” (256/512/1024, dark and light on transparent); hi-fi Start orb (not four 5px squares at opacity 0.45); `omarchy transcode ascii` into `logo.txt`; SDDM/Plymouth off Tokyo Night; icon-font glyph with provenance. Later: our own live logo (Quickshell/shader), ttfx as fallback.

Art can be designed in parallel. Do not block P0s on a logo.

### Superbar icons washed out; Chromium still blue

- Taskbar icons: 20×20 `Image`, no `sourceSize` / devicePixelRatio (`TaskButton.qml`) — soft on 1920×1080.
- Chrome hexes `#1b1b1b` `#333333` `#3a3a3a` `#4a4a4a` `#e8943a` `#9cbc0d`; tokens do not drive Superbar/hyprbars.
- Chromium’s mark is Google blue (stock icon + CSD). Product decision: keep stock vs custom `.desktop` icon. Do not “filter the blueness.” Cheap sharpness: `sourceSize` at `20 * Screen.devicePixelRatio` and/or 24–32px slots. Durable chrome: Phase 3 tokens. Do not mix into P0 commits.

### P0 Download Video

`bin/omarchy-chromium-ytdlp-host`: prints title+path, trusts path, no `realpath` into `$HOME/Videos`, no C0 reject, `mpv` without `--`, no yt-dlp `--no-exec` / `--`. Toast `--exec` → `Service.qml` `invokePopupDefault` → `Util.execDetached(command)`.

Until patched: do not use Alt+Shift+D / Download Video; do not click “Download complete.”

Patch from `#7847`. Harden path checks to all C0, not only `\n\r\t`. Prove `bash test/shell.d/chromium-ytdlp-test.sh`.

### P0 FIDO2

`pamu2fcfg >/tmp/fido2` then `sudo mv` preserves user ownership of `/etc/fido2/fido2`.

```bash
sudo stat -c '%U:%G %a %n' /etc/fido2/fido2
```

Safe: `root:root 644`. Do not `chown` the existing inode. Apply `#7703`: `mktemp`, `sudo install -o root -g root -m 644`, migration `migrations/1787085233.sh` (inode replace), `test/shell.d/fido2-authfile-migration-test.sh`.

### Notification execution model

After yt-dlp is safe, `omarchy-exec` remains. Durable:

```json
{ "capability": "media.play", "arguments": { "path": "/home/user/Videos/example.mp4" } }
```

Broker validates, executes without a shell parser, ledgers. Legacy trusted installer toasts may keep raw commands temporarily.

## In flight

Order is the lock. Do not start Agent Center UI or a huge visual pass first.

1. **P0 Download Video** — `bin/omarchy-chromium-ytdlp-host`, `test/shell.d/chromium-ytdlp-test.sh`, `#7847` / `49e870c9`. Commit alone.
2. **P0 FIDO2** — `bin/omarchy-setup-security-fido2`, `migrations/1787085233.sh`, `test/shell.d/fido2-authfile-migration-test.sh`, `#7703` / `e83a3944`. `stat` metal if the file exists. Commit alone.
3. **Start click-through** — coordinator + `HyprlandFocusGrab`. Extend `start-dismiss-proof.py`. Do not regress outside-click / orb toggle. Commit alone.
4. **Screensaver as lock surface** — exclude class from float/size/hyprbars/placement; Ultimate `logo.txt`. Confirm with `hyprctl -j clients` while idle.
5. **Branding + hi-fi icons** — generate resources above; SDDM off Tokyo Night; icon `sourceSize`.
6. **Independent W0 certification** after 1–3, from a clean checkout of that SHA: empty `git status`, apply tree, `./test/shell`, `windows-native-test.sh`, `hyprbars-pointer-proof.py`, Start matrix, security tests. Record `Accepted W0 tree: <full SHA>`, metal versions, exact commands, known exclusions.

Later fabric depth and Phase 3 tokens stay real. They are not the next commit.

## Known traps

- Duplicate QML functions crash the shell.
- After hyprbars rebuild: `hyprctl eval '_G.omarchy_hyprbars_buttons = nil'` then reload (reload clears C++ buttons).
- No `(?:…)` in Lua patterns.
- `omarchy-shell` JSON IPC is success; `ping` stays `"ok"`.
- Do not `loginctl | awk` under `set -o pipefail`.
- `keepLoaded` + `visible: false` on a full-screen layer can leave an input sink — Start uses `Loader { active: root.opened }`. Keep that when swapping MouseArea for FocusGrab.
- ABS pointer: ydotool is the wrong seat. Reuse `hyprbars-pointer-proof.py` `AbsPointer`. Create the device **after** the overlay maps.
- `omarchy-toggle-idle stay-awake`; restore `allow-idle`.
- Metal sudo: `C:\dev\omarchy-vm\.w0-sudo` → `/tmp/.w0-sudo`, shred. Unlock: `C:\dev\omarchy-vm\uinput-type.py`.
- HDMI 1920×1080@60 is a product constraint.

## Exact commands

```bash
git clone https://github.com/jnadeau207-collab/omarchy-7-ultimate.git
cd omarchy-7-ultimate
git checkout work
git log -1 --format=%H -- HANDOFF_NEW_BOX_2026-08-23.md
```

Metal:

```bash
ssh -o BatchMode=yes -o StrictHostKeyChecking=yes \
  -o UserKnownHostsFile=/c/dev/omarchy-vm/omarchy_known_hosts \
  jesse@192.168.1.171
cd /home/jesse/omarchy7ultimate
git fetch origin && git checkout work && git pull --ff-only origin work
git rev-parse HEAD
hyprctl eval 'hl.monitor({ output = "HDMI-A-1", mode = "1920x1080@60", position = "0x0", scale = 1 })'
```

Until P0s land: do not use Download Video; do not click its completion notification.

## Resume

`resume: read HANDOFF_NEW_BOX_2026-08-23.md §In flight` — start at item 1 (Download Video P0).
