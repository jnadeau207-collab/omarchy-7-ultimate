# HANDOFF_W0_2026-08-23

Do not edit this file. If the lock moves, write a new dated `HANDOFF_*`. Do not edit `HANDOFF_NEW_BOX_2026-08-23.md`.

The product is still **REJECTED**. This file is not GO. Agent writeup is not GO. IPC `omarchy-shell window maximize` is not maximize.

> Windows 7 Ultimate's complete, obvious, mouse-native desktop model rebuilt for 2026, with an agent-native operating fabric underneath every system capability.

## What blacked the TV this morning

This was **not** an NVIDIA driver, GSP, modprobe, Caps Lock, or click-to-raise change.

This Samsung HDMI panel's **preferred mode is 4K@30, which is no-signal**.

Night session: pointer proofs called `pin_session_monitor()` / `windows-native-test.sh`, which did `hyprctl eval 'hl.monitor({ output = "HDMI-A-1", mode = "1920x1080@60", ...})'` and disabled phantom DP outputs. Runtime 1080p kept the TV alive. Each modeset logged `nvidia-modeset: WARNING: GPU:0: Unable to read EDID for display device DP-0` (overnight 00:35–03:23, then 11:27).

`~/.config/hypr/monitors.lua` stayed **stock** until morning (`mode = "preferred"`, `GDK_SCALE=2`). Evidence: `~/.config/hypr/monitors.lua.pre-rescue`. Reboot **2026-08-23 11:39** reapplied preferred → 4K@30 → black TV, SSH still worked. Jesse wrote the live pin at **11:42**:

```lua
-- Samsung TV on HDMI. preferred/4K@30 is no-signal on this panel.
hl.monitor({ output = "HDMI-A-1", mode = "1920x1080@60", position = "0x0", scale = 1 })
hl.monitor({ output = "DP-1", disabled = true })
-- also DP-2, DP-3 disabled
```

**Do not** `hyprctl eval` monitor modes on HDMI-A-1. **Do not** `omarchy-refresh-hyprland` (overwrites `monitors.lua` with stock preferred). `HANDOFF_NEW_BOX_2026-08-23.md` still contains that eval in Exact commands — ignore it. `pin_session_monitor()` is now a no-op. The windows-native harness only evals `HEADLESS*` outputs.

Live check this session: HDMI-A-1 **1920×1080@60**.

## Tree

Repo `/home/jesse/omarchy7ultimate`, branch **`work`** (ahead of `origin/work`). Two heads only: `main` = upstream quattro, `work` = Ultimate. Do not create `cursor/*` branches, force-push, or amend locked SHAs.

HEAD when this file was written (then this HANDOFF commit sits on top): `cd0899c6a270fb04a9ffae9957cbe51b908f8986`

| SHA | What |
|-----|------|
| `6319f79b85041eba1718d0a95e4d2bea43235450` | P0 Download Video — do not redo |
| `99ce4083701a31b437123d21aef0db73fa4c3cc5` | P0 FIDO2 PAM mapping — do not redo |
| `7d9542b38b8c60318a219353008d1e9232e04b33` | Caps Lock stays Caps Lock |
| `25a407eee67157120219b323309db957fa02e210` | CSD/hyprbars maximize same as minimize; no global `mouse:272` dismiss |
| `8f005883f00857f9c84f70b1e1e8ebf306a5843c` | Screensaver is a lock surface, not 880×560 foot |
| `4404b1b9c2c27041cf89020d7bd6691335832274` | Superbar icons `sourceSize` at 20 × devicePixelRatio |
| `6a548ba9ec7d9640d3b0cfc86fab0c0bb7502f64` | Start is a 440×560 card; click-through via compositor + `onActiveToplevelChanged` |
| `cd0899c6a270fb04a9ffae9957cbe51b908f8986` | windows-native harness must not modeset live HDMI |

Architecture lock: card-sized Start 440×560, no `HyprlandFocusGrab`, no fullscreen Start `MouseArea`, no global `mouse:272` dismiss. Click-through = compositor delivers the click + `onActiveToplevelChanged` (`!next` ignored). `Start.close` calls `activateAtCursorSoon` only when `raiseUnderCursorOnClose` is set. Wallpaper `dismiss()`. Superbar `setExempt("taskbar")`.

**Maximize-by-click is in W0.** It is not an exclusion.

## Proven this boot (not GO)

- HDMI stayed 1920×1080@60 after `omarchy-restart-shell`.
- Start IPC summon: layer `omarchy-start` **440×560 at (8, 472)** on HDMI-A-1. Grim `/home/jesse/Pictures/screenshot-2026-08-23_12-19-36.png` (card, search, pinned list, Superbar, no desktop dim). Hide unmapped the layer.
- `./test/shell` on this machine: **6 of 195 files failed**, none of them the Start/window files we added:
  - `config-test.sh`, `snapper-test.sh`, `unowned-system-paths-test.sh` — no `omarchy-pkgs` checkout
  - `manifest-entrypoints-test.sh` — pre-existing `shell/plugins/dev-gallery/GalleryPanel.qml` missing `}`
  - `keybindings-menu-test.sh` — alternative chord row join
  - `launch-about-test.sh` — roomy-window animation
- Focused greps: `ultimate-desktop-plugin-test.sh`, `window-service-test.sh` — green.

## Not proven this boot

- Absolute-pointer Start matrix, hyprbars caption clicks, Chromium CSD clicks. `/dev/uinput` is `crw------- root` after the 11:39 reboot. `sudo -n` fails. **Do not chmod uinput as a surprise.** Last night those proofs were green when uinput was `crw-rw----` group `input`.
- Clean-checkout SHA apply + independent cert.
- User click of Cursor CSD `□` (never AbsPointer Cursor).

## Known exclusions (if still true at cert)

- Multi-monitor Start
- Branding / Phase 3 tokens / Agent Center
- This boot: uinput 0600 so pointer proofs cannot run
- `./test/shell` env gaps above (omarchy-pkgs, GalleryPanel)

Maximize-by-click is **not** an exclusion.

## Do not

- `hyprctl eval` `hl.monitor` on HDMI-A-1 or phantom DP-*
- `omarchy-refresh-hyprland`
- AbsPointer Cursor
- Claim W0 GO from chat
- Merge `work` into `main`
- `HyprlandFocusGrab` or a fullscreen Start click sink

## Next

1. Jesse (or explicit ask) restore `/dev/uinput` to group `input` writable, same as last night.
2. Run `test/acceptance.d/hyprbars-pointer-proof.py`, `csd-caption-pointer-proof.py`, `start-dismiss-proof.py` on HDMI. They tuck Cursor; they must restore it.
3. Independent cert: empty `git status`, `./test/shell` (record remaining env fails honestly), pointer proofs green, new HANDOFF with `Accepted W0 tree: <full SHA>`. Product stays REJECTED until that SHA exists. Known exclusions stay listed. **Maximize-by-click is not an exclusion.**
