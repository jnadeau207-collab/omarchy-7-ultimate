# Files and default-application providers

The Files and Defaults providers are typed Fabric backend foundations for consumer surfaces that need Files, This PC, Desktop, Trash, removable and SMB mounts, recent items, bounded search, and default MIME or protocol applications. They fit the central provider registry without granting the provisional daemon an untracked host mutation path.

## Provider surfaces

`files.provider` exposes these read actions:

- `inspect` returns one revisioned workspace containing locations, entries, mounts, and recent-item ordering.
- `browse` returns bounded children of one canonical location-relative directory.
- `search` performs a deterministic bounded search over the trusted inventory snapshot.
- `recent` resolves recent-item identities back to the same typed entry records used by browse and search.

It also exposes complete preflight, execute, validate, and undo hooks for `directory.create`, `entry.rename`, `entry.copy`, `entry.trash`, `trash.restore`, `trash.manage`, `mount.connect`, and `mount.disconnect`, plus a launch-plane `entry.open` hook (identity-scoped `xdg-open`; listing unchanged; no undo). The central registry names the runtime hooks `apply` and `rollback`; the provider keeps those as exact aliases of execute and undo so the lifecycle vocabulary remains explicit to future durable-operation consumers.

`defaults.provider` exposes `inspect`, `mime.query`, `protocol.query`, and `association.inspect`, plus complete lifecycle hooks for `mime.set`, `protocol.set`, and `association.clear`. `association.inspect` is the scoped reader durable apply uses to re-read `{kind, key, defaultAppId}` — the same class as Files `directory.inspect`. Its preflight includes the association revision, selected application revision, whole-database revision, and a closed execution-plan descriptor naming the typed helper as `defaults.apply-v0` with `shell: false`.

## Identity and revision rules

Provider resource identities are deterministic SHA-256-derived stable IDs. Native paths, device selectors, and desktop-file paths are not used as Fabric resource IDs. Whole-domain revisions and per-entry, per-location, per-application, and per-association identities use canonical finite JSON fingerprints. Fake inputs are canonicalized by stable ID before admission, so backend enumeration order cannot change a consumer-visible revision.

The Files workspace is the single rollback unit. This makes a preflight freeze every location, entry, mount, and recent-item relationship that can affect a safe mutation. The Defaults database is likewise one rollback unit so a change cannot validate against a different application or association inventory than the one the user approved.

## Files path boundary

All caller-supplied Files paths are location-relative UTF-8 values. The provider normalizes Unicode to NFC and rejects absolute paths, backslashes, empty segments, `.` or `..`, control characters, NUL, overlong segments, and overlong paths. Mutations reject symlink targets and any symlink in the selected ancestry.

Real inventory opens every component of roots and descendant directories with `O_DIRECTORY` and `O_NOFOLLOW`, and performs descendant metadata reads through directory file descriptors. Configuration, XDG user-directory, recent-file, desktop-file, and fake restart documents are opened through the same no-follow component boundary, must be regular files, are byte-bounded, and are rejected if device, inode, size, mode, ctime, or mtime changes during the read. A race that changes an opened identity is skipped or reported as typed degradation rather than trusted. Preflight records the whole-workspace revision plus root and entry identity anchors; execute performs an atomic compare-and-swap on the same revision, which closes the time-of-check/time-of-use gap at the provider seam.

`files.entry.open` is launch-plane reachable (`files-entry-open` helper). Real preflight binds entry identity and the parent directory scope, then the helper launches `/usr/bin/xdg-open` on the resolved path. Risk is `low`. SHELL can hold a standing grant. Catalog claim is partial; humanRoute is visible `Files > Open`. Agent stays unavailable. Files Open / double-click uses that plane for regular files, including PDF via the tip-true default/associated graphical viewer (`application/pdf` → Evince in `mimeapps.list`) and `.txt` via the tip-true default/associated graphical editor (`text/plain` → org.gnome.TextEditor in `mimeapps.list`, not Neovim-as-default). session leftover recorded: `files.document.open` stays leftover `legacy-direct` with claim=partial and visible `Files > PDF`, soft leftover-attached to the same `openEntry` mouse path (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close `windows-native.17`. `windows-native.17` stays prototype/pending. session leftover recorded: `files.text.edit` stays leftover `legacy-direct` with claim=partial and visible `Files > Text`, soft leftover-attached to the same `openEntry` mouse path (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close `windows-native.18`. `windows-native.18` stays prototype/pending. It does not read file contents and does not invent an Open With picker or MIME association UI product-complete. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

`files.entry.rename` is write-plane reachable (`files-entry-rename` helper). Real preflight binds entry identity inside the scoped directory listing (`selectedEntry.{entryId,identity}`) and keeps the rename in the same directory. The helper refuses collision, identity drift, traversal, symlink targets, and Trash. Risk is `low`. SHELL can hold a standing grant. Catalog claim is partial; humanRoute is visible `Files > Rename`. Agent stays unavailable. Files Rename uses that plane. It does not invent cut, move-across-directories, or an OS clipboard.

`files.entry.copy` is write-plane reachable (`files-entry-copy` helper). Real preflight binds source identity inside the destination directory listing and writes a scoped regular-file or directory replica into a writable Files location. The helper refuses collision, identity drift, traversal, symlink targets, symlink loops, nest-inside-source, and Trash. Copy maps `EXDEV` errno from `mkdir`/`open` only; `copyfileobj` does not raise `EXDEV`. Overlay or bind-mount children may copy. There is no `st_dev` walk-root refuse. Risk is `low`. SHELL can hold a standing grant. Catalog claim is partial; humanRoute is visible `Files > Copy and Paste`. Agent stays unavailable. Files Copy and Paste use that plane with in-app staging and also place or read files on this session's clipboard (`files-clipboard-copy` / `files-clipboard-paste`). OS clipboard residual CLOSED for Files copy-out/paste-in. It does not invent thumbnails, LIVE Cut, Empty Bin LIVE, or Restore LIVE. OS clipboard residual OPEN after PR #64.

`files.entry.move` is write-plane reachable (`files-entry-move` helper). Real preflight binds source identity inside the destination directory listing and relocates a regular file with `os.rename`. The helper refuses collision, identity drift, traversal, symlink targets, directories, Trash, same-directory name changes (those stay `entry.rename`), and cross-device `EXDEV`. Risk is `consequential` because the source name disappears from its original listing, the same class as `entry.trash`, not an additive replica. SHELL cannot hold a standing grant. Catalog claim is partial; current humanRoute is visible `Files > Cut`. Dated leftover humanRoute planned empty is stale vs that session-visible route and is Fabric under SHELL. Session Cut is visible `Files > Cut` through this session's `files-entry-move` helper. Paste-after-cut composes with the existing OS clipboard / in-app copy path. Agent stays unavailable. Files pins `cutAuthorized=false` as leftover Fabric SHELL refuse and does not offer Fabric LIVE Cut. Session Cut does not consult that pin. Folder copy CLOSED via `files.entry.copy` directories; move still refuses directories. It does not invent Explorer claim=present or LIVE Cut under SHELL.

`files.entry.delete` is write-plane reachable (`files-entry-delete` helper). Real preflight binds entry identity inside the scoped directory listing and permanently removes a regular file (`unlink`) or an empty directory (`rmdir`). The helper refuses identity drift, traversal, symlink targets, non-empty directories, and Trash. It does not recursively `rm -rf` a tree. Risk is `consequential`, the same class as `entry.trash` / `entry.move`. SHELL cannot hold a standing grant. Catalog claim is partial; current humanRoute is visible `Files > Permanently delete`. Dated leftover humanRoute planned empty is stale vs that session-visible route and is Fabric under SHELL. Session Permanent Delete is visible `Files > Permanently delete` through this session's `files-entry-delete` helper after confirm. Agent stays unavailable. Files pins `deleteAuthorized=false` as leftover Fabric SHELL refuse and does not offer Fabric LIVE Delete. Session Permanent Delete does not consult that pin. The permanent delete write plane exists but is not shell-authorizable. It does not invent Empty Bin LIVE or Recycle product-complete. OS clipboard residual OPEN after PR #60. OS clipboard residual OPEN after PR #64. Recycle / Empty Bin LIVE / `files.trash.manage` residual OPEN after PR #63.

`files.archive.create` is session-reachable (`files-archive-create` helper) and is not a Fabric durable writer. Files Compress zips selected regular files and folders through this session's `omarchy-fabric-session-apply` path. Paths stay on stdin JSON. The helper refuses traversal, symlink sources, symlink children, and Trash, and writes a non-colliding `.zip` beside the selection. Catalog claim stays partial; provider state stays leftover `legacy-direct`; humanRoute is visible `Files > Compress`. Agent stays unavailable. It does not invent a Fabric SHELL LIVE archive writer, Explorer claim=present, or `windows-native.13` present. Cloud EXIT 0 is not metal leftover CLOSED.

`files.archive.extract` is session-reachable (`files-archive-extract` helper) and is not a Fabric durable writer. Files Extract unzips a selected `.zip` through this session's `omarchy-fabric-session-apply` path into a non-colliding folder named after the archive, created beside the zip. Paths stay on stdin JSON. The helper opens with O_NOFOLLOW and refuses zip-slip (`..` / absolute arcnames), symlink writes, traversal, Trash, and bounds entry count plus uncompressed bytes. Catalog claim stays partial; provider state stays leftover `legacy-direct`; humanRoute is visible `Files > Extract`. Agent stays unavailable. It does not invent a Fabric SHELL LIVE archive writer, Explorer claim=present, or a windows-native Extract present row. `windows-native.13` stays Compress-only prototype/pending. Cloud EXIT 0 is not metal leftover CLOSED.

`files.properties.read` is session-reachable (`files-entry-properties` helper) and is not a Fabric durable writer. Files Properties reads name, type/kind, size for regular files, modified time, and location path through this session's `omarchy-fabric-session-apply` path. Paths stay on stdin JSON. The helper opens with O_NOFOLLOW and refuses symlink follow, traversal, and Trash, and bounds the location path. Catalog claim stays partial; provider state stays leftover `legacy-direct`; humanRoute is visible `Files > right-click > Properties`. Agent stays unavailable. It does not invent a Fabric SHELL LIVE Properties writer, attribute toggles, NTFS ADS, permissions UI, or Explorer claim=present. `parity.properties` stays missing as product. Cloud EXIT 0 is not metal leftover CLOSED.

`storage.removable.eject` is session-reachable (`storage-removable-eject` helper) and is not a Fabric durable writer. The session Eject plane landed; this is session leftover honesty, not product CLOSE. Files Devices Eject unmounts a selected removable volume under `/run/media/$USER/` or `/media/$USER/` through this session's `omarchy-fabric-session-apply` path, then ejects optical (`udisksctl eject`) or powers off the USB parent disk (`udisksctl power-off`) when udisks supports it. Identities stay on stdin JSON (`mountId` only). The helper re-inventories `/proc/self/mountinfo` with the same Files mount digest and refuses system disks, SMB, traversal, and non-`/dev` sources. Busy or in-use failures use `device.busy` and do not pretend eject succeeded. Scope is USB volumes plus optical. Catalog claim stays partial; provider state stays leftover `legacy-direct`; humanRoute is visible `Files > Devices > Eject`. Agent stays unavailable. session leftover recorded: Files Devices Eject session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close `windows-native.15`. `windows-native.15` stays prototype/pending. It does not invent a Fabric SHELL LIVE eject writer, Explorer claim=present, Devices and Printers product, or `windows-native.15` present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

`storage.removable.mount` is session-reachable (`storage-removable-mount` helper) and is not a Fabric durable writer. The session Mount plane landed; this is session leftover honesty, not product CLOSE. Files Devices Mount mounts a selected unmounted removable volume through this session's `omarchy-fabric-session-apply` path with absolute `/usr/bin/udisksctl` (`gio` fallback if udisksctl is unavailable). Identities stay on stdin JSON (`volumeId` only). The helper inventories `/usr/bin/lsblk` and refuses system disks, locked crypto, traversal, and non-`/dev` sources. Busy or in-use failures use `device.busy` and do not pretend mount succeeded. Structured auth refusals use `mount.auth-denied` and do not echo secrets. Scope is USB volumes plus optical. Catalog claim stays partial; provider state stays leftover `legacy-direct`; humanRoute is visible `Files > Devices > Mount`. Agent stays unavailable. session leftover recorded: Files Devices Mount session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close `windows-native.14`. `windows-native.14` stays prototype/pending. It does not invent a Fabric SHELL LIVE mount writer, Explorer claim=present, Devices and Printers product, or `windows-native.14` present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

`sharing.smb.connect` is session-reachable (`sharing-smb-connect` helper) and is not a Fabric durable writer. The session Connect plane landed; this is session leftover honesty, not product CLOSE. Files Connect to Server mounts a guest or public SMB share through this session's `omarchy-fabric-session-apply` path with absolute `/usr/bin/gio mount --anonymous`. Identities stay on stdin JSON (`host` and `share` only). The helper validates hostname or IPv4 plus a single share name and refuses userinfo, path separators, and extra credential keys. Password shares use `share.auth-required` and do not invent password vault or keyring UI. Secrets are never echoed. Catalog claim stays partial; provider state stays leftover `legacy-direct`; humanRoute is visible `Files > Connect to Server`. Agent stays unavailable. session leftover recorded: Files Connect to Server session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close `windows-native.16`. `windows-native.16` stays missing/pending. It does not invent a Fabric SHELL LIVE SMB writer, Explorer claim=present, Network Places product, or `windows-native.16` present. Cloud EXIT 0 is not metal leftover CLOSED. Product REJECTED.

The real adapter never reads user file contents. It inventories bounded metadata only. Startup traversal is capped by entry count, depth, and a 36 KiB state budget. Byte-budget trimming removes leaves before ancestors, and semantic admission independently rejects slash-containing orphan roots, inconsistent parents, symlink ancestors, and spoofed mount/location relationships. It parses `/proc/self/mountinfo` directly under a byte bound, classifies SMB only from the kernel filesystem type, strips authority credentials, returns only a bounded host and first share component, and does not traverse removable or network roots during startup. Recent-file XML is byte-bounded, refuses DTD or entity declarations, accepts only local `file:` URIs, and resolves only paths already present in the trusted inventory.

Trash is recoverable, not permanent deletion. `entry.trash` preserves the original location, parent identity, and relative path in typed recovery metadata. The scoped `files-directory-{preflight,result,state}-v1` family was widened in place — `action` is an enum of `directory.create` and `entry.trash` (and still lists `trash.restore`) rather than a parallel Trash family — because trashing an entry is a name disappearing from the same `{locationId, parentRelativePath, names}` listing `directory.create` already validates from disk. v0 workspace schemas were not edited. Shared family is not shared risk: `directory.create` stays `low`; `entry.trash` stays `consequential`. SHELL cannot hold a standing grant for trash.

`files.trash.restore` is write-plane reachable (`files-trash-restore` helper). Real preflight reads the trashed entry's `.trashinfo` and scopes the original destination directory, the listing-diff inverse of `entry.trash`. Risk stays `consequential`. SHELL cannot hold a standing grant. Catalog claim is partial; current humanRoute is visible `Files > Restore`. Dated leftover humanRoute planned empty is stale vs that session-visible route. Session Restore runs through this session's `files-trash-restore` helper. Fabric Restore LIVE under SHELL stays refused. The `files.entry.delete` write plane exists but is not shell-authorizable (`deleteAuthorized=false`). Recycle / Empty Bin LIVE residual stays OPEN after PR #63. Recycle Bin is not product-complete.

`files.trash.manage` is write-plane reachable (`files-trash-manage` helper). Real preflight binds the Trash location listing and permanently removes regular files (`unlink`) and empty directories (`rmdir`) plus their `.trashinfo`. The helper refuses identity drift, traversal, symlink targets, non-empty trash trees, and any location other than Trash. It does not recursively `rm -rf` a tree. Risk is `consequential`, the same class as `entry.trash` / `entry.delete`. SHELL cannot hold a standing grant. Catalog claim is partial; current humanRoute is visible `Files > Empty Recycle Bin`. Dated leftover humanRoute planned empty is stale vs that session-visible route. Session Empty Recycle Bin runs through this session's `files-trash-manage` helper. Agent stays unavailable. Files pins `emptyBinAuthorized=false` and does not offer Empty Bin LIVE under SHELL. The empty Recycle Bin write plane exists but is not shell-authorizable under SHELL. Desktop Recycle icon, Recycle Properties, and Recycle product-complete stay leftovers. Recycle / Empty Bin LIVE residual OPEN after PR #63.

## Default-application boundary

The real Defaults adapter reads `.desktop` files through no-follow directory file descriptors, accepts only stable bounded regular files with ASCII desktop IDs, rejects control-bearing display fields, and never returns an absolute icon path. User entries are scanned before local and system entries so the global bound cannot starve higher-priority intent; user entries override local entries, local entries override system entries, and a higher-priority `Hidden=true` tombstone suppresses the lower-priority application. The final database is capped at 36 KiB, and trimming rewrites candidate lists and dangling status before semantic validation.

Default queries are a code-owned catalog of immutable `FixedArgvCommand` values for `/usr/bin/xdg-mime query default`. No caller value is appended to argv, no command is interpreted by a shell, and the shipped association catalog must match the code-owned MIME and protocol tuple exactly. Empty, missing, malformed, or dangling query results remain explicit unconfigured, degraded, or dangling association states.

The real adapter deliberately does not call `xdg-mime default`. Its `compare_and_swap` stays mutation-unavailable. Production builtins set `session_operable=True`, so `mime.set` and `protocol.set` preflight are reachable. Durable apply re-reads scoped state through published `association.inspect`, then the coordinator runs the typed `defaults-mime-set` and `defaults-protocol-set` helpers. Settings > Default Programs offers LIVE CONTROL for the default browser (`protocol.set`, http+https via `applyDefaultBrowser`) and for per-MIME defaults (`mime.set`, writable associations with more than one installed candidate). Catalog `defaults.protocol.set` stays claim=partial with visible `Settings > Default Programs`. session leftover recorded: soft leftover-attach ACC `windows-native.19` to that tip-true browser plane (not product CLOSED / not metal CLOSED / not claim=present); `windows-native.19` stays prototype/pending; Cloud mocks do not close windows-native.19. AutoPlay, Set Program Access and Computer Defaults, and `files.associations.set` stay unavailable / missing/planned MIME. Do not invent MIME association UI / AutoPlay / SPAC / `files.associations.set` product-complete or claim=present.

## Availability

Availability is a three-state typed value:

- `available` means reads and fake lifecycle operations are complete and no degradation reasons exist.
- `degraded` means a trusted partial or read-only snapshot exists, with one or more structured reasons.
- `unavailable` means no trusted state exists and at least one structured reason explains why.

The real Defaults backend still refuses provider-side mutation. Production preflight is session-operable; the helper owns the write. A `session_operable=False` builder stays read-only with an `operation.integration-required` recovery seam. Missing folders, unsafe roots, malformed recent XML, bounded-inventory truncation, missing `xdg-mime`, and malformed query output are reported independently instead of being hidden by mock data or fallback commands.

## Hermetic lifecycle adapters

Fake adapters exist solely for contract, operation, restart, and adversarial tests. They validate the full closed state schema on construction and after every transition, cap mutable state at 12 KiB so the three-state preflight remains within Fabric's frame budget, cap the persistence envelope at 16 KiB, reject duplicate keys and non-finite numbers on restart, and require an existing real absolute persistence parent. Persistent transitions reload under a process-shared lock and a POSIX advisory lock, write an owner-only exclusive temporary file, fsync the file and directory, and atomically replace the state document before publishing the new in-memory revision. Independent provider instances therefore share one compare-and-swap boundary instead of losing updates from stale restart snapshots.

Every recovery payload binds the prior state to the action and exact proposed-state revision. Undo verifies the prior state's semantic fingerprint before any write and refuses an old recovery payload when the current revision contains newer intent, even if the caller supplies that newer current revision as the generic compare-and-swap value. A repeated undo against an already restored exact state is a no-op; a mismatched wrapper is a typed `state-corrupt` failure with zero writes.

The tests cover central registry admission, closed schema traversal, every read and operation action, no-op writes, exact validation and guarded undo, rollback over newer intent, same-process and independent-instance concurrency, cancellation before lock admission, state drift after preflight, orphan graphs, Unicode control attacks, symlink and ancestor-symlink traversal, destination collisions, restart persistence, duplicate-key corruption, non-finite corruption, backend failures, no-follow Linux inventory, malicious and symlinked recent XML, mount authority spoofing and credential redaction, fixed argv, missing dependencies, malformed and secret-bearing probe failures, bounded priority overrides, unsupported applications, read-only associations, and unauthenticated preflight.

## Integration seams

Central integration should register `build_provider()` from `omarchy_fabric.providers.files` and `omarchy_fabric.providers.defaults` in the code-owned provider catalog. Registration is intentionally outside this tranche so the root registry, capability catalog, parity graph, packaging, and durable operation coordinator can be updated atomically by their owners.

`OMARCHY_PATH` must identify the installed Omarchy tree. The Files provider loads `default/ultimate/files/locations-v0.json`; the Defaults provider loads `default/ultimate/files/default-associations-v0.json`. Both refuse to construct when that session invariant is absent.

A future Files host executor must preserve the same location-relative contract, openat-style no-follow resolution, frozen root and entry identity checks, and atomic revision semantics. It must not accept absolute paths or shell commands. A future Defaults executor must be the named typed helper or a versioned successor, accept structured input through a non-argv channel, recheck association and application revisions, and validate the resulting database before reporting success.

`test/shell.d/fabric-files-defaults-test.sh` is a runnable shell-test entry point and must be committed with executable mode `100755`. Archive copies made on filesystems without POSIX mode support can lose that bit; integration must restore it before committing rather than weakening the wrapper requirement.
