import QtQuick

// Grouped taskbar button. TaskButton already renders a pin/app group
// (multiple windows share one button), so this file exists as the named
// type the taskbar plugin contract lists.
TaskButton {
}
