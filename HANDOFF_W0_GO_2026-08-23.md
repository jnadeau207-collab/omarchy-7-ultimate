# HANDOFF_W0_GO_2026-08-23

Do not edit this file. If the lock moves, write a new dated `HANDOFF_*`.
This file supersedes `HANDOFF_W0_CERT_2026-08-23.md` on certification status
only. Traps in `HANDOFF_W0_METAL_2026-08-23.md` still apply.

The **product / OS is still REJECTED.** W0 is a windowing architecture gate, not
a shippable Windows 7 Ultimate. That is the doctrine. It is not a project
failure.

## Verdict

**Machine proofs for Windowing Gate W0: PASS on `ab94cbd96e146b1d6669ca0a9894a7f78c2f9bbd` (`work`).**

`Accepted W0 tree:` is **not written yet**. `W0_GATE.md` item 8 is Jesse's
eyes on the TV vs grim. Grim-vs-glass has lied on this HDMI before. Look at
`C:\dev\omarchy-vm\desktop-rescue\w0-go-final.png` (Sunday 22:49 on the Superbar
clock). If that is what the Samsung is showing, reply that it matches and the
accepted SHA is this one.

Adversarial source review of the windowing path was done this session (item 9).
It is not deferred.

## What ran on the box

Live tree and clean clone were both `ab94cbd9`. Hyprland was **reloaded** so
today's lua was actually in the running desktop, then Quickshell was restarted.
HDMI-A-1 stayed 1920×1080@60. No SDDM restart for this run.

| Gate item | Result |
|-----------|--------|
| 1. Pushed SHA | `ab94cbd96e146b1d6669ca0a9894a7f78c2f9bbd` |
| 2. Clean clone, empty status | `/home/jesse/w0-cert`, dirty=0 |
| 3. `./test/shell` | exit 1, 7/198 files, classified below. No windowing regression. |
| 4. `monitor-apply-test.sh` | green |
| 5. `windows-native-test.sh` | **exit 0, 76 ok, 0 not ok** including Alt+Tab and restoreLayout |
| 6. Three pointer proofs | hyprbars=0, csd=0, start-dismiss=0. `/dev/uinput` `root:input 660` via shipped udev. |
| 7. Auto ranking | emit-lua `HDMI-A-1` `1920x1080@60.00`, DP-* disabled, no `preferred` |
| 8. Human eyes | **open — Jesse** |
| 9. Adversarial review | done this session |

Focus still worked after `hyprctl reload` (the earlier null-focus wedge). The
restoreLayout miss on the first harness run was saveLayout capturing a lagged
880×560 float; `ab94cbd9` stamps the snap verb. Second harness run was green.

### `./test/shell` 7 failures, classified

| Test | Classification |
|------|----------------|
| `config-test.sh`, `snapper-test.sh`, `unowned-system-paths-test.sh` | env gap: no `omarchy-pkgs` checkout |
| `keybindings-menu-test.sh` | known pre-existing (alternative chord row join) |
| `branding-about-animation-test.sh` | known pre-existing (About logo animation) |
| `manifest-entrypoints-test.sh` | known flake: Quickshell DBus/bluez `GalleryPanel` family |
| `ssh-reconnect-test.sh` | flaky mock harness |

## Do not

- Do not treat this file as OS GO.
- Do not cite the earlier REJECTED cert (`c5ff25ce`) or the SDDM-babysat round as this evidence.
- Do not `hyprctl eval` a hardcoded HDMI pin. Ranking stays.
- Do not surprise-chmod `/dev/uinput`.

## Resume

`resume: look at the TV vs C:\dev\omarchy-vm\desktop-rescue\w0-go-final.png`. If it matches, write `Accepted W0 tree: ab94cbd96e146b1d6669ca0a9894a7f78c2f9bbd` into a new dated handoff. If it does not match, this SHA is not accepted.
