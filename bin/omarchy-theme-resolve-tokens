#!/bin/bash

# omarchy:hidden=true
# omarchy:summary=Resolve the active theme into semantic design tokens
# omarchy:args=--active | --colors <colors.toml> [--shell <shell.toml>] [--output <file>] [--chrome-output <file>] [--stdout]

set -euo pipefail

exec python3 "$OMARCHY_PATH/default/ultimate/design-system/resolve_tokens.py" "$@"
