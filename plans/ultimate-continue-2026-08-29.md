# Continue Ultimate: Windows 7 desktop and agent-native Omarchy

Living execution after the 2026-08-29 Quattro rebase. Identity remains `PRODUCT_DOCTRINE.md`. Fabric backbone remains `ultimate-capability-engines-product-ownership-convergence-2026-08-27.md`. This file replaces stale “current position” SHAs in `project-ultimate.md` and the 2026-08-26 program header.

## Identity (locked)

Windows 7 Ultimate's complete, obvious, mouse-native desktop rebuilt for 2026, **and** Omarchy's agents in their truest form: every meaningful capability callable on the same path a human uses.

Not Windows-like Omarchy with a chat panel. Not Fabric plumbing with Tokyo Night, Nautilus, nvim, and Super+Space as the product. Both halves are the product. `work` does not merge into `main` as the OS until the six program conditions pass at one packaged SHA.

## Git (after Step 0)

Rebase onto Quattro is the integration method. Do not merge Quattro into `work`.

- Recovery: `codex/work-pre-quattro-rebase-2026-08-27-1654` (`4a4cd72d`), `codex/work-pre-quattro-rebase-2026-08-29-7b3fd00d`, `codex/origin-work-merge-08c5ae1d`
- `main` tracks live `upstream/quattro`
- pkgs remains `e902923c` pin `5504a044`; ISO remains quattro `268bac16`
- Metal packaged pair remains `omarchy 4.0.0-1` without fabricd until Issue 16

After each rebase: Windows `work`, GitHub `origin/work`, and metal `/home/jesse/omarchy7ultimate` are the same SHA. Never `reset --hard` on a dirty box. Chrome geometry, `WaylandWindowDecorations`, HDMI pin ban, overlay-must-not-rewrite-`shell.json`, no mutation UI — all stay.

## Dual tracks

| Track | May run now | Must wait |
|---|---|---|
| **W — Windows 7 desktop** | Identity, chrome, read-only hosts, first-run/manual, SUPER+E → first-party Files (read) | Jobs that mutate the machine |
| **A — Agent-native Omarchy** | Issue 7 metal reads, honest empty/offline/stale, checkout-runnable daemon | Execution UI, `provider.apply`, privileged executors, Agent Center Run/Approve |

Join rule: a Windows 7 surface is not done until an agent can invoke the same capability. Until then `availability.agent=unavailable` and no fake success. `CapabilityBroker.permit("agent")` is not the agent path.

## Done in this follow-through

- Step 0: Ultimate replayed onto live Quattro; GitHub merge `08c5ae1d` is not the product tip
- Phantom-success `windowIpc` default removed; overlay summons do not report window mutation
- Desktop Mode first-run toast and `manual/01` + `manual/04` lead with Start/Superbar
- Default theme seed is Ultimate Dark
- Window catalog `availability.agent` is `unavailable` until Fabric owns the verb
- Catalog/product-contract tests fail closed without `python-jsonschema`

## Next

1. Issue 7 metal close: checkout `omarchy-fabricd` on metal, recorded proofs, no execution UI
2. W2 remainder: consumer chrome font; light theme actually propagates
3. W3 after frozen reads: SUPER+E → `org.omarchy.Files`; Win+I → standalone Settings; `.txt` → Kate
4. Issue 8 coordinator in the daemon before any product mutation

## Refuses

Choosing agents or Windows 7. Merging Quattro into `work`. Declaring Issue 1 / 5 / W0-eyes closed. Mutation UI. Software Center as a store on contract-seed digests. WSL skip-as-pass as metal recert. Pkgs/ISO pin on a moving tip.
