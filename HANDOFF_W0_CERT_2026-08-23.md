# HANDOFF_W0_CERT_2026-08-23

Do not edit this file. If the lock moves, write a new dated `HANDOFF_*`. This
file supersedes `HANDOFF_W0_METAL_2026-08-23.md` on certification status only;
its traps and do-nots still apply.

The product is still **REJECTED**. This file is not OS GO.

## Verdict

**W0 gate certification: REJECTED on tree `c5ff25ce25ac5a0845f2733f4f0cec831e6dd295` (`work` HEAD).**

No `Accepted W0 tree:` record exists because the gate did not pass. The prior
session's claim that `windows-native-test.sh` exits 0 on metal "including all
three pointer proofs" **did not reproduce** hours later on the same box,
same session, same code.

## Certification method

Per `W0_GATE.md`. Fresh GitHub clone into `/home/jesse/w0-cert`, empty
`git status`, `chmod 0755 bin/omarchy-hyprland-monitor-apply` (git records
0644; known trap). `4a30a6ba..c5ff25ce` is documentation-only (verified with
`git diff --stat`: handoff + transcript), so running the live-session harness
with `OMARCHY_PATH=/home/jesse/omarchy7ultimate` exercises identical code.
Live session: Hyprland HIS `efb5099…`, HDMI-A-1, `/dev/uinput`
`root:input 660` via the newly shipped udev rule.

## Evidence

### PASS

- `test/shell.d/monitor-apply-test.sh`: **exit 0**, all cases green
  (TV ranking from sysfs names, 4K60, 1440p144, EDID 4K30 DTD + sysfs 1080p,
  disconnected DP disabled).
- Harness numbered rows 20–24: pin, unpin, minimize three, restore one,
  snap two, snap top-left quarter — **green both runs**.
- IPC `omarchy-shell window ping` green throughout.
- Live monitors: HDMI-A-1 desktop timing via emit-lua ranker, no pin.

### FAIL — blocking

1. **Row 25 Alt+Tab failed twice out of two runs.**
   - Run 1: `timed out after 10s waiting for: layer_absent omarchy-task-switcher`.
   - Run 2: overlay closed, but
     `commitCycle kept focus on null … after=null` → `not ok - use Alt+Tab`.
   - Run 3 (independent): `hyprbars-pointer-proof.py` — *"Alt+Tab card click
     left omarchy-task-switcher mapped; MouseArea did not pick."*
   Lead: `commitCycle` leaves focus null after commit (`d595d46f`'s area).
   This is inside W0's own scope and blocks the gate by itself.
2. **All three pointer proofs exited 1**: hyprbars (switcher defect above),
   csd-caption ("timed out waiting for csd maximize click maximizes: None"),
   start-dismiss (`grim` itself timed out after 20s mid-proof).

### FAIL — `./test/shell`: exit 1, 7 of 198 files, classified

| Test | Classification |
|------|----------------|
| `config-test.sh`, `snapper-test.sh`, `unowned-system-paths-test.sh` | env gap: no `omarchy-pkgs` checkout |
| `keybindings-menu-test.sh` | known pre-existing (alternative chord row join) |
| `branding-about-animation-test.sh` | known pre-existing family (About logo animation) |
| `omarchy-superbar-icons-test.sh` | **stale test vs intended change**: greps `20 * devicePixelRatio`, but `a69721d4` intentionally moved icons to 32px (documented W0 gate item). Test fixed in follow-up commit, post-cert. |
| `ssh-reconnect-test.sh` | flaky mock harness: fake ssh generates `sleep: invalid time interval` / non-numeric `exit` |

No windowing regressions found in `./test/shell`; the regressions are above.

### Human eyes

Not witnessed. Jesse must confirm the glass matches grim
(`C:\dev\omarchy-vm\desktop-rescue\w0-cert-final.png`) before any future GO.

### Adversarial source review

Deferred → even if tests were green, verdict would cap at "provisionally
accepted" per `W0_GATE.md` item 9.

## Shipped in the same tranche (post-cert commits)

- `etc/udev/rules.d/70-omarchy-uinput.rules` + install wiring in
  `install/post-install/udev.sh`; applied live on metal. Reboot no longer
  reverts uinput to 0600. No more surprise chmods.
- `W0_GATE.md`: single locked definition + evidence checklist for W0.
- Doc drift fixes: Start click-through status reconciled in
  `WINDOWS_NATIVE_ACCEPTANCE.md` and `WINDOWS_7_ULTIMATE_PARITY.md`.
- Stale superbar-icons expectation fixed (20 → 32 px).

## Next work order (supersedes HANDOFF_W0_METAL §In flight)

1. Fix Alt+Tab `commitCycle` null-focus (root cause first: read the switcher
   pick path; do not paper over with a sleep). Add a regression case that
   asserts focused address after commit, not just layer absence.
2. Re-run pointer proofs until green without session babysitting.
3. Investigate the csd maximize timeout and the grim 20s hang separately.
4. Only then re-run this certification from a fresh clean checkout.
5. Phase 3 token unification / branding stays **blocked** behind a green W0
   cert — the lock does not allow building past a red gate.

## Do not

Everything in `HANDOFF_W0_METAL_2026-08-23.md` §Open defects / known traps
still applies. Additionally: do not cite the afternoon `exit 0` run as
evidence of anything; it does not reproduce.

## Resume

`resume: read HANDOFF_W0_CERT_2026-08-23.md §Next work order` — start at 1.
