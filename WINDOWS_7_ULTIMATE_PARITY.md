# Windows 7 Ultimate Parity Matrix

Job completeness for Project Ultimate. Names are **jobs**, not a requirement to copy Microsoft's labels. A Windows 7 Ultimate user should recognize every row and complete it with a mouse.

This matrix is the product bar. `WINDOWS_NATIVE_ACCEPTANCE.md` is the forty-task smoke test (necessary, not sufficient; six numbered rows automated, plus unnumbered harness proofs). `AGENT_NATIVE_ACCEPTANCE.md` is the same jobs on the agent path.

Status (tree audited at `1334ba30` on 2026-08-26; metal windowing baseline HDMI-A-1 1920×1080):

- `missing` — no product surface for the job
- `plumbing` — Omarchy/system machinery exists; no consumer mouse path that hides the plumbing
- `prototype` — a Desktop Mode surface exists and is not job-complete
- `present` — a usable mouse path exists for the job (still not a quality claim)

Do not mark a row `present` because a panel or hotkey exists for power users.

When extending a row, cite the matching ground-truth file from `plans/win7-ultimate-ground-truth/` (see `_PARITY_ACCEPTANCE_POINTERS.md`). Pointers do not change status.

## Desktop jobs

| Job | Status | Tree notes |
|-----|--------|------------|
| Desktop (icons, wallpaper, context menu, Recycle) | prototype | Wallpaper picker (`omarchy.image-picker`) exists. Desktop Mode shows real XDG Desktop icons (`omarchy.desktop-icons`, `desktopIcons` true). Computer.desktop launches Files This PC through the same `uwsm-app` / `OMARCHY_PATH` path as Start Computer. Files Recycle Bin place keeps the `entry.trash` write plane reachable through `files.provider` (catalog id `files.entry.trash`, availability.claim=partial; consent high-risk / consequential; SHELL refused at `grant.shell-consequential`; Delete/Trash control gated `trashAuthorized=false`). `files.trash.restore` write plane is reachable (`files-trash-restore` helper; real preflight reads `.trashinfo` and derives the destination; risk consequential; SHELL refused at `grant.shell-consequential`; catalog claim=partial; humanRoute planned empty). Restore UI stays honest-unavailable. `files.trash.manage` write plane is reachable (`files-trash-manage` helper; claim=partial; humanRoute planned empty; `emptyBinAuthorized=false`). Empty Bin LIVE stays honest-unavailable. Desktop Recycle icon, Recycle Properties, and a full desktop context menu remain unavailable and are not product-complete. Recycle Bin / Empty Bin LIVE residual OPEN after PR #63. Recycle Bin is not product-complete. Claim stays prototype; this row is not present. |
| Superbar (taskbar) | prototype | `omarchy.ultimate-taskbar`: Start orb, stacked-window Task View mark, running groups, Show Desktop. Notification cluster from `barConfig` + `BarWidgetRegistry` (Desktop Mode overlay includes Quick Settings, Notification Center, agents, the existing `omarchy.system-update` and `omarchy.keyboard-layout` widgets, tray, and a two-line clock; does not rewrite `shell.json`). Those two widgets hide when idle (no pending update; a single keyboard layout). Peek captures a live grim thumbnail of each mapped window on the active desktop and keeps title-only rows for minimized, off-desktop, occluded, or uncapturable clients so grim does not steal another window's pixels. The peek stays held while the pointer is on the task button or the peek card. Lock preview and session lock hide Superbar peeks, jump menus, Start, and Task View so those PopupWindows do not stack above the lock overlay. Superbar running groups include every non-special window, not only the active desktop. Unpinned `org.omarchy.terminal` uses the foot icon; reverse-DNS compositor ids do not themed-fallback onto the Settings gear. Right-click jump lists include desktop Actions plus pin/unpin and "Close group" / "Close window"; the Settings desktop entry (not a shipped pin) publishes Display, Sound, Network, Bluetooth, Power, Personalization, Apps, Input, Update, Recovery, and the honest missing Accessibility and System information pages; the Agent Center pin publishes Tasks, Approvals, Automations, Activity, History, Context, Usage, Permissions, Providers, Artifacts, and Troubleshooting. Group close still kills the entire group (`TaskButton.qml`). Peek × closes one. Superbar glass and hyprbars caption chrome read `Tokens.caption` / `Tokens.chrome` through the generated `chrome-tokens-v0.json` adapter. Multi-monitor policy: primary output owns the notification cluster; pins on every bar; secondary bars show running groups that own a window there; Start/Task View open on the clicked bar. |
| Start | prototype | `omarchy.ultimate-start` is a two-pane 720×640 Start: search, pinned, Recent from real launches, and letter-grouped All programs on the left; account plus iconed Files, Pictures, Computer, Settings, Agent Center, and power on the right. Idle list hides Terminal/Vim. Right-click reuses Superbar jump lists and pin/unpin; Files exposes Home / This PC / Desktop / Documents / Downloads / Pictures / Recent / Trash / Search once the product launcher is published. Start search still opens Files itself rather than inventing a second Home destination. In-app Files Search stays off Start search. Pictures uses that action instead of `xdg-open`. The power flyout (Lock / Restart / Log off / Shut down) is an in-card menu so click-through cannot steal the caret. Outside-click and orb re-toggle: `e3bf9385`. Click-through (compositor delivers the outside click; no full-screen swallow): `6a548ba9` + `66805cdc`, proved by `start-dismiss-proof.py`. |
| Search | prototype | Start `SearchBox` filters installed apps plus existing Settings pages, Agent Center sections, and Start places (Display, Sound, Network, Bluetooth, Power, Personalization, Apps, Input, Update, Recovery, Files, Pictures, Computer/This PC, Desktop, Documents, Downloads, Recent, Agent Center Tasks/Approvals/Automations/Activity/History/Context/Usage/Permissions/Providers/Artifacts/Troubleshooting, Settings). Those hits reuse the published Settings/Files/Agent Center desktop Actions. Not file-content search. Accessibility and System information are not invented as toggle destinations. Agent Center hits open the existing inspect pages; they do not invent consent or provider writers. The Settings jump list publishes Settings home; Start search still opens Settings itself. The Files jump list publishes Home; Start search still opens Files itself. Home, Network, Trash, Music, Videos, and in-app Files Search stay off Start search (default Home launch; Sharing/Recycle/MIME are not invented). Files Home stays degraded. |
| Explorer / Computer | prototype | `org.omarchy.Files` is a snappable Windows 7 Explorer over `files.provider`: circular back and forward driven by real history, a recent-locations chevron dropdown over the same history, a breadcrumb address bar, a search box, a command bar whose entries follow the route and selection, a navigation pane with Favorites, Libraries, the account name, Computer, Network and Recycle Bin, Details / Large Icons / List views with sortable Name, Date modified, Type and Size columns, a details pane, a right-click menu, and a General-tab Properties dialog. Computer renders Hard Disk Drives, Devices with Removable Storage and Network Location with capacity bars from `statvfs`. Folder icons, drive icons and file-type emblems are drawn, not themed. Browse routes take a bounded relative path so folders open in place. Start Computer uses the published Computer desktop action; Start Pictures uses the Pictures action. Pictures browse reports the location's own availability; an absent optional Recycle Bin does not paint Pictures DEGRADED. Computer reports the virtual this-pc location instead of inheriting workspace inspect degradation. Byte-bound eviction keeps a floor of Home/Desktop/Documents/Downloads/Pictures records. The information bar is reserved for faults; inventory truncation is reported in the status bar. Desktop Mode `SUPER + E` launches product Files (`default/hypr/bindings/desktop.lua`); Power User `SUPER + SHIFT + F` still opens Nautilus. File contents are never read, so there are no thumbnails. New folder applies through `files.provider` `directory.create` (catalog id `files.directory.create`). The `entry.trash` write plane is reachable through `files.provider` (catalog id `files.entry.trash`, availability.claim=partial; consent high-risk / consequential; SHELL refused at `grant.shell-consequential`; Delete/Trash control gated `trashAuthorized=false`). `files.trash.restore` write plane is reachable (`files-trash-restore` helper; claim=partial; humanRoute planned empty). Restore UI stays honest-unavailable. Open applies through `files.provider` `entry.open` (catalog id `files.entry.open`, availability.claim=partial; consent implicit; risk low; SHELL-grantable). Rename applies through `files.provider` `entry.rename` (catalog id `files.entry.rename`, availability.claim=partial; consent implicit; risk low; SHELL-grantable; same-directory; `renameAuthorized=true`). Copy and Paste apply through `files.provider` `entry.copy` (catalog id `files.entry.copy`, availability.claim=partial; consent implicit; risk low; SHELL-grantable; regular files and directories; in-app staging; `copyAuthorized=true`). The `files.entry.move` write plane is reachable (`files-entry-move` helper; claim=partial; risk consequential; SHELL refused at `grant.shell-consequential`; humanRoute planned empty; `cutAuthorized=false`). Cut UI stays honest-unavailable. The `files.entry.delete` write plane is reachable (`files-entry-delete` helper; claim=partial; risk consequential; SHELL refused at `grant.shell-consequential`; humanRoute planned empty; `deleteAuthorized=false`). Permanent Delete UI stays honest-unavailable. File contents are never read for thumbnails. `files.trash.manage` write plane is reachable (`files-trash-manage` helper; claim=partial; humanRoute planned empty; `emptyBinAuthorized=false`). Empty Bin LIVE, OS clipboard, and column resizing remain unavailable. OS clipboard residual OPEN after PR #60. OS clipboard residual OPEN after PR #64. Copy/Paste stay in-app only. Folder copy CLOSED. Recycle Bin / Empty Bin LIVE residual OPEN after PR #63. The permanent delete write plane exists but is not shell-authorizable. Recycle Bin is not product-complete. Claim stays prototype; this row is not present. |
| Network | prototype | Settings → Network reads Fabric `network.inspect` (Wi-Fi radio, interfaces, connection status). Wi-Fi radio applies through `network.provider` `wifi.set-enabled` (catalog id `network.manage`, availability.claim=partial). Joining a network and per-connection changes remain unavailable. `network.wifi.connect` stays the Superbar leftover; `windows-native.2` Connect Wi-Fi stays pending. Superbar still hosts the heritage `omarchy.network` panel. |
| Personalization | prototype | Settings → Personalization hosts the existing image picker for theme packs and wallpapers. Typed personalization.provider writers for density, cursor, motion, and a full theme service remain unavailable. Superbar glass, Superbar tooltips, Start, and hyprbars captions read the same resolved `Tokens.chrome` / `Tokens.caption` payload; `omarchy-theme-set` republishes `chrome-tokens-v0.json` for dark and light packs. Claim stays prototype; this row is not present. |
| Devices & Printers | missing / prototype | Settings → Bluetooth reads Fabric `bluetooth.inspect` (controller power, paired/connected devices). Pair, connect, and discover writers remain unavailable. Superbar still hosts the heritage `omarchy.bluetooth` panel. Administration > Printers and scanners is the visible host for bounded `printer.inspect` inventory; inspect stays honest-unavailable as Devices and Printers product. Printer add and Devices and Printers mutation remain unavailable. Forty-task printer add stays pending. Claim stays missing / prototype; this row is not present. |
| Device Manager | missing | Fabric `device.inspect` exists. Administration > Device Manager is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Device Manager product. It does not invent a Settings Device Manager page. Claim stays missing as product; this row is not present. |
| Programs and Features | missing | No Software Center; no Programs and Features. |
| Default Programs | prototype | Settings → Apps reads Fabric `defaults.inspect`, applies the default browser (http and https, scheme by scheme through the single-flight plane, skipping schemes already at the choice) and the default email application (mailto) through `defaults.provider` `protocol.set` (catalog id `defaults.protocol.set`), and applies per-MIME defaults through `defaults.provider` `mime.set` (catalog id `defaults.mime.set`, risk low; writable associations with more than one installed candidate; already-default and non-candidate refuse before preflight). `association.inspect` is published so durable apply can re-read scoped association state. Startup applications are readable through `defaults.inspect`; Settings cannot enable, disable, or remove them. Protocol defaults beyond browser and email, and the Win7 Default Programs four-link applet, remain unavailable. `files.associations.set` stays a planned MIME symbol. Claim stays prototype; this row is not present. Forty-task "Change the default browser" stays pending. |
| User Accounts | missing | Fabric `account.inspect` exists. Administration > User accounts is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as User Accounts product. It does not invent a Settings or Start Accounts page. Claim stays missing as product; this row is not present. |
| Credential Manager | missing | |
| Power Options | prototype | Settings → Power reads Fabric `power.inspect` (AC/battery source, active profile, available profiles, battery). Profile mutation stays unavailable: `profile.set` write plane exists, but Settings does not offer LIVE control because polkit cannot authorize `org.freedesktop.UPower.PowerProfiles.switch-profile` from the fabric daemon under `app.slice`. Sleep, lock, and lid changes remain unavailable. Not full Power Options. `windows-native.35` stays pending. Superbar still hosts the heritage `omarchy.power` panel (Process/`omarchy-powerprofiles-set`, Hyprland `session.slice` child, not `session-N.scope`). That leftover was unverified on metal after PR #31. QS Power METAL_HEAD OPEN after metal FAIL on tip `20484de6` (pkcheck Not authorized / session-5103; !batteryPresent; amd_pstate EINVAL). KEEP OPEN. Settings Power LIVE refused. Do not claim the heritage QS Power panel works on this metal. Do not invent Settings Power LIVE. |
| Sound | prototype | Settings → Sound reads Fabric `audio.inspect` (sinks, default, mute, channel volume). Output volume applies through `audio.provider` `output-volume.set` (catalog id `audio.volume.set`, availability.claim=partial). Mute, routing, and port changes remain unavailable. `windows-native.6` stays pending. Superbar still hosts the heritage `omarchy.audio` panel. |
| Display | prototype | Settings → Display reads Fabric `display.inspect` (connector, mode, scale, position). Brightness applies through `display.provider` `brightness.set` on outputs with a controllable backlight (catalog id `display.brightness.set`, availability.claim=partial). Resolution, scale, and arrangement changes remain unavailable. `display.configure` stays the Superbar leftover; `windows-native.3` scaling stays pending. The Superbar / Quick Settings Night light tile is a visible leftover (`display.night-light.set` / `legacy-direct` / `NightlightService`); Settings does not invent night-light LIVE. `windows-native.34` stays pending. |
| Firewall | plumbing | `install/config/firewall.sh` / ufw on by default (`manual/35-networking.md`). Administration > Firewall is the visible host for bounded `firewall.inspect` inventory; inspect stays honest-unavailable as Firewall Settings product. It does not invent a Firewall Settings LIVE surface or `firewall.manage` UI. Claim stays plumbing; this row is not present. |
| Update | plumbing / prototype | Settings → Update reads Fabric `update.inspect` (available count, phase, checkpoint, reboot). Heritage `omarchy.system-update` widget loads in the Superbar cluster and shows only when `omarchy-update-available` reports pending updates. Download, apply, checkpoint, restart, and reboot writers remain unavailable. Forty-task rows 28–29 pending. |
| Backup & Restore | plumbing | Snapper via `omarchy-snapshot`. Administration > Backup is the visible host for bounded `backup.inspect` inventory; inspect stays honest-unavailable as Backup and Restore product. Writers (`backup.manage`) stay planned/unavailable. It does not invent a Backup and Restore product surface. Claim stays plumbing; this row is not present. |
| System Restore | plumbing | Settings → Recovery reads Fabric `recovery.inspect` (restore-point inventory). Same snapshot/rollback path. Restore planning and execution writers remain unavailable. Forty-task rows 30–31 pending. |
| Disk Management | missing | Fabric `storage.inspect` exists. Administration > Storage is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Disk Management product. Format, mount, and eject remain unavailable. Claim stays missing as product; this row is not present. |
| Task Manager | missing as product | `btop` is the Activity TUI (`manual/21-tuis.md`). Not Task Manager. Leftover `processes.inspect` (`processes.provider` / `legacy-direct` / `claim=partial` / Task Manager surface / btop source) is closed. Honest reader is `process.inspect` on `process.provider` (provider.state=present, availability.claim=missing). Administration > Processes is the visible host for that bounded inspect inventory and for the End Task control path; inspect stays honest-unavailable as Task Manager product. Catalog writer `process.termination.plan` on `process.provider` is honest-unavailable (availability.claim=missing, consent high-risk / consequential; `terminationAuthorized=false`; End Task stays hidden). The write plane exists, the shell principal cannot authorize, and the UI stays unauthorized. It does not invent Superbar or Administration Task Manager or End Task LIVE CONTROL. Claim stays missing as product; this row is not present. |
| Resource Monitor | missing | The leftover catalog keeps `resources.inspect` missing with an empty path; it does not invent Task Manager > Performance > Resource Monitor. |
| Services | missing | Fabric `service.inspect` exists. Administration > Services is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Services product. Start, stop, enable, and disable remain unavailable. Claim stays missing as product; this row is not present. |
| Task Scheduler | missing | Fabric `schedule.inspect` exists. Administration > Scheduled tasks is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Task Scheduler product. Creating and disabling a task remain unavailable. Claim stays missing as product; this row is not present. |
| Event / history | prototype (toasts + window ledger) | Notification history directory + `showHistory` replay as toasts. Window capability calls append `capability-ledger.json`. Not Event Viewer. |
| Remote desktop | missing as product | |
| Drive encryption | missing as product | |
| Sharing | missing as product | Forty-task SMB row pending. |
| Language / locale | prototype | Heritage `omarchy.keyboard-layout` loads in the Superbar cluster and shows only when a keyboard has more than one layout. Settings Input applies the active layout through `input.provider` `keyboard-layout.set` on those keyboards (catalog id `input.keyboard-layout.set`, availability.claim=partial). Pointer, repeat rate, and locale/region changes remain unavailable. Forty-task "Change keyboard layout" stays pending. |
| Accessibility | missing as product | Settings jump opens the honest missing Fabric page (`settings.accessibility.overview`); no `accessibility.provider`. Start search does not invent it as a destination. Reduced motion and high contrast live in tokens. Ease of Access engine remains unavailable. |
| File associations | missing as product | Settings → Apps reads Fabric `defaults.inspect` MIME inventory and applies per-MIME defaults through `defaults.provider` `mime.set` (catalog id `defaults.mime.set`; risk low; writable associations with more than one installed candidate). `association.inspect` is published so durable apply can re-read scoped association state. `files.associations.set` stays missing/planned MIME. Claim stays missing as product; this row is not present. |
| Properties | missing as product | |
| Context menus | prototype | Superbar jump lists, Start pin/jump menus, and peek menus exist. Not "right-click everywhere" as the Windows API. Delete/Trash control gated off (`trashAuthorized=false`); write plane reachable but not shell-authorizable. Restore stays honest-unavailable. Desktop Recycle and a full desktop context menu remain unavailable and are not product-complete. |

## 2026 layers

| Job | Status | Tree notes |
|-----|--------|------------|
| Software Center | missing | Missing product; plan Phase 7 taxonomy. Pacman/AUR/Flatpak are CLI/menu today. No writer is waiting on a phase gate. |
| Compatibility Center | missing | Missing product; plan Phase 8 taxonomy. No "install one known-compatible .exe" path. No writer is waiting on a phase gate. |
| Snapshot recovery | plumbing | `omarchy-snapshot`, Limine snapper restore. Consumer "restore point" UI missing. |
| Modern display (scaling, HDR, night light) | prototype / plumbing | Monitor panel + `omarchy.nightlight` service. The QS Night light tile is a visible leftover; forty-task night light stays pending; scaling/HDR remain separate. Do not claim present. |
| Proton / gaming | missing | Steam install is forty-task pending; no Gaming / Proton surface. |
| Privacy | missing | Doctrine refuses telemetry; no Privacy Settings. |
| Agent Fabric | prototype (window) | See `AGENT_NATIVE_ACCEPTANCE.md`. WindowService + `CapabilityBroker` (results, permit, ledger, window undo). Settings Display, Sound, Network, Bluetooth, Power, Apps, Update, and Recovery read `display.inspect` / `audio.inspect` / `network.inspect` / `bluetooth.inspect` / `power.inspect` / `defaults.inspect` / `update.inspect` / `recovery.inspect`. Superbar still hosts audio/network/bluetooth/power panels. Live Settings writers are Sound volume, Network Wi-Fi radio, Display brightness, Input layout, and Apps default browser; Power profile stays inspect-only because polkit cannot authorize the fabric daemon under app.slice; the Superbar Power leftover was unverified on metal after PR #31; QS Power METAL_HEAD OPEN after metal FAIL on tip `20484de6` (pkcheck Not authorized / session-5103; !batteryPresent; amd_pstate EINVAL); KEEP OPEN; Settings Power LIVE refused; remaining Settings verbs stay unavailable. Jobs stay prototype/pending. |
| Agent Center | prototype | `org.omarchy.AgentCenter` is a snappable Superbar pin and Start destination. The pin jump list publishes Overview plus every existing inspect page: Tasks, Approvals, Automations, Activity, History, Context, Usage, Permissions, Providers, Artifacts, and Troubleshooting. Start search still opens Agent Center itself rather than inventing a second Overview destination. Overview reads owner-scoped managed-work counts from Fabric. Inspect tasks can be created, run, cancelled, and recovered for `system.info.read`. Context capture covers the five desktop sources. Consent and provider operations stay outside Agent Center. Usage stays one section. `omarchy.agents` remains a launch shim. |

## Caption / windowing (not a substitute for the jobs above)

Windowing Gate W0 is Phase 1 of `plans/project-ultimate.md` and is certified only by `W0_GATE.md` (Hyprland stays; three-button SSD, one-row CSD, min/max/restore, LTRB snap, Alt+Tab, Show Desktop, reopen memory, click-to-raise, Start click-through). The OS remains rejected because the product jobs above are incomplete. Founding execution authority is `PRODUCT_DOCTRINE.md` and `plans/project-ultimate.md`.

## Win7 ground-truth pointers (honesty only — statuses unchanged)

Concrete acceptance criteria for each job live under the **FULL CORPUS** in `plans/win7-ultimate-ground-truth/` (`01`–`06` md+json + `fleet/`) and are indexed by `plans/WIN7_SPEC_INDEX.md`. Full pointer table: `plans/win7-ultimate-ground-truth/_PARITY_ACCEPTANCE_POINTERS.md`.

**Caption BINDING LOCK:** restored visual top NC **30** px; `SM_CYCAPTION` **22** is metric band only (never ship a 22 px title bar). See `plans/win7-ultimate-ground-truth/01-window-chrome.md`.

Do **not** read these pointers as a status upgrade. Product remains **REJECTED**.
