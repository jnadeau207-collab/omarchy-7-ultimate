# Windows 7 Ultimate Parity Matrix

Job completeness for Project Ultimate. Names are **jobs**, not a requirement to copy Microsoft's labels. A Windows 7 Ultimate user should recognize every row and complete it with a mouse.

This matrix is the product bar. `WINDOWS_NATIVE_ACCEPTANCE.md` is the forty-task smoke test (necessary, not sufficient; six numbered rows automated, plus unnumbered harness proofs). `AGENT_NATIVE_ACCEPTANCE.md` is the same jobs on the agent path.

Status (tree audited at `1334ba30` on 2026-08-26; metal windowing baseline HDMI-A-1 1920×1080):

- `missing` — no product surface for the job
- `plumbing` — Omarchy/system machinery exists; no consumer mouse path that hides the plumbing
- `prototype` — a Desktop Mode surface exists and is not job-complete
- `present` — a usable mouse path exists for the job (still not a quality claim)

Do not mark a row `present` because a panel or hotkey exists for power users.

When extending a row, cite the matching ground-truth file from `plans/win7-ultimate-ground-truth/` (see `_PARITY_ACCEPTANCE_POINTERS.md`). Pointers do not change status.

## Desktop jobs

| Job | Status | Tree notes |
|-----|--------|------------|
| Desktop (icons, wallpaper, context menu, Recycle) | prototype | Wallpaper picker (`omarchy.image-picker`) exists. Desktop Mode shows real XDG Desktop icons (`omarchy.desktop-icons`, `desktopIcons` true). Computer.desktop launches Files This PC through the same `uwsm-app` / `OMARCHY_PATH` path as Start Computer. Files Recycle Bin place keeps the `entry.trash` write plane reachable through `files.provider` (catalog id `files.entry.trash`, availability.claim=partial; consent high-risk / consequential; SHELL refused at `grant.shell-consequential`; Delete/Trash control gated `trashAuthorized=false`). `files.trash.restore` write plane is reachable (`files-trash-restore` helper; real preflight reads `.trashinfo` and derives the destination; risk consequential; SHELL refused at `grant.shell-consequential`; catalog claim=partial; dated leftover humanRoute planned empty; session humanRoute visible `Files > Restore`). Fabric Restore UI stays honest-unavailable under SHELL. Session Restore and Empty Recycle Bin run through this session's trash helper (visible `Files > Restore` / `Files > Empty Recycle Bin`; claim=partial). `files.trash.manage` write plane is reachable (`files-trash-manage` helper; claim=partial; dated leftover humanRoute planned empty; session humanRoute visible `Files > Empty Recycle Bin`; `emptyBinAuthorized=false`). Fabric Empty Bin LIVE stays honest-unavailable under SHELL. Recycle Bin.desktop ships on the XDG Desktop and launches `omarchy-launch-files --source desktop files.trash` through the same uwsm argv path as Computer. Recycle Properties and a full desktop context menu remain unavailable and are not product-complete. Recycle Bin / Empty Bin LIVE residual OPEN after PR #63. Recycle Bin is not product-complete. Claim stays prototype; this row is not present. |
| Superbar (taskbar) | prototype | `omarchy.ultimate-taskbar`: Start orb, stacked-window Task View mark, running groups, Show Desktop. Notification cluster from `barConfig` + `BarWidgetRegistry` (Desktop Mode overlay includes Quick Settings, Notification Center, agents, the existing `omarchy.system-update` and `omarchy.keyboard-layout` widgets, tray, and a two-line clock; does not rewrite `shell.json`). Those two widgets hide when idle (no pending update; a single keyboard layout). Peek captures a live grim thumbnail of each mapped window on the active desktop and keeps title-only rows for minimized, off-desktop, occluded, or uncapturable clients so grim does not steal another window's pixels. The peek stays held while the pointer is on the task button or the peek card. Lock preview and session lock hide Superbar peeks, jump menus, Start, and Task View so those PopupWindows do not stack above the lock overlay. Superbar running groups include every non-special window, not only the active desktop. Unpinned `org.omarchy.terminal` uses the foot icon; reverse-DNS compositor ids do not themed-fallback onto the Settings gear. Right-click jump lists include desktop Actions plus pin/unpin and "Close group" / "Close window"; the Settings desktop entry (not a shipped pin) publishes Display, Sound, Network, Bluetooth, Power, Personalization, Apps, Input, Update, Recovery, the honest missing Accessibility page, and System information; the Agent Center pin publishes Tasks, Approvals, Automations, Activity, History, Context, Usage, Permissions, Providers, Artifacts, and Troubleshooting. Group close still kills the entire group (`TaskButton.qml`). Peek × closes one. Superbar glass and hyprbars caption chrome read `Tokens.caption` / `Tokens.chrome` through the generated `chrome-tokens-v0.json` adapter. Multi-monitor policy: primary output owns the notification cluster; pins on every bar; secondary bars show running groups that own a window there; Start/Task View open on the clicked bar. |
| Start | prototype | `omarchy.ultimate-start` is a two-pane 720×640 Start: search, pinned, Recent from real launches, and letter-grouped All programs on the left; account plus iconed Files, Pictures, Computer, Settings, Agent Center, and power on the right. Idle list hides Terminal/Vim. Right-click reuses Superbar jump lists and pin/unpin; Files exposes Home / This PC / Desktop / Documents / Downloads / Pictures / Recent / Trash / Search once the product launcher is published. Start search still opens Files itself rather than inventing a second Home destination. In-app Files Search stays off Start search. Pictures uses that action instead of `xdg-open`. The power flyout (Lock / Restart / Log off / Shut down) is an in-card menu so click-through cannot steal the caret. Shut down runs through tip-true `Start.shutdown` → `omarchy-system-shutdown`. soft leftover-attaches `windows-native.40` to that tip-true Start > Shut down plane. Catalog `power.shutdown` stays leftover `legacy-direct` / claim=partial with visible `Start > Shut down` (human=partial; never claim=present). session leftover recorded: Shut down leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.40. Forty-task "Shut down" stays pending. `windows-native.40` stays prototype/pending. Outside-click and orb re-toggle: `e3bf9385`. Click-through (compositor delivers the outside click; no full-screen swallow): `6a548ba9` + `66805cdc`, proved by `start-dismiss-proof.py`. Claim stays prototype; this row is not present. |
| Search | prototype | Start `SearchBox` filters installed apps plus existing Settings pages, Agent Center sections, and Start places (Display, Sound, Network, Bluetooth, Power, Personalization, Apps, Input, Update, Recovery, System information, Files, Pictures, Computer/This PC, Desktop, Documents, Downloads, Recent, Agent Center Tasks/Approvals/Automations/Activity/History/Context/Usage/Permissions/Providers/Artifacts/Troubleshooting, Settings). Those hits reuse the published Settings/Files/Agent Center desktop Actions. Not file-content search. Accessibility is not invented as a toggle destination. System information opens the session System information page. Agent Center hits open the existing inspect pages; they do not invent consent or provider writers. The Settings jump list publishes Settings home; Start search still opens Settings itself. The Files jump list publishes Home; Start search still opens Files itself. Home, Network, Trash, Music, Videos, and in-app Files Search stay off Start search (default Home launch; Sharing/Recycle/MIME are not invented). Files Home stays degraded. |
| Explorer / Computer | prototype | `org.omarchy.Files` is a snappable Windows 7 Explorer over `files.provider`: circular back and forward driven by real history, a recent-locations chevron dropdown over the same history, a breadcrumb address bar, a search box, a command bar whose entries follow the route and selection, a navigation pane with Favorites, Libraries, the account name, Computer, Network and Recycle Bin, Details / Large Icons / List views with sortable Name, Date modified, Type and Size columns, a details pane, a right-click menu, and a General-tab Properties dialog. Computer renders Hard Disk Drives, Devices with Removable Storage and Network Location with capacity bars from `statvfs`. Folder icons, drive icons and file-type emblems are drawn, not themed. Browse routes take a bounded relative path so folders open in place. Start Computer uses the published Computer desktop action; Start Pictures uses the Pictures action. Pictures browse reports the location's own availability; an absent optional Recycle Bin does not paint Pictures DEGRADED. Computer reports the virtual this-pc location instead of inheriting workspace inspect degradation. Byte-bound eviction keeps a floor of Home/Desktop/Documents/Downloads/Pictures records. The information bar is reserved for faults; inventory truncation is reported in the status bar. Desktop Mode `SUPER + E` launches product Files (`default/hypr/bindings/desktop.lua`); Power User `SUPER + SHIFT + F` still opens Nautilus. File contents are never read, so there are no thumbnails. New folder applies through `files.provider` `directory.create` (catalog id `files.directory.create`). The `entry.trash` write plane is reachable through `files.provider` (catalog id `files.entry.trash`, availability.claim=partial; consent high-risk / consequential; SHELL refused at `grant.shell-consequential`; Delete/Trash control gated `trashAuthorized=false`). `files.trash.restore` write plane is reachable (`files-trash-restore` helper; claim=partial; dated leftover humanRoute planned empty; session humanRoute visible `Files > Restore`). Fabric Restore UI stays honest-unavailable under SHELL. Session Restore and Empty Recycle Bin run through this session's trash helper. Open applies through `files.provider` `entry.open` (catalog id `files.entry.open`, availability.claim=partial; consent implicit; risk low; SHELL-grantable). Rename applies through `files.provider` `entry.rename` (catalog id `files.entry.rename`, availability.claim=partial; consent implicit; risk low; SHELL-grantable; same-directory; `renameAuthorized=true`). Copy and Paste apply through `files.provider` `entry.copy` (catalog id `files.entry.copy`, availability.claim=partial; consent implicit; risk low; SHELL-grantable; regular files and directories; in-app staging; `copyAuthorized=true`). The `files.entry.move` write plane is reachable (`files-entry-move` helper; claim=partial; risk consequential; SHELL refused at `grant.shell-consequential`; humanRoute visible `Files > Cut`; `cutAuthorized=false`). Session Cut and Paste-after-cut run through this session's move helper. Fabric Cut LIVE stays honest-unavailable under SHELL. The `files.entry.delete` write plane is reachable (`files-entry-delete` helper; claim=partial; risk consequential; SHELL refused at `grant.shell-consequential`; humanRoute visible `Files > Permanently delete`; `deleteAuthorized=false`). Session Permanent Delete runs through this session's delete helper after confirm. Fabric Delete LIVE stays honest-unavailable under SHELL. Session Compress zips selected regular files and folders through this session's `files-archive-create` helper (visible `Files > Compress`; catalog `files.archive.create` leftover-direct / claim=partial). Session Extract unzips a selected zip through this session's `files-archive-extract` helper into a non-colliding folder named after the archive (visible `Files > Extract`; catalog `files.archive.extract` leftover-direct / claim=partial). Session Properties reads name, type, size for regular files, modified time, and location path through this session's `files-entry-properties` helper (visible `Files > right-click > Properties`; catalog `files.properties.read` leftover-direct / claim=partial; READ-ONLY). Session Eject unmounts then ejects a selected removable USB volume or optical drive through this session's `storage-removable-eject` helper (visible `Files > Devices > Eject`; catalog `storage.removable.eject` leftover-direct / claim=partial; `device.busy` refuse). Session Mount mounts a selected unmounted removable USB volume or optical drive through this session's `storage-removable-mount` helper (visible `Files > Devices > Mount`; catalog `storage.removable.mount` leftover-direct / claim=partial; `device.busy` refuse). session leftover recorded: session Connect mounts a guest or public SMB share through this session's `sharing-smb-connect` helper (visible `Files > Connect to Server`; catalog `sharing.smb.connect` leftover-direct / claim=partial; `share.auth-required` refuse; no password vault or keyring UI). session leftover recorded: Open a PDF reuses tip-true `files.provider` `entry.open` / `files-entry-open` → `/usr/bin/xdg-open` from Files Open / double-click with the default/associated graphical viewer (visible `Files > PDF`; catalog `files.document.open` leftover-direct / claim=partial soft leftover-attached to `openEntry`; shipped `application/pdf` → Evince; `windows-native.17` stays prototype/pending; Cloud mocks do not close windows-native.17; no Open With / MIME association UI invent). session leftover recorded: Edit a text file reuses tip-true `files.provider` `entry.open` / `files-entry-open` → `/usr/bin/xdg-open` from Files Open / double-click with the default/associated graphical editor (visible `Files > Text`; catalog `files.text.edit` leftover-direct / claim=partial soft leftover-attached to `openEntry`; shipped `text/plain` → org.gnome.TextEditor not Neovim-as-default; `windows-native.18` stays prototype/pending; Cloud mocks do not close windows-native.18; no Open With / MIME association UI invent). Files does not invent a Fabric SHELL LIVE archive writer. Files does not invent a Fabric SHELL LIVE Properties writer. Files does not invent a Fabric SHELL LIVE eject writer. Files does not invent a Fabric SHELL LIVE mount writer. Files does not invent a Fabric SHELL LIVE SMB writer. `windows-native.15` stays prototype/pending. `windows-native.14` stays prototype/pending. `windows-native.16` stays missing/pending. `windows-native.17` stays prototype/pending. `windows-native.18` stays prototype/pending. `parity.properties` stays missing as product. `windows-native.13` stays prototype/pending. There is no windows-native Extract row. File contents are never read for thumbnails. `files.trash.manage` write plane is reachable (`files-trash-manage` helper; claim=partial; dated leftover humanRoute planned empty; session humanRoute visible `Files > Empty Recycle Bin`; `emptyBinAuthorized=false`). Fabric Empty Bin LIVE stays honest-unavailable under SHELL. Session Empty Recycle Bin runs through this session's trash helper. Column resizing remains unavailable. OS clipboard residual OPEN after PR #60. OS clipboard residual OPEN after PR #64. Copy/Paste stay in-app only. OS clipboard residual CLOSED for Files copy-out/paste-in. Folder copy CLOSED. Recycle Bin / Empty Bin LIVE residual OPEN after PR #63. The permanent delete write plane exists but is not shell-authorizable. Recycle Bin is not product-complete. Claim stays prototype; this row is not present. |
| Network | prototype | Settings → Network reads Fabric `network.inspect` (Wi-Fi radio, interfaces, connection status). Wi-Fi radio applies through `network.provider` `wifi.set-enabled` (catalog id `network.manage`, availability.claim=partial). Joining an open or password-protected network uses this session's NetworkManager access from Settings (`network.wifi.connect`, claim=partial, provider leftover `legacy-direct`); the passphrase never enters Fabric. Enterprise, VPN, and per-connection DNS remain unavailable. `windows-native.2` Connect Wi-Fi stays pending until metal join proof. Superbar still hosts the heritage `omarchy.network` panel. |
| Personalization | prototype | Settings → Personalization hosts the existing image picker for theme packs and wallpapers. Wallpaper apply runs through tip-true `ImagePicker.applyEmbedded` → `omarchy-theme-bg-set`. soft leftover-attaches `windows-native.4` to that tip-true Settings > Personalization wallpaper plane. Catalog `desktop.wallpaper.set` stays leftover `legacy-direct` / claim=partial with visible `Settings > Personalization`. session leftover recorded: Change the wallpaper leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.4. Typed personalization.provider writers for density, cursor, motion, and a full theme service remain unavailable. Forty-task "Change the wallpaper" stays pending. `windows-native.4` stays prototype/pending. Superbar glass, Superbar tooltips, Start, and hyprbars captions read the same resolved `Tokens.chrome` / `Tokens.caption` payload; `omarchy-theme-set` republishes `chrome-tokens-v0.json` for dark and light packs. Claim stays prototype; this row is not present. |
| Devices & Printers | missing / prototype | Settings → Bluetooth reads Fabric `bluetooth.inspect` (controller power, paired/connected devices). Pairing and connecting use this session's BlueZ adapter (`bluetooth.audio.pair`, claim=partial, leftover `legacy-direct`); pairing secrets never enter Fabric. Audio routing, PIN-entry UI, and adapter rfkill remain unavailable. Superbar still hosts the heritage `omarchy.bluetooth` panel. Settings → Printers hosts this session's `omarchy-fabric-session-apply` printer inventory and queue plane (`printer-status` / `printer-default-set` / `printer-pause` / `printer-resume` / `printer-test-page` through absolute `/usr/bin/lpstat`, `/usr/bin/lpoptions`, `/usr/sbin/cupsdisable`, `/usr/sbin/cupsenable`, `/usr/bin/lp`; tip-true `printer.*` identities; credentialed URIs refused). Soft leftover-attach of catalog `printers.manage` leftover `legacy-direct` / claim=partial with visible `Settings > Printers`. session leftover recorded: Settings Printers inventory and queue session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Settings does not invent a `printers.provider` durable writer or Fabric SHELL LIVE queue mutation. Administration > Printers and scanners remains the visible host for bounded `printer.inspect` inventory; inspect stays honest-unavailable as Devices and Printers product. Network Add / driver wizard remains OPEN leftover. Printer add and Devices and Printers product mutation remain unavailable. Forty-task printer add stays pending. `windows-native.32` stays prototype/pending. `windows-native.5` stays pending until metal pair proof. Claim stays missing / prototype; this row is not present. |
| Device Manager | missing | Fabric `device.inspect` exists. Administration > Device Manager is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Device Manager product. It does not invent a Settings Device Manager page. Claim stays missing as product; this row is not present. |
| Programs and Features | missing | No Software Center; no Programs and Features. |
| Default Programs | prototype | Settings → Default Programs (`settings.apps.default-programs`) reads Fabric `defaults.inspect`, applies the default browser (http and https, scheme by scheme through the single-flight plane, skipping schemes already at the choice) and the default email application (mailto) through `defaults.provider` `protocol.set` (catalog id `defaults.protocol.set`), and applies per-MIME defaults through `defaults.provider` `mime.set` (catalog id `defaults.mime.set`, risk low; writable associations with more than one installed candidate; already-default and non-candidate refuse before preflight). `association.inspect` is published so durable apply can re-read scoped association state. Default Programs page partial LIVE; Win7 applet parity still open. AutoPlay, Set Program Access and Computer Defaults, and protocol defaults beyond browser and email remain unavailable. Startup applications enable or disable on Settings → Apps through this session's XDG autostart helper. Fabric `defaults.inspect` stays readable. Settings does not invent a Fabric `apps.startup.disable` durable writer. Settings does not invent Task Manager present. `windows-native.27` stays pending. `files.associations.set` stays a planned MIME symbol. Claim stays prototype; this row is not present.  session leftover recorded: Change the default browser soft leftover-attaches `windows-native.19` to this tip-true Settings > Default Programs browser plane via `defaults.protocol.set` / `applyDefaultBrowser` (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.19. Cloud EXIT 0 is not metal leftover CLOSED. Forty-task "Change the default browser" stays pending. |
| User Accounts | missing | Fabric `account.inspect` exists. Administration > User accounts is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as User Accounts product. It does not invent a Settings or Start Accounts page. Claim stays missing as product; this row is not present. |
| Credential Manager | missing | |
| Power Options | prototype | Settings → Power reads Fabric `power.inspect` (AC/battery source, active profile, available profiles, battery). Profile mutation stays unavailable: `profile.set` write plane exists, but Settings does not offer LIVE control because polkit cannot authorize `org.freedesktop.UPower.PowerProfiles.switch-profile` from the fabric daemon under `app.slice`. Sleep, lock, and lid changes remain unavailable. Not full Power Options. `windows-native.35` stays pending. Superbar still hosts the heritage `omarchy.power` panel (Process/`omarchy-powerprofiles-set`, Hyprland `session.slice` child, not `session-N.scope`). That leftover was unverified on metal after PR #31. QS Power METAL_HEAD OPEN after metal FAIL on tip `20484de6` (pkcheck Not authorized / session-5103; !batteryPresent; amd_pstate EINVAL). KEEP OPEN. Settings Power LIVE refused. Do not claim the heritage QS Power panel works on this metal. Do not invent Settings Power LIVE. |
| Sound | prototype | Settings → Sound reads Fabric `audio.inspect` (sinks, default, mute, channel volume). Output volume applies through `audio.provider` `output-volume.set` (catalog id `audio.volume.set`, availability.claim=partial). Output mute and default sink apply through this session's `omarchy-fabric-session-apply` `audio-output-mute-set` / `audio-output-default-set` helpers (`/usr/bin/pactl`, tip-true `audio.sink.*` identities). Soft leftover-attach of catalog `audio.output.manage` leftover `legacy-direct` / claim=partial with visible `Settings > Sound; Superbar > Quick Settings > Sound`. session leftover recorded: Settings Sound mute and default output session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Settings does not invent an `audio.provider` mute or default-sink durable writer. Audio troubleshoot restart applies through this session's `omarchy-fabric-session-apply` `audio-troubleshoot-restart` helper wrapping tip-true absolute `omarchy-restart-audio` FixedArgv. Soft leftover-attach of catalog `troubleshooting.audio.run` leftover `legacy-direct` / claim=partial with visible `Settings > Sound`. session leftover recorded: Settings Sound mute, default output, and audio restart session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Port changes and the full troubleshoot wizard remain OPEN leftover. Mute, default, or restart alone is not Sound product-complete. `windows-native.6` stays pending. `windows-native.39` stays prototype/pending. Superbar still hosts the heritage `omarchy.audio` panel. |
| Display | prototype | Settings → Display reads Fabric `display.inspect` (connector, mode, scale, position). Brightness applies through `display.provider` `brightness.set` on outputs with a controllable backlight (catalog id `display.brightness.set`, availability.claim=partial). Night light uses the same NightlightService session/heritage plane as Superbar / Quick Settings; Settings Display hosts that control and does not invent a `display.provider` night-light durable writer. Settings does not invent Fabric durable night-light LIVE / claim=present. Scale applies through this session's `omarchy-fabric-session-apply` `display-monitor-scale` path (`omarchy-hyprland-monitor-scaling` whitelist 1 / 1.25 / 1.6 / 2 / 3 / 4). Settings does not invent a `display.provider` scale durable writer. Settings does not invent Fabric durable scale LIVE / claim=present. session leftover recorded: Settings Display scaling session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). Resolution, arrangement, and HDR remain unavailable. `display.configure` stays the Superbar leftover. Catalog `display.scale.set` stays leftover `legacy-direct` / claim=partial with visible `Settings > Display`. `windows-native.3` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED; Cloud mocks do not close windows-native.3. Catalog `display.night-light.set` stays leftover `legacy-direct` / claim=partial with visible dual routes `Settings > Display; Superbar > Quick Settings > Night light` (Settings is the primary path; the QS leftover tile remains). `windows-native.34` stays pending. `parity.modern-display-scaling-hdr-night-light` stays prototype. Night light or scaling alone is not modern display complete. |
| Firewall | plumbing | `install/config/firewall.sh` / ufw on by default (`manual/35-networking.md`). Administration > Firewall is the visible host for bounded `firewall.inspect` inventory; inspect stays honest-unavailable as Firewall Settings product. It does not invent a Firewall Settings LIVE surface or `firewall.manage` UI. Claim stays plumbing; this row is not present. |
| Update | plumbing / prototype | Settings → Update reads Fabric `update.inspect` (available count, phase, checkpoint, reboot). Settings also checks and installs through this session's update helper (`omarchy-fabric-session-apply` `system-update`); elevated auth never enters Fabric. Fabric `system.update` stays inspect-only / not LIVE. Heritage `omarchy.system-update` widget loads in the Superbar cluster and shows only when `omarchy-update-available` reports pending updates. History is readable from this session's Omarchy update transcript through `omarchy-fabric-session-apply` `system-update-history` (`SettingsSessionUpdate.qml` `readHistory`; SettingsUpdateHistory chrome). soft leftover-attaches `windows-native.29` to that tip-true Settings > Update history plane. Catalog `update.history.read` stays leftover `legacy-direct` / claim=partial with visible `Settings > Update`. session leftover recorded: Inspect update history leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.29. Restart and reboot writers remain unavailable. `parity.update` stays prototype. Forty-task row 28 stays pending until human/metal proof; Cloud EXIT 0 is not metal leftover CLOSED. Forty-task "Inspect update history" stays pending. `windows-native.29` stays prototype/pending. |
| Backup & Restore | plumbing | Snapper via `omarchy-snapshot`. Administration > Backup is the visible host for bounded `backup.inspect` inventory; inspect stays honest-unavailable as Backup and Restore product. Writers (`backup.manage`) stay planned/unavailable. It does not invent a Backup and Restore product surface. Claim stays plumbing; this row is not present. |
| System Restore | plumbing | Settings → Recovery reads Fabric `recovery.inspect` (restore-point inventory). Same snapshot/rollback path. Restore planning and execution writers remain unavailable. Forty-task rows 30–31 pending. |
| Disk Management | missing | Fabric `storage.inspect` exists. Administration > Storage is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Disk Management product. Format, mount, and eject remain unavailable. Settings > System information also reads OS, product, hardware, and root storage through this session's `omarchy-fabric-session-apply` `system-information-inspect` path. Settings does not invent a `system-information.provider` durable writer. Settings does not invent Fabric durable `system.info.read` LIVE / claim=present. Catalog `system.info.read` stays leftover `legacy-direct` / claim=partial with visible `Settings > System information`. session leftover recorded: Settings System information session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). `windows-native.38` stays prototype/pending. Cloud mocks do not close windows-native.38. Agent Center bubblewrap `system.info.read` stays separate. Encryption, firmware flash, Device Manager, and storage mutation remain unavailable. Claim stays missing as product; this row is not present. |
| Task Manager | missing as product | `btop` is the Activity TUI (`manual/21-tuis.md`). Not Task Manager. Leftover `processes.inspect` (`processes.provider` / `legacy-direct` / `claim=partial` / Task Manager surface / btop source) is closed. Honest reader is `process.inspect` on `process.provider` (provider.state=present, availability.claim=partial / human=partial; FixedArgv `/usr/bin/ps` with `pcpu`). Administration > Processes is the visible host for that bounded CPU inspect inventory and for the End Task control path; inspect stays honest-unavailable as Task Manager product. session leftover recorded: Administration > Processes CPU inspect plane only / session-UI leftover only / not product CLOSED / not metal CLOSED / not claim=present. `windows-native.26` stays prototype/pending. Catalog writer `process.termination.plan` on `process.provider` is honest-unavailable (availability.claim=missing, consent high-risk / consequential; `terminationAuthorized=false`; End Task stays OPEN leftover / hidden). The write plane exists, the shell principal cannot authorize, and the UI stays unauthorized. Startup disable lives on Settings → Apps through this session's XDG autostart helper, not a Task Manager Startup tab. It does not invent Superbar or Administration Task Manager or End Task LIVE CONTROL. Claim stays missing as product; this row is not present. |
| Resource Monitor | missing | The leftover catalog keeps `resources.inspect` missing with an empty path; it does not invent Task Manager > Performance > Resource Monitor. |
| Services | missing | Fabric `service.inspect` exists. Administration > Services is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Services product. Start, stop, enable, and disable remain unavailable. Claim stays missing as product; this row is not present. |
| Task Scheduler | missing | Fabric `schedule.inspect` exists. Administration > Scheduled tasks is the visible host for that bounded inspect inventory; inspect stays honest-unavailable as Task Scheduler product. Creating and disabling a task remain unavailable. Claim stays missing as product; this row is not present. |
| Event / history | prototype (toasts + window ledger) | Notification history directory + `showHistory` replay as toasts. Window capability calls append `capability-ledger.json`. Not Event Viewer. |
| Remote desktop | missing as product | |
| Drive encryption | missing as product | |
| Sharing | missing as product | Forty-task SMB row pending. |
| Language / locale | prototype | Heritage `omarchy.keyboard-layout` loads in the Superbar cluster and shows only when a keyboard has more than one layout. Settings Input applies the active layout through this session's `omarchy-fabric-session-apply` `input-keyboard-layout` path (`/usr/bin/hyprctl switchxkblayout`, configured identities only). Settings does not invent an `input.provider` keyboard-layout durable writer. Settings does not invent Fabric durable keyboard-layout LIVE / claim=present. Catalog `input.keyboard-layout.set` stays leftover `legacy-direct` / claim=partial with visible `Settings > Input`. session leftover recorded: Settings Input keyboard layout session-UI leftover only (not product CLOSED / not metal CLOSED / not claim=present). `windows-native.33` stays prototype/pending. Cloud mocks do not close windows-native.33. Pointer, repeat rate, IME, and locale/region changes remain unavailable. Layout alone is not locale complete. Forty-task "Change keyboard layout" stays pending. |
| Accessibility | missing as product | Settings jump opens the honest missing Fabric page (`settings.accessibility.overview`); no `accessibility.provider`. Start search does not invent it as a destination. Reduced motion and high contrast live in tokens. Ease of Access engine remains unavailable. |
| File associations | missing as product | Settings → Apps reads Fabric `defaults.inspect` MIME inventory and applies per-MIME defaults through `defaults.provider` `mime.set` (catalog id `defaults.mime.set`; risk low; writable associations with more than one installed candidate). `association.inspect` is published so durable apply can re-read scoped association state. `files.associations.set` stays missing/planned MIME. Claim stays missing as product; this row is not present. |
| Properties | missing as product | Session read-only Properties sheet via this session's `omarchy-fabric-session-apply` `files-entry-properties` helper (visible `Files > right-click > Properties`; catalog `files.properties.read` leftover-direct / claim=partial). Files does not invent a Fabric SHELL LIVE Properties writer. `parity.properties` stays missing as product. Recycle Properties, attribute toggles, NTFS ADS, and permissions UI stay leftovers. This row is not present. |
| Context menus | prototype | Superbar jump lists, Start pin/jump menus, and peek menus exist. Not "right-click everywhere" as the Windows API. Delete/Trash Fabric control gated off (`trashAuthorized=false`); write plane reachable but not shell-authorizable. Session Delete moves a selected item to Trash through this session's helper. Restore stays honest-unavailable under SHELL; session Restore is the Recycle Bin path. Desktop Recycle and a full desktop context menu remain unavailable and are not product-complete. |

## 2026 layers

| Job | Status | Tree notes |
|-----|--------|------------|
| Software Center | missing | Missing product; plan Phase 7 taxonomy. Pacman/AUR/Flatpak are CLI/menu today. No writer is waiting on a phase gate. |
| Compatibility Center | missing | Missing product; plan Phase 8 taxonomy. No "install one known-compatible .exe" path. No writer is waiting on a phase gate. |
| Snapshot recovery | plumbing | `omarchy-snapshot`, Limine snapper restore. Consumer "restore point" UI missing. |
| Modern display (scaling, HDR, night light) | prototype / plumbing | Monitor panel + `omarchy.nightlight` service. Settings Display and the QS Night light tile share the NightlightService leftover; forty-task night light stays pending; scaling/HDR remain separate. Do not claim present. |
| Proton / gaming | missing | Steam install is forty-task pending; no Gaming / Proton surface. |
| Privacy | missing | Doctrine refuses telemetry; no Privacy Settings. |
| Agent Fabric | prototype (window) | See `AGENT_NATIVE_ACCEPTANCE.md`. WindowService + `CapabilityBroker` (results, permit, ledger, window undo). Settings Display, Sound, Network, Bluetooth, Power, Apps, Update, and Recovery read `display.inspect` / `audio.inspect` / `network.inspect` / `bluetooth.inspect` / `power.inspect` / `defaults.inspect` / `update.inspect` / `recovery.inspect`. Superbar still hosts audio/network/bluetooth/power panels. Live Settings writers are Sound volume, Network Wi-Fi radio, Display brightness, Input layout, and Apps default browser; Bluetooth pair and connect use this session's BlueZ adapter; Power profile stays inspect-only because polkit cannot authorize the fabric daemon under app.slice; the Superbar Power leftover was unverified on metal after PR #31; QS Power METAL_HEAD OPEN after metal FAIL on tip `20484de6` (pkcheck Not authorized / session-5103; !batteryPresent; amd_pstate EINVAL); KEEP OPEN; Settings Power LIVE refused; remaining Settings verbs stay unavailable. Jobs stay prototype/pending. |
| Agent Center | prototype | `org.omarchy.AgentCenter` is a snappable Superbar pin and Start destination. The pin jump list publishes Overview plus every existing inspect page: Tasks, Approvals, Automations, Activity, History, Context, Usage, Permissions, Providers, Artifacts, and Troubleshooting. Start search still opens Agent Center itself rather than inventing a second Overview destination. Overview reads owner-scoped managed-work counts from Fabric. Inspect tasks can be created, run, cancelled, and recovered for `system.info.read`. Context capture covers the five desktop sources. Consent and provider operations stay outside Agent Center. Usage stays one section. `omarchy.agents` remains a launch shim. |

## Caption / windowing (not a substitute for the jobs above)

Windowing Gate W0 is Phase 1 of `plans/project-ultimate.md` and is certified only by `W0_GATE.md` (Hyprland stays; three-button SSD, one-row CSD, min/max/restore, LTRB snap, Alt+Tab, Show Desktop, reopen memory, click-to-raise, Start click-through). The OS remains rejected because the product jobs above are incomplete. Founding execution authority is `PRODUCT_DOCTRINE.md` and `plans/project-ultimate.md`.

## Win7 ground-truth pointers (honesty only — statuses unchanged)

Concrete acceptance criteria for each job live under the **FULL CORPUS** in `plans/win7-ultimate-ground-truth/` (`01`–`06` md+json + `fleet/`) and are indexed by `plans/WIN7_SPEC_INDEX.md`. Full pointer table: `plans/win7-ultimate-ground-truth/_PARITY_ACCEPTANCE_POINTERS.md`.

**Caption BINDING LOCK:** restored visual top NC **30** px; `SM_CYCAPTION` **22** is metric band only (never ship a 22 px title bar). See `plans/win7-ultimate-ground-truth/01-window-chrome.md`.

Do **not** read these pointers as a status upgrade. Product remains **REJECTED**.

- Honesty addendum 2026-09-06 vs Settings Update history leftover plane (windows-native.29): session leftover recorded: Settings > Update can read this session's Omarchy update transcript (`/tmp/omarchy-update.log`) through `omarchy-fabric-session-apply` `system-update-history` (`SettingsSessionUpdate.qml` `readHistory`; SettingsUpdateHistory chrome). Empty state is honest when no transcript exists. Structured failures reuse in-tree analyze-logs patterns (initramfs) plus transcript failure markers. Elevated auth never enters Fabric durable evidence. Fabric update.inspect / system.update stay inspect-only / not LIVE apply. Catalog `update.history.read` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Update` (never claim=present). Soft leftover-attach ACC `windows-native.29` to that tip-true Settings > Update history plane. `parity.update` stays prototype. windows-native.28 stays missing/pending. `windows-native.29` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. Soft leftover-attach: Settings > Update history exists (tip-visible); not product CLOSED / not metal CLOSED / not claim=present. session leftover recorded: Inspect update history leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.29. OPEN leftovers: metal proof, Update present, windows-native.28 metal, Fabric LIVE, Restart/reboot writers, Files LIVE metal, Win7 visual, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Software Center present, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Fabric LIVE. Do not invent Restart/reboot writers. Do not invent Win7 visual closed. Do not walk `update.history.read` or windows-native.29 to present. Product REJECTED.

- Honesty addendum 2026-09-06 vs Settings Personalization wallpaper leftover plane (windows-native.4): session leftover recorded: Settings can change the wallpaper from Settings > Personalization through the tip-true `omarchy.image-picker` / `SettingsPersonalizationHost` wallpaper plane (`ImagePicker.qml` `applyEmbedded` → `omarchy-theme-bg-set`). recoveryExpectation tip-true set-only applyEmbedded → omarchy-theme-bg-set (mode=none; no prior-wallpaper undo / no compensating fingerprint invent). Catalog `desktop.wallpaper.set` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Settings > Personalization` (never claim=present). Soft leftover-attach ACC `windows-native.4` to that tip-true Settings > Personalization wallpaper plane. `parity.personalization` stays prototype. Typed personalization.provider writers for density, cursor, motion, and a full theme service remain unavailable. `windows-native.4` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Change the wallpaper leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.4. OPEN leftovers: metal proof, density/cursor/motion/full theme service, Win7 Personalization bottom-row product, Files LIVE metal, Win7 visual, Settings Power LIVE, Empty Bin LIVE, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent Win7 visual closed. Do not invent wallpaper product-complete or Win7 visual closed from hex-grep. Do not walk `desktop.wallpaper.set` or windows-native.4 to present. Product REJECTED.


- Honesty addendum 2026-09-06 vs Start Shut down leftover plane (windows-native.40): session leftover recorded: Start can shut down from Start > Shut down through the tip-true `shell/plugins/ultimate-start/Start.qml` `Start.shutdown` plane (`Util.execDetached("omarchy-system-shutdown")` and the power flyout Shut down row → `omarchy-system-shutdown` → systemd-run user timer → `systemctl poweroff`). recoveryExpectation tip-true irreversible poweroff with no undo / no compensating fingerprint invent (visible Shut down choice is the consent gesture). Catalog `power.shutdown` stays leftover `legacy-direct` with claim=partial, human=partial, and visible `Start > Shut down` (never claim=present; source stays `Start.shutdown`). Soft leftover-attach ACC `windows-native.40` to that tip-true Start > Shut down plane. `parity.start` stays prototype. `windows-native.40` stays prototype/pending; Cloud EXIT 0 is not metal leftover CLOSED. session leftover recorded: Shut down leftover-attach only (not product CLOSED / not metal CLOSED / not claim=present). Soft Ship park: leftover-attach before citing suite EXIT 0 as metal; Cloud mocks do not close windows-native.40. OPEN leftovers: metal proof, Settings Power LIVE, Empty Bin LIVE, End Task LIVE, Files LIVE metal, Win7 visual, Restart/reboot writers as product-complete, claim=present. Do not invent claim=present. Do not invent Settings Power LIVE. Do not invent Empty Bin LIVE. Do not invent End Task LIVE. Do not invent Win7 visual closed. Do not invent Cloud EXIT 0 as metal leftover CLOSED. Do not walk `power.shutdown` or windows-native.40 to present. Product REJECTED.